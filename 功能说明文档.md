# 金融工作台 — 功能说明文档

> 本文档**系统记录本项目的全部功能**，并作为**功能更新日志**持续维护。每新增/修改功能时，须同步更新本文档对应章节与文末"版本更新记录"。
>
> 适用范围：个人投资者（单用户、免登录），投资/交易工作台 + 资产组合管理，**仅做记账和行情查看，不接券商真实下单**。

---

## 目录

- [一、应用概况](#一应用概况)
- [二、功能清单](#二功能清单)
  - [1. 行情看板](#1-行情看板)
  - [2. 指数 K 线 + 技术指标](#2-指数-k-线--技术指标)
  - [3. 场内基金穿透](#3-场内基金穿透)
  - [4. 持仓管理](#4-持仓管理)
  - [5. 组合分析](#5-组合分析)
  - [6. 报表导出](#6-报表导出)
  - [7. 财经资讯](#7-财经资讯)
  - [8. 市场覆盖](#8-市场覆盖)
  - [9. 跨平台部署与局域网访问](#9-跨平台部署与局域网访问)
- [三、数据源与数据真实性](#三数据源与数据真实性)
- [四、技术栈](#四技术栈)
- [五、维护约定](#五维护约定)
- [六、版本更新记录](#六版本更新记录)

---

## 一、应用概况

- **业务定位**：投资/交易工作台 + 资产组合管理。仅记账与行情查看，不接券商真实下单。
- **目标用户**：个人投资者（单用户、免登录）。
- **应用形态**：Web 网页应用，本地 `localhost` 运行，单端口访问（默认 `8000`）。
- **支持系统**：Linux / Windows 双平台（Python 3.7+）。
- **数据存储**：SQLite（本地单文件，随应用自动生成）。

---

## 二、功能清单

### 1. 行情看板

| 功能 | 说明 |
|---|---|
| 实时国内金价条 | 人民币/克，**优先用沪金 Au99.99（SGE 现货 `hf_AU9999`）价格，取不到回退沪金连续（上期所 `nf_AU0`）**；显示来源名与更新时间（asof，如 `2026-08-24 15:00`），非交易时段显示最近成交价属正常；**v32fix 起更新时间旁新增交易状态小字**（如 `交易中（日盘）`/`午间休市 · 13:30 恢复`/`周末休市`，悬停可见该金价源的完整交易时段说明——跟随当前实际源：沪金连续=上期所黄金期货日盘 09:00-10:15/10:30-11:30/13:30-15:00 夜盘 21:00-次日 02:30；Au99.99=上金所现货，法定节假日以交易所公告为准），价格在午休/收盘段冻结属正常现象；两者皆无则「数据暂不可用」；**v32 起持有实物黄金时，金价条尾部展示「实物黄金 X 克 · 市值 · 持仓盈亏 · 累计盈亏 · 当日盈亏」三项盈亏**（持仓盈亏=市值−成本，累计盈亏=已实现收益+持仓盈亏，**v32fix：持仓盈亏与累计盈亏均与持仓管理页实物黄金卡片完全同源——含用户在持仓卡片手动编辑的累计/持仓收益覆盖**，当日盈亏=克数×(今价−沪金AU0昨收)；三项数值正数红色、负数绿色，随行情自动刷新） |
| 国内外指数大字卡 | 可展开/收起，覆盖国内外主要指数 |
| 自选基金小字列表 | 每行一个；**自选基金 = 自选列表 FUND + 持仓管理已持仓基金**（合并去重）；仅在首次初始化预置默认基金，用户删除后不自动重刷；**点击表头「日涨跌幅」可切换升/降序（默认降序）**，行情不可用者沉底 |
| 基金详情弹窗 | 净值、持仓分布饼图、前十重仓近期行情、**净值K线**（天天基金单位净值历史）、场内穿透估算与折溢价 |
| 场内基金穿透概览 | （**v23 起行情看板暂不展示该独立模块**，保留代码可由 `SHOW_PENETRATION` 开关恢复）持仓基金覆盖率/估算涨跌/折溢价，点击看详情；单只基金穿透详情仍可在「基金详情弹窗」查看 |
| 自选标的添加 | 支持 A股 / 美股 / 港股 / 黄金 / 基金（含场外基金）/ 债券 / 指数 |
| 自选标的列表 | 行情看板「自选标的」表，每行一个（A股/美股/港股/债券/指数等）；**点击表头「涨跌幅」可切换升/降序（默认降序）**，行情不可用者沉底 |
| 自选基金/标的「预估日收益」列 | 行情看板「自选基金」与「自选标的」两表**新增「预估日收益」列**（v26）：= 当日涨跌幅(%) × 当前持仓市值（持仓市值来自「持仓管理」聚合，**基金按 NAV 计算**，与组合汇总同源；无持仓则显示「—」）。该列**可点击表头排序**，且与「日涨跌幅」排序相互独立（默认降序、缺失值沉底），点击在同列内切换升/降、异列默认降序，带 ↑/↓ 箭头指示 |
| **模块「当日收益总和」（v31）** | 行情看板「自选基金」与「自选标的」**各自模块标题右侧（异动阈值左侧）新增「当日收益总和」显示**：= 该模块内**所有有持仓产品**的预估日收益合计（口径与「预估日收益」列一致 = 日涨跌幅 × 持仓市值；自选基金模块含自动同步的持仓基金；两模块**各算各的**）。**正收益数字红色、负收益数字绿色**（与全站涨跌配色一致）；无任何持仓收益时显示 `¥0.00` 灰色；悬停提示计算公式 |
| 涨跌幅异动跟踪 | 行情看板「自选基金」「自选标的」模块均提供**异动跟踪阈值设置（1%~10%，默认 5%，localStorage 持久化）**（v26）；当某产品当日**涨幅 > 阈值 或 跌幅 < −阈值**时，该行**高亮标红**并在名称后标「异动」徽标，同时看板顶部弹出**异动提醒横幅**列出所有触发产品（名称 + 涨跌幅），涨跌幅回落阈值内自动取消标亮与横幅 |
| 智能代码识别 | `guessMarket` 识别：`sh/sz` 前缀=A股，`hk`=港股，字母=美股，6位数字中 `15/16/50/51/52/56/58/159` 开头=场内基金，`0` 开头优先=场外基金，`6/3` 开头=A股 |

### 2. 指数 K 线 + 技术指标

| 功能 | 说明 |
|---|---|
| K线面板 | 日K/周K/月K/年K/分钟K（v31 起支持周期切换，见下表「周期切换与分时」），三面板布局，**每面板左上角标注图名**：`K线 · MA`（MA5/10/20）、`成交量`（独立y轴，亿刻度）、`MACD`（DIF/DEA/柱状）。**v18 起全图统一红涨绿跌**：K线阳线（上涨）红色、阴线（下跌）绿色，成交量与 MACD 柱随涨跌同色，与全站行情配色（`--up` 红 / `--down` 绿）一致 |
| 市场覆盖 | A股 / 美股 / 港股 / 北交所（如北证50 `bj899050`） |
| 美股指数 K 线 | 内置与**自定义**美股/海外指数均走腾讯 `usfqkline` 完整历史（120天）；费城半导体等腾讯确实无数据的指数明确提示"该指数历史K线数据暂不可用" |
| 国内/港股指数 K 线 | 默认腾讯 `fqkline` 取历史；当腾讯返回不足（1天空壳/WAF 拦截）时**自动回退新浪 `sina_kline`** 取完整历史（如北证50 `bj899050` 稳定 120 天）；代码格式偏差返回"K线数据为空"而非崩溃 |
| **图例与划线颜色一致（v31）** | 图例色块颜色与实际渲染色**严格一一对应**（此前图例取 ECharts 默认调色板色，与实际线色完全无关）。单色系列显式指定 `legend.data[].itemStyle.color`：MA5 `#d29922` 黄 / MA10 `#58a6ff` 蓝 / MA20 `#a371f7` 紫 / DIF `#58a6ff` 蓝 / DEA `#d29922` 黄；K线 / 成交量 / MACD 三个**红涨绿跌双色**系列改用**内嵌 SVG 双色图例图标**（左红 `#f85149` 右绿 `#3fb950`）；取消勾选时双色图标同步置灰 `#ccc`（image 图标 ECharts 不会自动置灰，由 `legendselectchanged` 回调以 merge 模式重建 `legend.data` 实现，不重置其它图例的选中态）。全部颜色统一由 `frontend/src/components/KlineChart.jsx` 导出的 `KL_COLORS` 常量提供（含买卖点 markLine 的买入 `#ff8c00` 橙 / 卖出 `#1e90ff` 蓝），弹窗头部买卖点图例与图内标注共用同一常量，**消除两处硬编码漂移** |
| **周期切换与分时（v31）** | 所有 K 线弹窗顶部新增周期 tab：**分时｜1分｜5分｜15分｜30分｜60分｜日K｜周K｜月K｜年K**，**默认显示当日分时走势**（A股/港股有真实源；无分时源的市场自动落回日K并提示）。**历史深度大幅扩展**（此前仅 120 根≈半年）：日K 1000 根档（个股前复权源上限约 641 根≈2.6年，指数/港股≈4年）、周K 640 根≈12年、月K 500 根档（上证自 1990-12 基点起 36 年、个股自上市起）、**年K由真实月K聚合**（上证 37 年、茅台 26 年）；分钟K 仅 A股（含指数）有真实源；分时含价格线（红涨绿跌）、均价线（黄，指数无均价概念不显示）、昨收虚线、底部当分钟量柱；腾讯 K 线接口域名切至 `proxy.finance.qq.com`（原 `web.ifzq.gtimg.cn` 会触发 WAF 501），并修正腾讯 K 线高低位列序（`[开,收,高,低]`→`[开,收,低,高]`，与新浪逐日核对一致） |
| **K线默认视野（v32）** | 打开K线/切换周期时**默认聚焦最近日期窗口**（数据仍按原档位拉满，可拖动回看更早历史）：**日K=近6个月 / 周K=近1年半（18个月）/ 月K=近5年 / 年K=近10年**，**所有标的统一生效**（指数/个股/基金/港股/黄金一致）。实现：以「最后一根K线日期」向前回推 N 个月为视野起点（ECharts dataZoom `startValue/endValue` 精确对齐自然日期、随最新数据自动平移），历史不足窗口长度时自动显示全部；分钟K（1分~60分）无日期窗口语义，维持原默认比例（后 60%） |
| **指数卡片迷你分时与本地跟踪（v31）** | 行情看板**全部指数卡片**在当日涨幅文字下方显示**迷你分时小图**（SVG 折线，涨红跌绿，每 60s 刷新；title 标注数据日期与来源），点开指数卡片**先显示当日分时**（每个新交易日重置）。分时统一数据链：**腾讯当日实时 → 东财最近交易日（美股指数休市时段可显示上一交易日全天分时）→ 本地跟踪文件**；**确无任何真实分时源的指数**（如纳指综合 IXIC/费半 SOX 在 A股白天）由后端后台线程**每分钟在其实时交易时段用新浪报价采样**，写入 `data/intraday_track/` 本地文件供分时图保留统计数据，**每个新交易日自动重置**（休市日不采样、文件保留上一交易日数据并标注日期）；源恢复可用时自动退出跟踪名单；无源且暂无记录时卡片显示"分时·暂无（每交易日自动记录）"占位并明确提示；**v32 起黄金（纽约金 hf_GC / 沪金 nf_AU0 / 伦敦金 hf_XAU 等 GOLD 期货指数）纳入本地记录**——其无外部逐分钟源，系统运行期间每分钟记录其当日实时价作为分时数据源（卡片占位改「分时·记录中」，点开 K 线弹窗等待本地记录满 2 点自动显示当日分时曲线）（本地跟踪文件为行情衍生记录，不纳入持仓数据导入导出） |

### 3. 场内基金穿透

基于基金**季报前十大重仓股 + 盘中实时股价**的加权估算，**分档策略**显著降低低覆盖率误差：

| 指标 | 说明 |
|---|---|
| 估算涨跌 `est_pct` | 覆盖率 ≥ 60%：直接用重仓股涨跌幅 × 占比加权；覆盖率 < 60%：未覆盖部分用**基金近20日平均日涨跌**（中性化动量）填充，避免把未覆盖持仓按 0 涨跌计算导致的系统性偏差 |
| 校准模式 `calib_mode` | `direct`（高覆盖直接加权）/ `calibrated`（低覆盖历史动量校准）/ `no_coverage`（无覆盖） |
| 覆盖率 `coverage` | 已匹配实时行情的重仓股占比总和；覆盖率低时前端提示校准说明 |
| 报告期 `report` | 从季报 HTML 提取"截止至：YYYY-MM-DD" |
| 折溢价 `premium` | (场内价 − 净值) / 净值 |
| 重仓股贡献归因 | 逐行展示最新价/涨跌幅/对基金贡献 |
| 排序 | **v16 起**持仓基金概览按**当天收益**（估算涨跌 `est_pct` 降序，缺失用实际净值涨跌 `pct`）排序；穿透失败（`available=False`）的基金垫底 |
| 覆盖范围 | `/api/fund/penetration/holdings` 包含两类持仓：① 交易记录中 `market=FUND` 的基金；② **v16 起**通过「新增持仓」录入的基金（`position_override` 中 `market=FUND`）——确保直接录入的场内/场外基金也能进入穿透估值与组合分析饼图 |
| 重仓解析 | **v18 起**按 td 位置通用解析，兼容 A股（6 位代码）、美股（字母代码，QDII/跨境 ETF 如纳指/标普成分）、港股（5 位代码）三类持仓行；重仓股实时行情按代码自动映射新浪 symbol（A股 `sh/sz`、港股 `hk`、美股 `gb_`），因此 QDII/跨境 ETF 也能正常穿透估算 |
| 名称显示 | 穿透列表每张卡片均显示**基金名称 + 代码**；即使穿透失败（无法获取持仓）也会尽力补齐基金名称（新浪 `fu_` 报价），不再只显示孤立的代码 |
| 接口 | `/api/fund/penetrate`（单基金）、`/api/fund/penetration/holdings`（持仓基金概览） |

> 验证示例：161725（白酒）覆盖率 85.95%，`direct` 模式，估算涨跌与实际高度接近；510300（宽基）覆盖率仅 23.25%，`calibrated` 模式，用基金历史动量校准未覆盖部分，估算更稳健。

### 4. 持仓管理

| 功能 | 说明 |
|---|---|
| 交易录入 | 市场/代码/方向/数量/价格/费用/日期，按加权平均成本自动聚合持仓并计算实时盈亏 |
| 持仓明细 | **可直接编辑数量/成本**（写入 `position_override` 表）、**按持仓成本排序**、**直接删除**（删除持仓同时删除其全部交易记录），已编辑项标注"已编辑"徽标 |
| **新增初始持仓** | **直接录入初始持仓**：输入产品代码 + 持有金额（当前市值）+ 持有收益（浮动盈亏），后端用**当日价格自动计算**持仓数量（= 持有金额 / 价格）与成本（= 持有金额 - 持有收益），存入 `position_override`，无需通过交易记录 |
| **批量导入持仓** | 新增持仓弹窗支持**单条 / 批量**两种模式切换；批量模式整批指定市场后**多行粘贴**（每行 `代码 持有金额 [持有收益]`，空格 / Tab / 逗号分隔，持有收益可留空），实时显示解析条数，逐条调用批量接口，并**展示每条导入结果**（✅ 成功含现价/数量/成本，❌ 失败含原因） |
| 基金确认份额规则 | 录入**基金**交易时显示"交易时间"选择器（15:00 前/后）；**系统自动按代码识别 QDII（非国内）基金**（内置 119 个常见 QDII 代码），QDII 确认份额固定 **T+2**（不受 15:00 前后影响，但**仍显示交易时间选择器仅作记录**并显示 🌍 QDII 标签）；境内普通基金 15:00 前 → **T+1**、15:00 后 → **T+2**。**确认份额日期计算跳过周末与法定节假日（非交易日）**：基于沪深北交易所官方休市安排（2026 年已内置，来源见下表），由后端 `/api/fund/confirm-date` 计算并返回。提供**手动勾选兜底**：即使代码不在内置清单中，用户也可手动勾选"非国内基金（QDII）"，确保未收录代码也能正确标记 T+2 |
| 确认信息写入备注 | 保存交易时把交易时间与确认份额日期（含 QDII 判定）写入备注便于追溯 |
| 实物黄金 | 独立卡片编辑持有克数与成本价（元/克），实时按国内金价计算市值与盈亏，可清空；**v16 起支持买卖交易流水**（方向/克数/价格/日期/备注），按加权平均成本更新克数与成本价，**自动计算已实现收益**（卖出 (卖价−均价) × 克数累计）；删除任意一笔交易后，按剩余流水重放重建持仓与已实现收益。**v18 起**卖出后的已实现收益计入该持仓的**累计收益**（累计 = 已实现 + 实时持仓收益），卖出落袋的收益不再从累计中消失 |
| 卖出收益口径 | **v18 修复**：卖出后**已到手收益单独记录**（`realized_pnl`，股票/基金来自交易记录 `compute_realized_pnl`，实物黄金来自黄金流水 `compute_gold_realized_pnl`），**当前持仓收益按剩余持仓实时重算**，累计收益 = 已实现收益 + 当前持仓收益，卖出本身不再导致累计收益异常减少；同时持仓聚合按 (成交日期, id) **升序重放**（此前误用倒序导致卖出早于买入处理、持仓不减） |
| 编辑交易 | 支持修改已录入的交易记录、删除交易 |
| **累计收益联动** | **v16 起**编辑累计收益时，把"编辑值 − 当前实时持仓收益"保存为**历史收益基准**；之后累计收益 = 历史收益基准 + 实时持仓收益，随行情自动联动更新 |
| **持仓卡片双栏收益** | **v32 起**：每张持仓卡片顶部收益区改为**两栏并列**，以文字标签区分两种收益类型——左栏「**当前持仓收益**」金额 + 涨跌幅%，右栏「**累计收益**」金额 + 构成说明（普通持仓「含已实现收益」、已手动编辑「已手动编辑」、已清仓「全部已实现收益」），正数红色、负数绿色；**已清仓卡片统一同版式**（当前持仓收益显示 0.00 并注明「已清仓 · 无当前持仓」），移除原底部单独的累计收益行 |

### 5. 组合分析

| 功能 | 说明 |
|---|---|
| 汇总 | 总市值 / 成本 / 盈亏（含实物黄金） |
| **当日收益卡片（v32）** | 总览（Dashboard）页「总成本」与「当前持仓收益」卡片之间**新增「当日收益」卡片**：= 当日**所有持仓产品**（股票/基金/实物黄金等，已清仓不计）`数量×(现价−昨收基准价)` 的**实时加总**（实时行情口径，盘中随行情跳动），数字**正收益红色 / 负收益绿色**（全站配色）；卡片副行显示「N 项已计 · 含估算 · N 项缺失」。**点击卡片弹出当日收益明细弹窗**：每产品一行（名称+市场·代码、当日涨跌幅%、当日收益额，按收益降序），基金官方净值当日未更新时为新浪估算净值并标「估算」、实物黄金昨收取沪金 AU0 日K前一交易日收盘，行情缺失的产品在下方列「未计入」及原因、绝不填充虚拟值，底部注明口径。**新增接口 `GET /api/portfolio/day-pnl`**（只读实时计算，不落库、无需历史快照） |
| 分配 | 按市场 / 标的查看持仓分配；**v16 起**基于统一口径（与组合汇总同源，含持仓覆盖/新增持仓/实物黄金），保证「直接录入的基金/商品」也能正确进入饼图 |
| 净值曲线 | 组合净值曲线与最大回撤：**每日快照自动记录**（组合页每天首次打开时自动写入当日总市值/总成本，snapshots 表每日一条、幂等），随使用天数自然积累；支持 all / 最近30 / 最近90 天查看 |
| 累计收益 | 持仓收益 / 累计收益（支持手动编辑并持久化；**v16 起**编辑累计收益后与实时持仓收益自动联动） |
| **收益分析** | **v23 新增，v24 修正计算口径与配色**：组合分析页「收益分析」模块，**可选日收益 / 月收益 / 年收益 / 累计收益（默认日收益）**；范围选择（日=日期、月=月份、年=年份，默认今天/本月/今年）；**图表可选柱状图 / 日历图（默认日历图）**。**v24 计算口径（统一以「日收益」为单元）**：① **日收益** = 仅取「上一个交易日 → 当日」这一对相邻快照，即所有持仓当日收益的总和；② **月/年收益** = 累加所选范围内**每一天的日收益**（而非首末差值）；③ **累计收益** = **所有持仓产品的累计收益总和**（每个持仓 最新市值 − 最早市值，再加总），与收益明细严格自洽。所有数值**全部基于真实的每日净值快照计算**（无虚拟数据），区间内任一天某持仓无快照则跳过该日避免误导。日历图为每日组合收益热力日历、柱状图为每持仓区间收益；**v24 起配色与全站一致：红色=赚（正收益）、绿色=亏（负收益）**（修正此前红跌绿涨）。模块跟随页面自动刷新频率实时更新（当日净值更新到最新后自动刷新）。**数据前提**：需组合分析页积累至少 2 个交易日快照才有日收益数据，历史天数不足时诚实提示（如"暂无相邻交易日快照，无法计算日收益"）。 |

### 6. 报表导出

| 功能 | 说明 |
|---|---|
| 持仓报表 | 一键导出持仓 CSV（含实物黄金） |
| 交易明细 | 一键导出交易记录 CSV |

### 7. 财经资讯

| 功能 | 说明 |
|---|---|
| 新浪滚动财经新闻 | 浏览最新滚动财经新闻，时间戳格式化为 `YYYY-MM-DD HH:MM`，支持关键词搜索 |

### 8. 市场覆盖

| 市场 | 支持标的 |
|---|---|
| A股 / 场内 | 股票、场内基金（ETF/LOF） |
| 美股 | 个股 |
| 港股 | 个股 |
| 黄金 | 纽约金 / 沪金 / 伦敦金 / 实物黄金 |
| 公募基金 | 场内外基金（含 QDII 识别） |
| 债券 | 可转债 |
| 指数 | 国内外主要指数 |

### 9. 跨平台部署与局域网访问

| 能力 | 说明 |
|---|---|
| Linux 部署 | `setup_env.sh`（环境配置）+ `operation.sh`（启动/停止/重启/状态） |
| Windows 部署 | `setup_env.bat`（环境配置）+ `operation.bat`（启动/停止/重启/状态） |
| 局域网访问 | 默认监听 `0.0.0.0`，启动脚本自动探测本机**局域网 IP** 并打印访问地址；同一局域网内其他设备可打开 `http://局域网IP:8000` |
| 仅本机访问 | Linux：`bash operation.sh restart 8000 127.0.0.1` |
| 防火墙说明 | Linux 需放行 8000 端口（ufw/firewalld）；Windows 首次启动弹窗勾选"允许访问"或手动放行入站 TCP 8000 |
| 多端共享 | 同一份 SQLite 数据库，建议单端录入、多端查看 |

### 10. 数据导入导出（跨机器迁移）

| 功能 | 说明 |
|---|---|
| 导出 | 把当前机器上的**全部用户数据**（自选 / 交易 / 净值快照 / **持仓级每日快照（收益分析历史明细）** / 实物黄金 / 收益编辑覆盖 / 持仓覆盖 / 持仓置顶 / 行情看板指数配置）汇总为 JSON，写入**项目根目录**下的 `持仓数据_export.json` 并触发浏览器下载；点击"导出数据"即可 |
| 导入 | 把导出文件放到**项目根目录（同路径）**后，点击"导入数据"即可**整体恢复**数据库（先清空当前数据再写入，保证与导出时一致）；导入前会二次确认，导入后展示各类数据统计 |
| 接口 | `GET /api/data/export`（生成并下载）、`GET /api/data/export/info`（扫描根目录备份文件信息）、`POST /api/data/import`（从根目录文件整体导入） |
| 文件说明 | 导出文件含用户数据，已加入 `.gitignore` 不进版本控制；跨机器迁移时把该文件拷贝到目标机器项目根目录即可 |
| 数据范围 | 覆盖全部用户输入数据，**不含**实时行情/K线等动态数据（导入后自动重新拉取） |

---

## 三、数据源与数据真实性

- **数据源**：新浪 `hq.sinajs.cn`（实时行情）、腾讯 `qt.gtimg.cn` / `web.ifzq.gtimg.cn`（实时行情 + K线）、天天基金 `fundf10.eastmoney.com`（季报持仓）、新浪新闻 `feed.mix.sina.com.cn`（财经资讯）。
- **中国 A股 休市安排（确认份额日期用）**：来源为**沪深北交易所官方休市安排公告**（经东方财富转发，权威真实），2026 年数据见 [`stock.eastmoney.com/a/202512223598373130.html`](https://stock.eastmoney.com/a/202512223598373130.html) 与 [`finance.eastmoney.com/a/202512223598652817.html`](https://finance.eastmoney.com/a/202512223598652817.html)；该数据属公开固定的官方规则（非行情虚构数据），以内置集合形式载入 `datasource.py` 的 `CN_A_SHARE_HOLIDAYS`，用于"确认份额日期"跳过非交易日。未收录年份（历史/未来）不保证真实，前端会提示"休市安排待更新"。
- **接口符号规则**：A股 `sh600519`、美股个股 `usAAPL.OQ`/`gb_aapl`、美股指数 `usIXIC`/`gb_ixic`、港股 `hk00700`、黄金 `hf_GC`/`nf_AU0`/`hf_XAU`、基金 `fu_161725`。
- **真实性承诺**：所有数据来自真实接口，获取失败明确提示"数据暂不可用"，**绝不填充虚拟数据**。仅作参考，不做投资建议。

---

## 四、技术栈

| 层 | 技术 |
|---|---|
| 前端 | React 18 + Vite + ECharts 5 |
| 后端 | FastAPI 0.99.1 + uvicorn 0.22.0（Python 3.7 兼容锁定） |
| 数据 | SQLite（本地单文件） |
| 运行 | 单端口（默认 8000），前端由 FastAPI `StaticFiles` 托管 |

---

## 五、维护约定

> 以下为本项目**功能文档维护规范**，开发/交付时必须遵守。

1. **每次功能更新，必须同步更新本文档**：
   - 新增功能 → 在 [二、功能清单](#二功能清单) 对应模块补充条目；
   - 修改功能 → 更新对应条目的说明；
   - 移除功能 → 删除对应条目并注明。
2. **每次功能更新，必须在 [六、版本更新记录](#六版本更新记录) 顶部新增一行记录**，注明：版本号、日期、功能变更摘要。
3. **涉及接口变更**（新增/修改 `/api/*`）时，在对应功能条目注明接口路径，便于排查。
4. **涉及部署方式变更**时，同步更新 [9. 跨平台部署与局域网访问](#9-跨平台部署与局域网访问) 及根目录 `README.md`。
5. **数据源变更**时，同步更新 [三、数据源与数据真实性](#三数据源与数据真实性)。
6. 打包交付时，本文件必须包含在交付包内。

---

## 六、版本更新记录

### v32（2026-09-04）
- **fix·低（v32 需求2）：持仓管理卡片顶部收益区两栏并列——「当前持仓收益」右侧新增「累计收益」，文字区分两种收益类型**：① **需求**：持仓管理界面中的每张持仓卡片，把该持仓的**累计收益**也显示出来，显示在**当前持仓收益的右边**，并用**文字区分两种收益类型**。② **现状与口径确认（2 项，均已与用户确认）**：后端 `/api/positions` 每个持仓本就携带 `holdingPnl`（当前持仓收益，别名 `pnl`）与 `cumPnl`（累计收益=已实现收益+当前持仓收益，可被用户覆盖 `cumPnlEdited`）双字段（口径与明细表 ProfitDisplay 及总览一致），但卡片此前只大字展示当前持仓收益且**无文字标签**、累计收益仅在"已清仓"卡片底部以独立行出现 → 本需求纯前端改造。用户确认：a) 版式=**两栏并列**——左栏标签「当前持仓收益」金额+涨跌幅%，右栏标签「累计收益」金额+构成小注，栏间以竖虚线分隔，金额正红负绿（沿用 `up/down` 全局色）；b) "已清仓"卡片**统一改为同版式**（当前持仓收益显示 0.00、小注「已清仓 · 无当前持仓」，累计收益=全部已实现收益、小注「全部已实现收益」），删除原底部独立累计收益行。③ **实现**（仅前端 2 文件 + 产物）：`pages/Positions.jsx` 卡片渲染将原「大字当前收益+百分比」块与已清仓大字态统一替换为 `.pos-pnl` 两栏结构：左栏 `pos-pnl-label`「当前持仓收益」+ `pos-pnl-val`(sign(p.pnl)) + `pos-pnl-sub`（正常 `涨跌幅%`；已清仓「已清仓 · 无当前持仓」）；右栏「累计收益」+ sign(p.cumPnl) + `pos-pnl-sub`（`cumPnlEdited`→「已手动编辑」，否则已清仓→「全部已实现收益」、正常→「含已实现收益」）；行情不可用（`data_available===false`）卡片仍显示「实时行情数据暂不可用」不改动；底部累计收益行（仅已清仓）删除。`index.css` 新增 `.pos-pnl/.pos-pnl-item/.pos-pnl-label/.pos-pnl-val/.pos-pnl-sub`（两栏 flex、右栏 `border-left` 竖虚线分隔、数值 18px tabular-nums 等宽、`.up/.down` 红绿）。④ **影响面核查**：纯前端展示层改动，**未触及后端/数据库结构/收益计算口径**；卡片展示字段 `holdingPnl/cumPnl` 全部来自既有 `/api/positions`（已在持仓数据导出范围内），无新持久化数据 → **持仓/港股数据导入导出无需变更**。⑤ **验证（构建产物校验）**：esbuild 打包无警告；产物 `assets/index.js` 经 minify 后中文以 `\uXXXX` 转义（如 `\u5F53\u524D\u6301\u4ED3\u6536\u76CA`=当前持仓收益、`\u5DF2\u6E05\u4ED3`=已清仓），逐项比对「当前持仓收益/累计收益/含已实现收益/全部已实现收益/已手动编辑」全部命中；CSS 产物含 `.pos-pnl*` 全部 5 个新类。⑥ **数据真实性**：纯展示改造，金额仍为真实持仓收益计算值（当前持仓收益/累计收益口径未变），无虚拟数据。⑦ **UI 语义**：累计收益小注文案按状态区分（含已实现收益/全部已实现收益/已手动编辑），避免把「累计收益」误读为单纯的浮动盈亏。
- **feat·低（v32 需求3）：行情看板「国内金价信息框」——「盈亏」文字改「持仓盈亏」，其后新增「累计盈亏」「当日盈亏」实时展示，三者正红负绿**：① **需求**：行情看板页面中的国内金价信息框中，盈亏文字改为**持仓盈亏**；改完之后在持仓盈亏的数据后面新增**累计盈亏**和**当日盈亏**数据展示，**实时计算实物黄金的累计盈亏和当日盈亏**；这三种盈亏数值的颜色，正数红色、负数绿色。② **口径（真实数据，与既有模块一致）**：持仓盈亏 = 金价×克数 − 成本（既有 `pnl`）；累计盈亏 = 已实现收益（黄金卖出流水累计）+ 持仓盈亏（与持仓卡片「累计收益」默认口径一致，仅未计入用户手动收益覆盖）；当日盈亏 = 克数 × (实时国内金价 − 沪金AU0 日K前一交易日收盘)，昨收基准与总览「当日收益」卡片的实物黄金口径**完全一致**（复用同源 AU0 日K，绝不用虚拟价）。③ **实现**：后端 `app/main.py`——抽取 `_au0_prev_close(today)` 辅助函数（AU0 日K取前收，末根为今日则退上一根，取不到返回 None），`/api/portfolio/day-pnl` 实物黄金段改用该辅助（消除重复代码）；`/api/gold` 响应新增 `cum_pnl` 与 `day_pnl` 字段（已实现收益只算一次 `compute_gold_realized_pnl` 复用），金价/昨收不可用或未持仓时返回 `null`。前端 `pages/Quotes.jsx` 金价信息框（`.gold-bar`，持有实物黄金时）持仓段文案 `盈亏`→`持仓盈亏`，其后追加 `· 累计盈亏`、`· 当日盈亏`（数值用 `<b className={up/down}>` 着色：正红 `#f85149`/负绿 `#3fb950`，不可用显示「—」；`gold` 状态随既有 `refreshSec` 轮询自动刷新 → 三项实时更新）。④ **影响面核查**：`/api/gold` 仅新增两个只读计算字段（无新持久化数据、无表/SQL 变更），黄金克数/成本/流水仍来自既有存储（已在持仓/港股数据导出范围内）→ **持仓/港股数据导入导出无需变更**；金价条为纯展示 + 只读计算。⑤ **验证（沙箱 8124 + 真实行情）**：无持仓 → `pnl/cum_pnl/day_pnl` 均 `null` ✓；直接持仓 50g@900：金价 969.02 → pnl=969.02×50−900×50=3451.0 ✓、cum=已实现0+3451=3451.0 ✓、day=(969.02−958.38 沪金AU0昨收)×50=532.0 ✓（独立用同一行情源复算逐项一致）；流水场景（BUY 100g@900 + SELL 20g@1000）：已实现=2000、持仓 80g、pnl=5510.4、cum=7510.4=2000+5510.4 ✓、day=840.0 ✓（昨收一致 958.38）；测试流水与持仓已清理、库干净；产物含「持仓盈亏/累计盈亏/当日盈亏/cum_pnl/day_pnl」标记校验通过。⑥ **数据真实性**：三项盈亏全部由真实行情（实时国内金价、沪金AU0 日K）与真实持仓/流水实时计算，缺失时显示「—」/null，绝不填充虚拟值。
- **fix 修复合订（v32 需求3 补丁，2026-09-04 同会话，实测后用户反馈）**：① **累计盈亏与持仓明细实物黄金同源**——用户反馈「累计盈亏显示和持仓盈亏数量一致，并不是取持仓明细中实物黄金的累计盈亏」。根因：`/api/gold` 的 `pnl/cum_pnl` 原按裸口径「市值−成本 / 已实现+持仓盈亏」计算，**未应用 `asset_profit` 表中用户在持仓卡片手动编辑的累计/持仓收益覆盖**（实测用户库存在 GOLD 行：`cum=90128.18, cum_base=6835.18`）→ 持仓卡片累计收益 = 编辑基准+实时持仓收益（动态联动），金价框却显示未编辑口径且与持仓盈亏同值，两处分叉。修复：`gold_holding` 复用 `_gold_position(g=g)`（内部 `_apply_profit_overrides` 应用同一覆盖）取 `holdingPnl/cumPnl` 作为 `pnl/cum_pnl`，`day_pnl` 保持实时口径独立；实测 `/api/gold` 与 `/api/positions` 实物黄金条目完全一致（cum_base 6835.18 + 实时持仓收益 71393.21 → 两者 cumPnl 均 78228.39，`cumPnlEdited=True`）。② **金价条新增交易状态提示**（用户观察到「国内金价和沪金指数数值卡住没变化」）：`/api/gold` 新增 `price_trade_status`（当前状态：交易中（日盘）/小节休市·10:30 恢复/午间休市·13:30 恢复/日盘已收盘·夜盘 21:00 开盘/交易中（夜盘）/周末休市）与 `price_hours_hint`（完整交易时段悬停说明），**跟随当前实际金价源**（`gold_cn_price` 返回新增 `src`：`FUT_AU0` 上期所沪金期货 / `SGE_AU9999` 上金所现货），按北京时间+交易时段表判定（凌晨 00:00-02:30 归前一晚夜盘、周二~周六凌晨为交易夜盘、周末休市、法定节假日以交易所公告为准，hint 明示）；前端 `.gold-bar` 更新时间旁渲染状态小字（`.gold-session`，悬停显示完整时段，css 容错换行）。判定 12 边界场景全过（09:00 交易中/10:20 小节休/11:35 午休/13:31 交易中/15:01 盘后/周五 21:30 夜盘/周六 01:00 仍属周五夜盘/周六白天休市/周日夜休市/周一凌晨休/周二凌晨交易/周一午后交易）。③ **沪金昨收解析修复（涨跌幅恒 0）**：新浪 `nf_AU0`（沪金连续）昨收基准原取 index 5（恒 0）→ 沪金指数卡与金价框（回退沪金连续时）`chg/pct` 恒 0；改用 index 27 昨结算价（实测 971.322 → chg -2.402 / pct -0.25% 正确）。④ **现象说明**：沪金期货午休 11:30-13:30、日盘 15:00-21:00、周末/节假日的价格冻结属交易所作息（新浪返回最近成交价 asof 不变），13:30 开盘/21:00 夜盘自动恢复跳动（实测 13:31 价格 968.92→968.66 恢复、状态转「交易中（日盘）」）；另新浪 `hf_AU9999`（上金所 Au99.99 现货）当前返回空 → 国内金价条恒回退「黄金连续」（期货价，`price_name` 如实显示），与 v23 设计一致。⑤ **影响面**：改动 `app/datasource.py`（nf_ 昨收解析、`gold_cn_price` 增 `src`）、`app/main.py`（`/api/gold` 同源重构 + `_gold_session_status`）、`frontend/src/pages/Quotes.jsx`（状态小字）、`frontend/src/index.css`（`.gold-session`）+ 重建 `frontend_dist`；**无新持久化/无表变更 → 持仓/港股数据导入导出无需变更**（`asset_profit` 覆盖本就在持仓管理既有编辑链路中，且属持仓数据已含于导出）。

- **fix·低（v32 需求4）：K线图默认显示范围按周期对齐「自然日期窗口」（日K近6个月/周K近1年半/月K近5年/年K近10年，所有标的统一）**：① **需求**：指数K线图默认显示范围调整——日K默认近6个月、周K默认近1年半、月K默认近5年、年K默认近10年。② **现状与口径确认（2 项，均已与用户确认）**：现状——K线弹窗为**全局共享组件** `KlineChart.jsx`（指数/个股/基金/港股/黄金均复用），原默认视野 dataZoom 硬编码 `start:40,end:100`（所有周期默认显示**最后 60% 数据**，指数日K拉满约 1000 根≈4年时默认即显示约 2.4 年，偏宽不聚焦近期）。用户确认：a) 适用范围=**所有标的统一**（指数/个股/基金/港股等K线弹窗均按周期套用，不做指数限定）；b) 窗口界定=**按自然日期回推**（从最新一根K线日期向前取：日K 6 个月 / 周K 18 个月 / 月K 60 个月 / 年K 120 个月，精确贴合"近N个月/年"、随时间自动平移），**不用固定K线根数近似**。③ **实现**（纯前端 `KlineChart.jsx` + 产物，后端零改动）：新增 `PERIOD_VIEW_MONTHS={day:6, week:18, month:60, year:120}` 与 `buildDefaultZoom(period, dates)`——以**最后一根K线日期**（正则 `YYYY-MM-DD`）`new Date(y, m-1-months, d)` 向前回推得 cutoff（Date 自动处理月份溢出/跨年），线性查找首个 `date >= cutoff` 的索引作 ECharts dataZoom `startValue`、末根索引作 `endValue`（category 轴按索引精确对齐自然日期）；历史不足窗口（cutoff 早于全部数据）自动 `startValue=0` 显示全部、数据 <2 根或日期格式异常返回 null；分钟K（m1~m60）无"日期窗口"语义返回 null → 回落原 `{type:'inside', start:40, end:100}`（分时视图 dataZoom 不受影响）。`KlineView` 的 dataZoom 改为 `[buildDefaultZoom(period, dates) || 原配置]`。**数据仍按 `PERIOD_CNT` 原档位拉满**（日K 1000/周K 640/月K 500/年K 60），仅默认视野收窄、可拖动滑块回看更早历史。④ **影响面核查**：纯前端默认视野展示调整，**未触及后端接口/表结构/任何持久化**；无新持久化数据 → **持仓/港股数据导入导出无需变更**。⑤ **验证**：a) node 独立运行同算法边界用例——日(6月)/周(18月)/月(60月)/年(120月)窗口定位正确、历史不足（50根全在窗口内）全显、单根与空数组回落 null、m5/minute 回落 null、`2026-05-31` 回推 6 个月溢出正确落至 `2025-12-01`；b) **沙箱 8125 + 真实K线端到端**：上证指数日K 1000 根 → 默认视野 2026-03-04~2026-09-04（128 根=近6个月）；周K 640 根 → 2025-03-07~2026-09-04（78 根=近1年半）；月K 426 根 → 2021-09-30~2026-09-04（61 根=近5年）；**个股**贵州茅台日K 641 根 → 近6个月、**港股**腾讯周K 640 根 → 近18个月（79 根）——所有标的统一生效；esbuild 产物校验 `{startValue:s,endValue:e.length-1}` 与回落 `start:40,end:100` 均在包。⑥ **数据真实性**：默认视野仅是图表缩放展示，K线数据仍为真实行情源、无虚拟数据。

- **fix·中（v32 需求5）：黄金相关指数（纽约金/沪金/伦敦金）分时改用「本地记录的当日实时数值」作数据源（修复 v31 本地跟踪对黄金未生效）**：① **需求**：黄金相关的指数直接获取不到分时数据，应使用**本地维护的实时数据（系统自己记录当天的实时数值）**作为数据源显示分时K线图（此前没有生效）。② **根因（代码勘察确认 3 处）**：a) `_intraday_for` 对 `GOLD` 市场**硬编码拦截**（`mkt in ("FUND","GOLD")` → 直接返回"基金/黄金仅有日级数据，无当日分时"），黄金指数从未进入本地跟踪登记链路；b) 即便放行，`intraday_track.in_trade_window` 只识别 A/HK/US，**GOLD 恒返回 False，后台采样永不执行**；c) 前端 `KlineChart.jsx` `canMinute` 只认 A/HK/BOND → **GOLD 的「分时」tab 为禁用态**。③ **口径（2 项，均按推荐方案实施、可调）**：a) **分时计价=自有真实价**——纽约金/伦敦金直接记录各自新浪美元实时价（hf_GC≈4518、hf_XAU≈4472 美元/盎司，与指数卡片大字同源一致；日K维持沪金 AU0 元/克代理、顶部 proxy 明示不变；沪金 nf_AU0 实时 969 元/克与日K同单位），不用沪金代理避免名义不符与外盘独有行情段缺失；b) **采样窗口=北京自然日连续、价格变动即记**——0-24 点允许采样（沪金夜盘与外盘凌晨段都涵盖），长时间无变动自动休眠不产生平线，0 点重置为新一天（"当天实时数值"字面语义，实现可靠不丢数据；不用品种精确交易时段以免沪金午休/夜盘跨零点归属复杂化）。④ **实现**：a) `app/main.py` `_intraday_for`：FUND 维持拦截（基金仅日净值），**GOLD 放行走本地跟踪专支**（无腾讯/东财源直接跳过；读 `data/intraday_track/GOLD_{code}.json` ≥2 点 → `source=local` 返回；不足 → `ensure_tracked()` 登记 + `_intra_note_unavailable(GOLD)` 专属文案**动态附已记录采样点数**，直观反馈"记录中"）；b) `app/intraday_track.py`：`in_trade_window` 对 GOLD 返回 True（自然日连续窗口）；**平线抑制重构**（原"价格不变 60 分钟后每分钟刷同价点"会令近 24h 黄金在午休/收盘等无行情段刷出大段无效平线）→ 改为**距上次价格变化 <15 分钟（真实交易短暂横盘）继续落同价点保持时间轴连续，≥15 分钟无变化即休眠**；`_prev_close_of` 对 GOLD 新浪不带昨收的品种（nf_AU0 prev=0）用 `sina_futures_kline(AU0/GC0/XAUUSD)` **前一交易日收盘兜底**（实测 AU0 前收 958.38 正确）；c) 前端 `KlineChart.jsx`：`canMinute` 增加 GOLD（**分时 tab 可用**）；GOLD 请求分时无点时**不再自动落回日K**，显示「⏳ 分时数据记录中…」占位（含说明）并**每 12s 轮询 intraday，本地记录满 2 点自动切换显示当日分时曲线**（手动切分时同样生效）；`Quotes.jsx` `IndexSpark` 对 GOLD 卡片空态文案改「分时·记录中（系统每分钟记录当日实时价，满2点显示）」。⑤ **影响面核查**：本地跟踪文件 `data/intraday_track/` 早已被 `.gitignore` 忽略（`data/`），为**行情衍生记录、非持仓/用户数据 → 持仓/港股数据导入导出无需变更**；改动文件 `app/main.py`、`app/intraday_track.py`、`frontend/src/components/KlineChart.jsx`、`frontend/src/pages/Quotes.jsx` + `frontend_dist`，无表结构变更。⑥ **验证（沙箱 8126 + 真实行情，北京时间周五 12:14 盘中）**：GOLD 三标的批量轮询 → 返回 unavailable + 本地记录 note，`tracked.json` 正确登记 3 项 ✓；`sample_once` 手动采样首点全部落盘：hf_GC 4515.47（prev 4539.9 新浪自带）、nf_AU0 968.92（**prev 958.38 成功 AU0 日K兜底**）、hf_XAU 4469.62（prev 4472.99）✓；**后台线程 60s tick 自动积累第 2 点**（12:14→12:15；nf_AU0 同价横盘因 <15min 正确继续落点）✓；`/api/kline/intraday?market=GOLD&code=hf_GC` → `available=true, source=local, times/price 2 点` ✓；回归 A 股上证/茅台=腾讯源 121 点、美股纳指休市=本地跟踪提示，**均不受影响** ✓；测试数据已清理（intraday_track 目录移除，data/ 不入 git）；esbuild 产物含「分时数据记录中/分时·记录中/本地记录」等标记 ✓。⑦ **数据真实性**：分时点全部来自新浪真实报价采样（纽约金/伦敦金美元价、沪金元/克），无外部源时绝不虚构；记录从系统运行（或当日首次访问）起逐分钟积累，**当日此前数据无法回补**（不造假），保持工作台运行越长当日曲线越完整；同价休眠保证曲线只含真实行情段。
### v31（2026-09-03）
- **feat·低（v31 第四需求）：自选基金 / 自选标的模块新增「当日收益总和」显示（异动阈值左侧，正红负绿）**：① **需求**：持仓管理相关页面中「自选基金和标的模块」新增当日收益总和显示，按所有自选中**有持仓**产品的预估日收益计算当日收益总和；显示在异动阈值左侧；正收益红色、负收益绿色。② **定位与澄清（已与用户确认）**：两个模块实际位于**行情看板**页（"自选基金"表格模块与"自选标的"表格模块，标题行右侧各有「异动阈值」下拉）；收益范围经确认=**各模块各算**——自选基金模块=该模块内（自选基金+自动同步的持仓基金）持仓产品的预估日收益合计；自选标的模块=自选标的中持仓产品的合计。③ **实现**（仅前端 `pages/Quotes.jsx`）：新增 `pnlSumOf(rows)`（对模块行集合求 `valOf(row,'pnl')` 非空合计，口径与「预估日收益」列完全一致=日涨跌幅×持仓市值，持仓市值映射 `mvMap` 由持仓管理提供）与 `pnlBadge(label,sum)` 展示块（标签如「基金当日收益 / 标的当日收益」+ `¥+xx.xx`，**正 `up` 红、负 `down` 绿、无持仓收益灰 `¥0.00`**，悬停 title 注明公式）；自选基金模块插入点=标题行 flex spacer 之后、异动阈值之前；自选标的模块把标题行改为 flex 布局（新增 spacer + 收益块 + 异动阈值），数字随行情轮询自动更新。④ **影响面核查**：仅前端 1 文件 + 重新 `vite build`；**未触及后端、数据库结构、持仓/港股数据导入导出**——预估日收益依赖的持仓市值映射本就来自持仓管理（已在数据导出范围），本次仅新增前端汇总展示，**无需**改动数据导入导出功能。⑤ **验证（真实浏览器 + 真实持仓数据）**：构造 招商中证白酒(FUND 161725) 与 招商银行(A股 sh600036) 等真实持仓与自选后——「基金当日收益」= 模块内持仓基金预估日收益之和、「标的当日收益」= 自选标的中持仓产品之和，**与表格各行「预估日收益」逐行累加核对精确一致（误差 0）**；负收益显示绿色（rgb 63,185,80）、正收益（招商银行 +0.59% 持仓）显示**红色（rgb 248,81,73）**；无持仓时 `¥0.00` 灰；控制台 0 错误；测试持仓/自选已清理（库还原为 3 只默认基金、无持仓）。⑥ **数据真实性**：收益全部由真实持仓市值 × 真实行情涨跌幅计算，无虚拟数据。

- **feat·中（v31 第三需求）：指数卡片迷你分时小图 + 点开指数先显示分时 + 无源指数本地文件跟踪（每交易日重置）**：① **需求**：a) 点开指数的 K 线图卡片时先显示当天分时（每个新交易日重置）；b) 卡片外部（当日涨幅文字下方）显示当天分时迷你小图；c) 先检查分时数据是否有获取源，**没有获取源的指数需在本地维护文件记录跟踪当天分时**供 K 线图保留统计数据，该文件每个交易日重置。经与用户确认：小图覆盖**全部指数卡片**（A股/港股/美股/自定义）；本地跟踪**仅当该指数确无真实分时源时启用**；驱动方式=**后端定时轮询**（每分钟 1 次）；采样频率=**每 1 分钟**。② **数据源检查结论（实测）**：A股/港股指数=腾讯 minute/query 当日逐分钟实时 ✓；美股指数=腾讯仅其交易时段实时，**非交易时段（A股白天）东财 `push2delay` 可返回最近完整交易日全天分时（纳指100/标普/道指等 391 点）**，但**纳指综合(.IXIC)、费半(SOX) 等东财无分时**、腾讯休市时也仅 1 点 → 属"无获取源"，落入本地跟踪；新浪报价源对全部指数（含 gb_sox）可用作采样输入。③ **实现**：a) 后端 `app/datasource.py` 新增东财 `eastmoney_trends()`（push2delay 稳定 host）+ `em_secid()` 指数 secid 映射（只收录与腾讯/新浪同标的项，避免标的不一致误导），新浪报价解析补充源侧 `src_date/src_time`（供采样新鲜度判断）；b) **新增 `app/intraday_track.py` 本地跟踪模块**：`data/intraday_track/` 下 `tracked.json`（无源指数名单）+ `{mkt}_{code}.json`（分时记录 `{trade_date, points:[{t,price}], prev_close}`）；后台 daemon 线程每 60s tick：对名单内指数在其**市场交易时段**（A/港股=北京时间窗口+交易日历，美股=美东时区窗口）用新浪报价采样；**每个新交易日重置**（采样点市场日期 != 文件 trade_date → 清空重记）；同分钟覆盖、平线抑制（60 分钟无价格变化即视为非活跃，防休市/节假日记出平线假数据）；c) `app/main.py`：抽 `_intraday_for()` 统一数据链（腾讯当日 → 东财最近交易日 → 本地文件 → 全无则 `available=false`+明确 note 并**登记本地跟踪**；腾讯/东财有源即自动移出跟踪名单），响应新增 `source`/`trade_date` 字段；`/api/kline/intraday` 改走统一链；**新增 `/api/kline/intraday/batch`**（指数卡片小图轻量批量接口，30s 内存缓存降探测开销）；startup 启动跟踪线程；d) 前端：`Quotes.jsx` 指数卡片 `.ic-pct`（当日涨幅）下方新增 **`IndexSpark` SVG 迷你分时小图**（涨红跌绿按最新价 vs 昨收；无源显示"分时·暂无（每交易日自动记录）"占位；title 标注数据日期与来源），每 60s 轮询 batch；`KlineChart.jsx`/`kline.jsx` 支持 `preferMinute`（指数卡片点开无条件先请求分时，无当日源自动落回日K并提示，提示含休市原因与本地跟踪说明）；分时视图顶部提示行动态标注"数据日期（最近交易日）"或"当日实时"，明确展示口径。④ **影响面核查**：改动 `app/datasource.py`、`app/main.py`、`app/intraday_track.py`（新）、`frontend/src/api.js`、`components/KlineChart.jsx`、`kline.jsx`、`pages/Quotes.jsx` + `frontend_dist`；**未触及数据库结构；本地分时跟踪文件属于行情衍生记录（同行情数据性质），不纳入"持仓/港股数据导入导出"**——本次**无需**改动数据导入导出功能（文档注明口径）。⑤ **验证**：a) 统一链冒烟：A股上证=腾讯源 112 点当日实时 ✓；纳指 ixic=全无 → `available=false` + "美股休市（美东时间）…已启用本地跟踪" + tracked.json 登记 ✓；batch 返回 5 A股指数腾讯源 + 纳指无源 ✓；b) 采样链路实测（A股盘中）：`sample_once` 写入 `{trade_date:今日, prev_close:3941.3862, points:[真实报价]}` ✓；c) **单测**：同分钟采样覆盖不追加 ✓、**交易日切换 → 旧点清空重记** ✓、报价新鲜度/平线抑制守卫 ✓；手工构造 SOX 本地文件 → `_intraday_for` 返回 `source=local` + 数据日期标注 ✓；d) **真实浏览器**：10 张指数卡片 A股全有红色/绿色 mini polyline（title=数据日期+腾讯实时），纳指卡片显示"分时·暂无（每交易日自动记录）"占位；点开上证 → 默认高亮"分时"；点开纳指 → 自动落回"日K" + 提示休市与本地跟踪；控制台 0 错误。⑥ **数据真实性**：所有分时点来自腾讯/东财真实接口或新浪真实报价采样；无源指数的本地记录完全由真实交易时段报价积累，不虚构；源全无时明确提示并登记跟踪，绝不造假。⑦ **说明**：美股指数（如纳指综合）的当日分时仅在美股交易时段（北京时间约 21:30-次日 04:00）由腾讯实时源提供或由本地跟踪采样积累——若用户仅白天使用工作台，纳指等卡片白天显示"暂无（每交易日自动记录）"，本地文件在美股开盘期间保持运行时会自动积累，收盘后/次日白天即可看到该交易日的完整曲线；**保持工作台（后端）在交易时段运行是本地跟踪生效的前提**。

- **feat·高（v31 第二需求）：K线历史深度扩展 + 分时/分钟K/日K/周K/月K/年K 周期切换，默认显示当日分时**：① **需求**：K线图数据此前只有约半年（120根）；要求①能看更长的历史记录；②可切换 日K/周K/月K/年K；③默认显示分时K线图。经与用户确认：分时指「当日分时走势 + 分钟K线都要」（tab：分时|1分|5分|15分|30分|60分|日K|周K|月K|年K，默认落在分时走势）；复权口径选「前复权为主」；无分时数据源的市场（美股/基金/黄金等）打开图表时**自动落回日K并明确提示**。② **数据源实测结论（全部真实能力，非猜测）**：a) 腾讯 K 线接口 `web.ifzq.gtimg.cn` 会被 WAF 拦成 501，同源**代理域名 `proxy.finance.qq.com` 稳定可用**且原生支持 day/week/month/year 全周期（year 仅返回当年 1 根，无历史年度序列）；b) 真实深度：A股个股前复权日K上限约 641 根（2.6年），不复权 1001 根（4年）；指数/港股日K 1000+ 根（4年）；周K 640-1000 根（12-19年）；月K 全量（上证 1990-12 基点起 426 根/茅台 2001 上市起 301 根/港股腾讯 2004 起）；美股个股与指数月K自 2007 起（腾讯美股源仅保留 2007+，为源侧事实）；c) 分钟K（m1/m5/m15/m30/m60）**仅 A股（含指数）有真实数据**，港股/美股/转债返回空；d) 当日分时（腾讯 minute/query，实测为逐分钟粒度）：A股个股/指数、港股有数据，**volume/amount 为当日累计值**（A股 volume 单位为手、港股为股、amount 元），美股非交易时段为空；e) 北交所走新浪回退（bj430047 自 2021-11 上市起有 周/月K；北证50 自 2022-11 起）；基金仅有日净值、黄金仅有新浪期货日K（周/月/年由日数据聚合）。③ **修复/实现**（后端 `app/datasource.py` + `app/main.py`，前端 `api.js` + `components/KlineChart.jsx` + `kline.jsx`）：a) 腾讯域名切至 `proxy.finance.qq.com`（提为常量 `TENCENT_IFZQ`）；b) **修正腾讯K线列序**：实测腾讯行列序为 `[日期,开,收,高,低,量]`，原解析按 `[开,收,低,高]` 存（高低错位风险），已统一为 `[开,收,低,高]`（与新浪一致、ECharts candlestick 所需），经与新浪逐日比对完全一致；c) `tencent_kline`/`tencent_us_index_kline` 支持解析 `qfq{period}`/`{period}` 键（day/week/month/year）；d) 新增 `tencent_mkline`（分钟K）、`tencent_minute_today`（当日分时）、`aggregate_kline`（周/月/年聚合，真实加工）；e) `/api/kline` 扩展 `period=day|week|month|year|m1|m5|m15|m30|m60`，各市场能力矩阵：A股/港股/美股/指数走腾讯原生（不足回退新浪/聚合兜底，北交所新浪周月K），**年K统一由真实月K聚合**（上证可到 1990 年）；基金净值日序列与黄金期货日K在 period≠day 时按日聚合（周=ISO周/月/年）；`count` 默认档位：日K 1000、周K 640、月K 500、年K 60、分钟 240；f) 新增 `/api/kline/intraday`（当日分时）：A股个股/指数、港股返回 `{times,price,avg_price,volume,amount,prev_close}`，**均价线=累计额/累计量**（个股/港股真实计算；A股指数无均价概念置 null），量柱返回当分钟量（累计差分），无法取到真实数据的市场（美股非交易时段/基金/黄金/数据为空）返回 `available=false` + 明确 note，绝不造假；g) 前端 `KlineChart.jsx` 重构：顶部周期 tab 栏（分时|1分|5分|15分|30分|60分|日K|周K|月K|年K），默认分时（仅 A/HK/BOND 市场，其余市场直接日K），分时不可用时**自动落回日K并提示原因**；新增分时走势视图（价格线红涨绿跌按最新价vs昨收、均价黄线、昨收灰虚线、底部当分钟量柱、tooltip 含涨跌幅/均价/量）；K 线视图沿用 v31 图例颜色规范（KL_COLORS），日K及以上周期保留买卖点标注、分时/分钟K 不标注（买卖点按交易日对齐，分钟K无法对齐）；图名与 x 轴标签随周期动态（分钟K显示 HH:MM）；h) `kline.jsx` 弹窗标题去掉固定「日K」字样。④ **影响面核查**：改动 `app/datasource.py`、`app/main.py`、`frontend/src/api.js`、`components/KlineChart.jsx`、`kline.jsx` + 重新 `vite build`；**未触及数据库结构；K线/分时数据是行情类数据而非用户数据，不涉及持仓/港股数据导入导出**——本次**无需**改动数据导入导出功能。⑤ **验证**：a) 后端矩阵冒烟全绿：茅台日K 641根/周K 640根(2014起)/月K 301根(2001起)/年K 26根；上证指数 日K 1000根/月K 426根(1990-12起)/年K 37根；港股 日K 1000/月K 268(2004起)/年K 23；美股 AAPL 与纳指 月K 235(2007起)/年K 20；基金周K 577(2015起)/年K 12；实物黄金 周K 640/年K 19(2008起)；北交所 bj430047 周K 199 月K 47（上市起）；分钟K m1/m5/m30/m60 均返回（A股）；b) **聚合正确性**：茅台 qfqday 聚合周K 与腾讯原生周K 逐项比对 130 个共同周 **0 差异**；c) **OHLC 列序**：修正后全部周期 `low<=high` 成立，且与新浪逐日一致；d) **真实浏览器（Playwright+Chromium）像素级**：指数分时价格线（红）与昨收虚线渲染、茅台个股分时**均价黄线**（#f2c230）出现、5分K/日K/年K 图例 8 项颜色一致（v31 图例需求无回归）、年K下取消 MA5 置灰仍生效（灰色像素检出）、持仓买卖点标注在日K正常（橙色/蓝色虚线检出，抗锯齿色 #d79024/#5099ec）、基金净值K线默认日K正常渲染，全程控制台 0 错误；e) SSR 单测（含均价线/指数无均价/年K 37根/分钟K 482根）全部通过。⑥ **数据真实性**：全部数据来自腾讯/新浪真实接口；周/月/年K优先腾讯原生，缺失市场用真实细粒度数据聚合（纯加工无虚构）；分时/分钟K无真实数据源的市场（美股分钟K、基金/黄金分时等）返回明确「暂不可用」提示，绝不造假。⑦ **注意事项（环境）**：腾讯接口域名变更仅影响代码常量；`requirements.txt` 仍建议补锁 `anyio==3.7.1`（见前条提醒）。

- **fix·中（v31 第一需求）：所有 K 线图界面，统一筛选按钮（图例）与实际划线/柱体的颜色（MA5/MA10/MA20/DIF/DEA/成交量/MACD/K线）**：① **现象**：K 线弹窗顶部的图例色块颜色与实际画出来的线/柱颜色**完全对不上**——MA5 图例显示蓝色而线是黄色、MA10 图例绿色而线是蓝色、MA20 图例黄色而线是紫色、DIF 图例浅蓝而线是蓝色、DEA 图例绿色而线是黄色；成交量与 MACD 的图例显示橙/红色，而实际柱体是「红涨绿跌」双色，根本无法用单一色块表达。② **根因**：ECharts 的 legend 图标颜色默认取自 **series 分配的调色板色（palette）**，与 `lineStyle.color` / `itemStyle.color` **毫无关系**；本项目在 series 上显式写了自定义色，导致「图例色=调色板色」与「线色=自定义色」两套颜色各走各的。对单色系列（MA/DIF/DEA），需在 `legend.data[].itemStyle.color` 上**再写一遍同一颜色**才能对齐；对成交量/MACD 这类 `itemStyle.color` 是**函数**（红涨绿跌回调）的系列，legend 无法取到函数返回值，只能回退调色板色——这是"双色系列不可能用单色图例表达正确"的结构性原因。③ **修复**（4 个前端文件）：a) `frontend/src/components/KlineChart.jsx` **新增并导出配色常量 `KL_COLORS`**（`up/down/volumeUp/volumeDown/ma5/ma10/ma20/dif/dea/buy/sell/inactive`），作为全 K 线的**唯一颜色源**，legend 与所有 series 一律引用它；b) 单色系列在 `legend.data[]` 上补 `itemStyle.color`，落入 `buildLegendData()` 统一构造；c) K线 / 成交量 / MACD 三个双色系列改用 `dualColorIcon()` 生成的**内嵌 SVG 双色图例图标**（`icon: 'image://data:image/svg+xml;charset=utf-8,' + encodeURIComponent(svg)`，左半红右半绿，K线为双蜡烛形态、成交量/MACD 为双柱形态）；d) **image 图标 ECharts 不会随取消勾选自动置灰**，故监听 `legendselectchanged`，按当前 `selected` 反算出未选中集合，以 **merge 模式**（`setOption(..., false)`，notMerge 会重置选中态）重建 `legend.data` 把对应图标换成灰版 `#ccc`；e) 买卖点 markLine 颜色改用 `KL_COLORS.buy/sell`，删除原局部变量 `BUY_COLOR`/`SELL_COLOR`；f) `frontend/src/kline.jsx` 弹窗头部「买入橙/卖出蓝」图例文字色改用同一 `KL_COLORS`，消除与图内标注的两处硬编码漂移；g) `frontend/src/components/EChart.jsx` 新增 `onChartReady` prop，在 `init`/`dispose` 时回传 ECharts 实例（用 `readyRef` 持有最新回调，避免闭包旧值），使 K 线能拿到实例做图例更新；h) **顺带修复** `frontend/src/pages/Analysis.jsx` 净值曲线图同类问题——「总市值/总成本」图例取调色板色与实际折线色（`#58a6ff`/`#8b949e`）不符，已显式指定 `legend.data[].itemStyle.color`。④ **影响面核查**：仅前端 4 文件 + 重新 `vite build` 的 `frontend_dist`；**未触及后端、数据库结构、持仓/港股数据导入导出**——本次为纯渲染层修复，故**无需**改动数据导入导出功能。⑤ **验证**：a) ECharts Node SSR（`init(null, null, {renderer:'svg', ssr:true})`）实测确认修复前 7 个图例色全部与实际色不符、补 `itemStyle.color` 后精确对齐；b) **真实浏览器（Playwright + Chromium）像素级校验**：弹窗图例从左到右 8 项色值逐一命中——x≈96-107 K线（红+绿双色）、146-158 MA5 `#d29922`、200-212 MA10 `#58a6ff`、262-274 MA20 `#a371f7`、323-333 成交量（红+绿）、388-400 DIF `#58a6ff`、437-449 DEA `#d29922`、492-502 MACD（红+绿）；c) **取消勾选置灰实测**：点 MA5 → 该处黄色块消失、图标与文字转为 `#ccc`（x=146..187）；再点 K线 → 灰色区扩展到 x=96..187，且红色像素区间由 x=96..494 收缩为 323..494（K线双色图标已灰，仅剩成交量/MACD 的红），证明 `legendselectchanged` 生效且**不影响其它图例的选中态**；d) 控制台 0 错误。⑥ **数据真实性**：本次仅改渲染配色，不新增/不改动任何数据源，全部 K 线数据仍为腾讯/新浪真实行情。

### v30（2026-08-29）
- **fix·高：Windows 一键脚本 `operation.bat` 改为与 Linux `operation.sh` 一致的命令行后台控制，解决双击启动后「按任意键不退出、关窗口即停服」**：① **现象**：双击 `operation.bat` 启动后卡在「按任意键继续」（无法退出），且关闭该窗口后端进程随之被杀，无法在窗口关闭后于 Windows 后台持续运行。② **根因**：原脚本用 `start /B` 把后端作为**当前 cmd 窗口的子进程**拉起，并末尾加 `pause` 阻塞；`start /B` 的子进程隶属于启动它的控制台窗口会话，窗口关闭即被终止，达不到真正后台常驻。③ **方案**：.backend 改用 PowerShell `Start-Process -WindowStyle Hidden` 以**完全独立的隐藏进程**启动（与 Linux `nohup ... &` 等价），PID 经 `-PassThru` 直接写入 `.server.pid`，uvicorn 日志由 `--log-file` 写入 `.server.log`；脚本启动后立即退出、不再阻塞。控制方式对齐 Linux：`operation.bat [start [port]]` / `stop` / `restart [port]` / `status`，无参数默认 `start`。④ **影响面核查**：仅部署脚本层改动（`operation.bat`），未触及 `app/main.py`、持仓导入导出、港股持仓备份恢复逻辑、数据库结构、`frontend_dist`——故本次**无需**改动数据导入导出功能。⑤ **验证（待用户本地实测）**：`operation.bat start` 后窗口立即退出、浏览器访问 `http://localhost:8000` 正常、关闭原窗口后端仍在线；`operation.bat status` 显示 PID、`stop` 可正常终止。⑥ **数据真实性**：不涉及数据源。
- **新增·中：持仓产品 K 线图标注用户录入的买入/卖出时间点**：① **需求**：在持仓管理点开产品 K 线图时，标出用户录入的买卖交易日期，并用竖直虚线在 K 线面板顶部以文字标注「买入/卖出」（买入橙色 `#ff8c00`、卖出蓝色 `#1e90ff`）。② **实现**：`frontend/src/kline.jsx` 的 `openKline(market, code, name, markers)` 新增可选 `markers` 参数（`[{date:'YYYY-MM-DD', side:'BUY'|'SELL'}]`，透传给 `KlineChart`）；持仓页 `Positions.jsx` 点开 K 线时按 `market+code` 过滤 `transactions`，将每条 `trans_date/side` 作为 marker 传入；`KlineChart.jsx` 将 marker 日期对齐到 K 线最近交易日（ISO 日期字典序比较，取 ≤ 该日的最近交易日，早于全部则取首根），通过 `candlestick` 系列的 `markLine` 按 `side` 着色，并去重 `(date,side)` 避免重叠。**标注形态（v30 修订）**：竖直虚线改为从面板顶部画至「与 K 线交叉处」（以该交易日收盘价 `#[1]` 为锚点），交叉处用对应颜色的**实心圆点**（`symbol:['none','circle']`，`symbolSize:8`）标注，**圆点上方保留虚线、圆点下方去掉虚线**；顶部起点（`position:'start'`）处用「买入/卖出」文字标注。弹窗头部新增橙/蓝图例提示。③ **影响面核查**：仅前端 `kline.jsx`/`components/KlineChart.jsx`/`pages/Positions.jsx` 三文件 + 重新 `vite build` 的 `frontend_dist`；买卖点由 `transactions` 派生，而交易记录本就在「数据导入导出」范围内，故**无需**改动导入导出功能。④ **数据真实性**：标注完全基于用户真实录入的交易日期，无任何虚拟数据。⑤ **验证（待用户本地实测）**：持仓管理点开存在买卖记录的产品 K 线，应见顶部橙/蓝虚线及「买入/卖出」文字，且虚线止于 K 线交叉处的同色圆点、圆点下方无延伸；同一日多次同方向交易仅一条线。
- **fix·高（v30 修订，同会话）：持仓有交易记录后点开 K 线图整页黑屏（markLine 配置错误致 `setOption` 抛错）**：① **现象**：新增交易记录后，从持仓点开该产品 K 线图，整个页面黑屏（无渲染）。② **根因**：`KlineChart.jsx` 的 `buildMarkLine` 用 `markLine.data` 的 `coords: [[x1,y1],[x2,y2]]` 字段描述两点线段；但 ECharts markLine 的**线段（两点之间）数据项必须是「两个端点对象组成的数组」`[[{coord:[x1,y1]}, {coord:[x2,y2]}]]`**，`coords` 不是 markLine 的合法字段。ECharts `MarkLineView.markLineFilter` 把每个数据项当 `[起点, 终点]` 解构、对端点读 `.coord`，取到 `undefined.coord` → 抛 `Cannot read properties of undefined (reading 'coord')`。由于 `EChart.jsx` 的 `setOption` 无 try/catch，该异常冒泡到 React 渲染阶段使整棵组件树卸载 → 整页黑屏。③ **修复**：`buildMarkLine` 改为返回 `[[{coord:[日期, topY], lineStyle, itemStyle, label}, {coord:[日期, 收盘价]}], ...]`（`topY` = 全部最高价×1.03 作顶部起点，`收盘价` 为交叉锚点）；series 的 `markLine` 用 `symbol:['none','circle']` + `symbolSize:8`（终点画同色圆点）。经 `echarts` Node SSR（`init(null,…,{ssr:true})` + `setOption`）实测：旧 `coords` 写法稳定抛上述错误、新写法 `SSR OK 无抛错` 且能渲染「买入」文字与线段。④ **影响面**：仅 `frontend/src/components/KlineChart.jsx` 单文件 + 重新构建的 `frontend_dist`，未触及导入导出逻辑、后端。

- **fix·中：交易记录表格增加「名称」列**：① **需求**：交易记录（持仓明细页「交易记录」、报表页「交易流水」）除显示产品代码外，需同时显示产品名称。② **现状**：`transactions` 表无 `name` 列（`database.py` 建表仅含 market/code/side/quantity/price/fee/trans_date/note），交易记录本身不含名称。③ **方案**：两页面均已加载 `positions`（含 `name`），前端以 `market|code` 为键构建名称映射，在交易表格「代码」列后新增「名称」列；删除持仓会连带删除其全部交易记录，故映射可靠，未匹配时回退为代码。④ **影响面核查**：仅前端 `pages/Positions.jsx`、`pages/Reports.jsx` 两文件 + 重新 `vite build` 的 `frontend_dist`；未改动 `transactions` 表结构与导入导出逻辑（交易记录导入导出仅含 market/code 等）→ **无需同步**导入导出功能。⑤ **验证（待用户本地实测）**：持仓明细页与报表页交易表格均显示产品名称，与持仓页名称一致。

- **fix·高（v30 修订，同会话）：总览「收益」每次刷新跳动、且近一个月收益折线图不基于当前持仓**：① **现象**：总览页点「⟳ 刷新」后，「当前持仓收益 / 累计收益」数字每次都会变化；点开卡片弹出的「近一个月收益折线图」里，曲线数值与实际当前持仓对不上。② **根因**：a) 顶部卡片原由 `portfolio_summary` 每次用实时行情重算，随盘中报价跳动（非稳定口径）；b) `pnl_series`（近一个月收益）的「累计收益」曲线原按 `Σ(当日市值−前日市值)`（即「市值差额累计」）计算，并非真实累计盈亏（含已实现）；且当日点取自每天首次算出的冻结快照，与当前持仓脱节——确属「不是用当前持仓计算的」。③ **修复**：`snapshots` 表新增 `total_cum_pnl` 列（`init_db` 内幂等 ALTER，导入导出 SQL 同步）；`ensure_snapshot_today` 增加 `cum_pnl` 参数，并对「旧版缺列快照」按当前持仓重算覆盖一次、之后冻结（锚定当日快照，刷新稳定）；`portfolio_summary` 改从当日快照读回收益数字（总资产/总成本/持仓收益/累计收益一致冻结）；`pnl_series` 的 cum 改用快照 `total_cum_pnl`（真实累计收益含已实现），holding 仍为市值−成本，每日收益由相邻快照差分得到。④ **影响面核查**：仅后端 `app/database.py`、`app/main.py` 两文件；前端数据结构不变（无需重建 `frontend_dist`）；`snapshots` 仅增一列且导入导出已同步 → 不影响持仓/港股数据备份恢复。经固定价格确定性测试：summary 三次调用完全一致、pnl_series 的 cum 末点 = 真实累计收益、与卡片口径一致。⑤ **验证（待用户本地实测）**：重启后端后，总览顶部「当前持仓收益 / 累计收益」多次刷新数值稳定不再跳动；点开卡片弹出的近一个月折线图，末点数值与顶部卡片一致（当前持仓收益=市值−成本、累计收益=含已实现），且「累计收益」曲线为真实累计盈亏而非市值差额累计。⑥ **数据真实性**：收益完全由真实持仓 + 真实行情（或冻结快照）计算，无虚拟数据。

### v29.2（2026-08-28）
- **修复·高：Windows 一键脚本括号/引号解析错误 + pip 以 GBK 解码 requirements.txt 失败**：① **现象**：修复 v29.1 后重跑 `setup_env.bat` 依次报 `skip was unexpected` / `13 was unexpected`；进一步在 pip 安装阶段报 `UnicodeDecodeError: 'gbk' codec can't decode byte 0xb0`。② **根因**：`setup_env.bat` 第 4 段 venv 版本判断中，块内 `echo` 含未转义 `)` 使 `if` 块解析失败，块内用 `%VMIN%`（解析期为空的普通变量）与 `13` 比较造成语法错误，导致 venv 重建分支不执行、持续复用旧的 3.13 环境，pydantic v1 必然崩溃；`requirements.txt` 含中文注释（UTF-8 多字节），Windows 下 pip 以 GBK(cp936) 读取，`0xb0` 字节非法。③ **修复**：第 4 段版本判定改用 `for` 元变量 `%%b` 比较次版本号（解析期即存在、无空值问题），消除解析期空变量比较；两个 `.bat` 顶部新增 `set PYTHONUTF8=1`，强制 pip 与 venv 内 Python 以 UTF-8 处理文本/中文数据（对港股持仓导入导出的中文名尤为关键）；`requirements.txt` 中文注释转英文 ASCII，保留全部 3.7 兼容性说明。④ **数据/逻辑影响**：仅部署脚本层改动，未触及业务代码、持仓导入导出、港股持仓备份恢复逻辑。⑤ **验证**：仓库与 `E:\Programs\finance-dashboard` 副本的 `setup_env.bat`/`operation.bat`/`requirements.txt` 经字节检测均为 0 非 ASCII 字节且内容一致；部署机以 Python 3.7.7 重跑 `setup_env.bat` 后依赖安装全绿。⑥ **部署指引**：在 `E:\Programs\finance-dashboard` 直接重跑 `setup_env.bat` → `operation.bat` 即可；建议优先使用本机已有的 Python 3.12（pydantic 1.10.14 在 3.12 正常，仅 3.13 报 recursive_guard 错），3.7 已 EOL 且部分依赖可能缺对应 wheel。

### v29（2026-08-28）

- **修复·高：Windows 一键脚本（`.bat`）中文乱码导致 `'口'`/`'n.bat'` 命令错误 + 部署机 Python 3.13 与锁定依赖不兼容**：① **现象**：双击 `operation.bat` 报 `'口' is not recognized` 与 `'n.bat' is not recognized`；另，`setup_env.bat` 在 Python 3.13 上建出的 `.venv` 因 `pydantic v1` 在 3.13 的 `ForwardRef._evaluate()` 缺 `recursive_guard` 关键字参数而导入即崩溃（`TypeError: ForwardRef._evaluate() missing 1 required keyword-only argument: 'recursive_guard'`）。② **根因**：`.bat` 脚本含中文（`echo` 提示、`start` 窗口标题「金融工作台后端」、全角符号），`cmd` 以系统 OEM 代码页(GBK) 解析 UTF-8 无 BOM 脚本，中文多字节被误拆成乱码命令；`chcp 65001` 仅改变输出编码、不改变脚本解析编码。`setup_env.bat` 旧版仅校验 `v[1]>=7`，对 3.13 放行，而本项目锁定 `fastapi 0.99.1 + pydantic 1.10.14`（v1），pydantic v1 从未支持 Python 3.13。③ **修复**：将两个 `.bat`（`operation.bat`、`setup_env.bat`）改为**纯 ASCII**（去全部中文，提示/标题/注释改用英文），与代码页无关；`setup_env.bat` 版本校验由「≥3.7」收为「**3.7–3.12**」（拦截 3.13+），并新增**优先探测 `py -3.12/3.11/.../3.7` 启动器**，本机若装有 3.12 则自动绕过 3.13 建 venv。④ **数据/逻辑影响**：仅脚本层改动，未触及后端代码、持仓导入导出、港股持仓备份恢复逻辑；`app/main.py`、`frontend_dist`、数据库结构均不变。⑤ **验证**：四个 `.bat` 副本（仓库 + `E:\Programs\finance-dashboard` 部署副本）经字节检测均为 0 非 ASCII 字节；`setup_env.bat` 版本拦截对 3.13 给出明确提示并要求安装 3.12。⑥ **部署指引（E: 环境）**：删除旧的 `.venv`（3.13 建的，已不可用）→ 安装 Python 3.12（勾选 Add to PATH，注册 `py -3.12`）→ 双击 `setup_env.bat` 重建 venv 并装依赖 → 双击 `operation.bat` 启动。⑦ **追加修复（同会话 v29.1）**：实践发现第 4 步原逻辑「`.venv` 已存在即跳过创建」会**复用旧版本 Python 建的残留 venv**（如机器上先装 3.13 建过 venv、后装 3.12，脚本虽检测到 3.12 却仍复用 3.13 的 venv），导致依赖装进 3.13 环境、验证仍报 `recursive_guard` 错。修复：复用前先取 venv 自身 Python 版本（`.venv\Scripts\python.exe --version`）与目标 `PY_VERSION` 比对，**不一致则 `rmdir /s /q` 删除重建**，确保 venv 的 Python 与本次检测到的目标严格一致。

### v28（2026-08-28）

- **fix·中 + 新增·中：纽约金/伦敦金 K 线改用沪金 AU0 真实K线替代并诚实标注**：① **背景**：v25 起纽约金（`hf_GC`）/ 伦敦金（`hf_XAU`）K 线因真实历史数据源不可达，仅返回「该黄金品种K线数据源暂不可用」提示，功能不可用。② **数据源实测（遵守"无法获取真实数据须提醒"）**：穷尽测试——新浪期货日K（`InnerFuturesNewService`）对纽约金 `GC0` / 伦敦金 `XAUUSD` 均返回空数组；腾讯 `fqkline`/`usfqkline` 对 `usGC`/`hf_GC` 的 `day` 为空（实时 `qt` 有值但无历史序列）；东方财富 `push2his` 连 A 股茅台都返回空（整体不可达）。**唯一可用的黄金真实历史K线为沪金 `AU0`（新浪期货，2008 年至今 4540+ 条）**。③ **方案**：纽约金/伦敦金 K 线回退到沪金 `AU0` 真实K线（同为黄金、走势强相关）；后端 `get_kline`（`app/main.py`）对 `hf_GC`/`hf_XAU` 先试真实源、失败则取沪金 `AU0`，并在 `/api/kline` 返回新增 `proxied=True` + `proxy_note`（说明真实源不可达、单位为元/克、与纽约金/伦敦金美元/盎司计价不同、仅供参考）；前端 `KlineChart.jsx` 在 K 线弹窗顶部新增提示条展示该标注（`index.css` 新增 `.kline-proxy-note`）。沪金（`nf_AU0`/`AU0`）、实物黄金保持原真实K线逻辑不变。④ **数据真实性**：代理数据为沪金 `AU0` 真实历史K线（非虚拟），仅来源标注诚实；明确告知用户纽约金/伦敦金本身历史源不可达、当前展示的是同属黄金的沪金真实K线，符合"不允许虚拟数据"约束。⑤ **验证**：沪金 `AU0` 真实K线可取（2026-08-28 收盘 999.28 元/克）；`hf_GC`/`hf_XAU` 触发回退 `proxied=True`；`AU0`/`nf_AU0` 不触发。构建产物 `frontend_dist` 已重新 `vite build`。

### v27（2026-08-28）

- **修复·高：收益分析编辑日收益后，月/年/累计与日历图未联动重算**：① **现象**：在「日收益」视图手动编辑某持仓（或组合）的日收益后，切换到月/年/累计视图，对应持仓与组合收益仍显示旧的原始计算值，未基于修改后的日收益重新累加；日历热力图对应日期的数值也未同步。② **根因**：`app/main.py` 的 `pnl_analysis` 在聚合 month/year/cum 时直接对相邻交易日对的快照市值差值求和（combo_pnl / pos_pnl），拼接日历图 `calendar` 时也只用原始日收益，两处都未读取 `pnl_override` 表中 `pnl_type='day'` 的覆盖值（该覆盖仅在 day 视图自身的 `details`/`combo` 套用）。③ **修复**：聚合循环改为——每天每个持仓优先采用 `pnl_override(pnl_type='day', range_val=当天)` 的 `detail_pnl` 作为当日收益单元；组合当日收益先由各持仓修正值求和、再优先采用组合级 `day` 覆盖；该修正值向上累加进 month/year/cum，并写入 `calendar`。即「日收益」编辑被视为该日收益单元的最终值，自动向上传播到所有聚合层级与日历图。④ **验证（注入 3 天快照 + 持仓 + 编辑 2026-08-27 日收益=9999）**：日视图该持仓 `pnl=9999, edited=true` ✅；月视图该持仓 `pnl=14999`（9999 已传播累加）✅；累计视图 `pnl=14999`（含传播）✅；日历图 `2026-08-27` 当天 `pnl=9999`（同步）✅；测试数据已清理，库干净。

### v26（2026-08-28）

- **新增·中：总览卡片点击查看近一个月收益走势折线图**：① 总览（Dashboard）「当前持仓收益」「累计收益」两张 stat 卡片**可点击**，点击弹出近一个月收益走势弹窗（v26）。② 后端新增接口 `GET /api/portfolio/pnl-series?days=30&kind=holding|cum`：取最近 N 天 `snapshots` 快照，相邻交易日对累加得到每日收益 `daily_pnl`（= 当日总市值 − 前一交易日总市值），`cumulative_pnl` 在 `holding` 模式为「每日总市值 − 总成本」、`cum` 模式为「累计日收益累加（含已实现）」，并回传 `latest_date`（最右当前日期）。③ 前端 `Dashboard.jsx` 新增 `pnlDetail` 状态 + `openPnlDetail(kind)` + `buildPnlSeriesOption(dates, daily, cumulative)`（ECharts line + `dataZoom` 滑块/滚轮，可向左滑动至有记录的第一天）、`api.js` 新增 `pnlSeries`、`index.css` 新增 `.alloc-modal-mask`/`.modal-card`/`.chart-lg`/`.pnl-series-foot` 弹窗样式。④ **数据前提**：需组合分析页积累 ≥2 个交易日快照才有折线数据；天数不足时弹窗诚实提示「暂无收益记录（组合分析页需积累至少 2 天快照）」，绝不造假。
- **新增·中 + 修复：行情看板「预估日收益」列 + 涨跌幅异动跟踪**：① **预估日收益列**：「自选基金」「自选标的」两表新增「预估日收益」列（v26）= 当日涨跌幅(%) × 当前持仓市值（持仓市值来自「持仓管理」聚合，基金按 NAV、与组合汇总同源；**无持仓则显示「—」**）。`fetchWatchlist` 调用 `api.positions()` 构建 `(market:code) → 持仓市值` 映射（基金 code 归一化去 `fu_` 前缀以匹配自选列表），渲染时按该列排序（与「日涨跌幅」排序相互独立，默认降序、缺失值沉底）。② **异动跟踪**：两模块均提供**异动阈值下拉（1%~10%，默认 5%，localStorage `fw_anomaly_pct` 持久化）**；当某产品当日**涨幅 > 阈值 或 跌幅 < −阈值**时，该行加 `.anomaly` 高亮标红 + 「异动」徽标，并在看板顶部弹出**异动提醒横幅**列出所有触发产品（名称 + 涨跌幅），回落阈值内自动取消。③ **排序重构**：原 `fundDir/watchDir`（仅按涨跌幅）升级为 `fundSort/watchSort`（含 `key=pct|pnl` + `dir`），`toggleSort` 同列翻转方向、异列默认降序；新增 `sortRows`/`valOf` 统一取值（pct=日涨跌幅、pnl=预估日收益）。④ 构建产物 `frontend_dist/assets/index-DFTyAmgQ.js` + `index-KDLOuEx6.css`（旧 `index-*.js` 已自动清理）。⑤ **验证（Playwright + 系统 chromium，真实浏览器）**：贵州茅台（sh600519）行「预估日收益」显示 `¥+90.52`（pct +0.07% × 市值 129318 ≈ 90.52，计算正确）✅；点击「预估日收益」表头后首行由「平安银行」变为「贵州茅台」（按估值降序）✅；阈值设 1% 后「招商国证生物医药」(−1.80%) 被标亮并出现在异动横幅 ✅；总览卡片点击弹窗渲染折线图 canvas、含「最右为当前日期（2026-08-28）」提示 ✅；控制台 0 错误 ✅；测试持仓与快照已清理，库干净。

### v25（2026-08-28）
- **fix·高：纽约金/沪金/伦敦金 K 线「数据暂不可用」修复**：① **根因**：`/api/kline` 的 GOLD 分支对期货代码（纽约金 `hf_GC` / 沪金 `nf_AU0` / 伦敦金 `hf_XAU`）走腾讯 `fqkline`，而腾讯对该类期货代码返回 `data:[]`（空 list），`tencent_kline` 取 `data.get("data",{}).get(symbol)` 在 list 上调用 `.get` 抛 `AttributeError`，被 `_fetch_kline` 兜底成空、再回退新浪 `sina_kline` 也对期货代码 456 失败 → 最终 "K线数据暂不可用"。沪金此前也因同样原因显示暂不可用。② **真实数据源排查**（遵守"无法获取真实数据需提醒"）：实测——**沪金连续 `AU0`** 新浪期货日K线接口 `InnerFuturesNewService.getDailyKLine` 稳定可用（2008 年至今 4540+ 条）；**纽约金 `GC0` / 伦敦金 `XAUUSD`** 在可访问的新浪/腾讯/东财源当前均返回空（COMEX GC、伦敦金 XAU/USD 历史K线源不可达）。③ **修复**：`datasource.py` 新增 `sina_futures_kline(sym)`（解析 `{d,o,h,l,c,v}`）；`get_kline` GOLD 分支按品种映射——实物黄金(沪金)→新浪 `AU0`、纽约金 `hf_GC`→新浪 `GC0`、伦敦金 `hf_XAU`→新浪 `XAUUSD`、沪金 `nf_AU0/AU0`→新浪 `AU0`；纽约金/伦敦金真实源暂不可达时返回诚实提示「该黄金品种K线数据源暂不可用」，绝不回退造假。④ **验证**：沪金 `AU0` K线返回 5 根真实数据（2026-08-21~27）✅、实物黄金(沪金)K线可用✅、纽约金 `hf_GC`/伦敦金 `hf_XAU` 返回友好提示而非崩溃✅。
- **fix·中 + 新增·中：收益分析收益数值可自定义编辑（持久化）+ 日历点击刷新 + 新增持仓不计日收益**：① **收益可编辑**：新增表 `pnl_override(market,code,pnl_type,range_val,detail_pnl,combo_pnl)`（唯一键 `(market,code,pnl_type,range_val)`，`__COMBO__` 为组合总收益行）；`database.py` 增 `get_pnl_override/save_pnl_override/get_all_pnl_overrides`；后端 `pnl_analysis` 套用覆盖（`details[].pnl` 用 `detail_pnl`、`combo_pnl` 用 `__COMBO__` 行，返回 `edited` 标记）；新增 `POST /api/pnl-override`（保存/清除）。前端 `Analysis.jsx` 顶部「当日/本月/本年/累计收益」与明细表「区间收益」列均改为可编辑 `input`（失焦/回车保存，调 `api.savePnlOverride`），刷新/重载后保留编辑值；`api.js` 新增 `savePnlOverride`。② **日历点击刷新修复**：日历图 EChart 加 `onEvents={{click:onCalClick}}`，点击某天 cell 取日期 → 切日收益模式 + 设 `pnlRange` + 显式 `loadPnl()`，**刷新到目标日期**（修复此前点击不刷新）。③ **新增持仓不计日收益**：`pnl_analysis` day 模式遍历相邻交易日对时，对「前一天无快照」的持仓（即当日才新增、添加前无持仓）跳过（`pv is None` 即跳过），其"从0到市值"不计入当日收益——符合需求；month/year/cum 同理跳过新持仓首日。④ **隐藏 bug 修复**：前端 `api.pnlAnalysis` 原拼 `&range=`，但后端参数名是 `range_val`（v23 改 `range`→`range_val` 防遮蔽内置但未同步前端），导致范围参数永远为空、收益分析恒用今天；现前端改为 `&range_val=` 与后端一致。⑤ **构建产物**：`vite build` → `frontend_dist/assets/index-*.js`（CSS 签名不变 `index-BUvwpTmp.css`）。⑥ **验证（全过）**：编辑茅台 detail=9999 + combo=8888 → 返回 9999/8888 且 `edited=true`、清除后恢复 -1000/-1500 ✅；新增持仓（仅当日有快照）day 模式不计入 ✅；日历点击代码逻辑确认 ✅；前端渲染编辑 input 存在、图表切换无错、控制台 0 错误 ✅；测试数据已清理，库干净。
- **fix·中：新增基金持仓用单位净值（NAV）计算市值**：① **根因**：`_positions_data` 的 FUND 分支用 `sina_quotes` 实时价（盘中估值）算基金市值，与需求"用净值、未闭市用前一交易日、已闭市且当日净值更新用当日"不符。② **修复**：FUND 分支改取天天基金单位净值 `fund_kline(code, count=1)` 最新 NAV 作 `price` 计算市值。**NAV 每日收盘后才更新**——取 NAV 最新一条即天然满足「当日已更新净值用当日、否则自动为前一交易日」，无需单独判断闭市时间。③ **验证**：加基金 161725 持仓，查 `/api/positions` 返回 `price=0.5463`（NAV 最新净值）而非实时价 0.5464 ✅（加仓时按当日价算数量、持仓展示按 NAV 算市值，二者分工正确）。④ **数据真实性**：NAV 来自天天基金真实净值接口，非虚拟；源不可达时 `data_available=False` 诚实提示。

### v24（2026-08-27）
- **修复·低：总览「持仓盈亏分布」柱状图横坐标排序方向反转（改为收益高→低）**：v23 该图按收益**升序**（亏在左、赚在右），用户反馈方向反了。修复：`Dashboard.jsx` 第 40 行排序比较由 `va - vb`（升序）改为 `vb - va`（降序），横坐标由左至右为**收益高（赚）→ 收益低/亏损**，与用户预期一致；同级仍按名称稳定排序。仅改一行比较符号，已 `vite build` → `frontend_dist/assets/index-BGEkeBei.js`（CSS 签名不变 `index-BUvwpTmp.css`，旧 `index-D7dqpddD.js` 已清理）。验证：通过后端接口取真实持仓（贵州茅台/苹果/平安银行 pnl 分别为 129230/31345/1159），用与前端完全相同的比较器排序，结果从左到右严格降序（茅台→苹果→平安银行）✅；测试持仓与快照已清理，库干净。
- **修复·高 + 新增·中：指数看板新增期货指数数据源 + 修复 K 线端点 500 崩溃（"Unexpected token 'I'…"）**：① **feat·期货指数**：行情看板「＋ 新增指数」表单新增「期货指数」市场选项，并新增 **纽约金 / 沪金 / 伦敦金** 三个快捷预设按钮（纽约金=`hf_GC` COMEX、沪金=`nf_AU0` 上期所黄金连续、伦敦金=`hf_XAU` 现货黄金），提示文案补充期货指数说明。**默认纽约金指数**已由 `DEFAULT_INDICES` 的 `("纽约黄金期货", "GOLD", "hf_GC")` 在首次启动填充（当前库已含，id=10），无需重复添加。② **bug 修复**：点击 A股板块指数等卡片查看 K 线时前端报 `Unexpected token 'I', "Internal S"... is not valid JSON`——根因是 `/api/kline` 只以 `except ds.DataSourceError` 包裹 `tencent_kline`，当腾讯对个别代码返回 HTTP 200 但行数据异常（非数字 / `day` 为字典而非列表）时 `float()` 抛 `ValueError`/`TypeError` 冒泡成 500，Starlette 非调试态返回纯文本 `Internal Server Error`，前端 `res.json()` 解析即报该错。现：`tencent_kline` / `tencent_us_index_kline` / `sina_kline` 解析时校验 `rows` 为列表、跳过/容错非数字行、非列表数据显式抛 `DataSourceError`；抽出 `_fetch_kline()` 统一「腾讯优先→不足 2 根回退新浪」且用 `except Exception` 兜底；`get_kline` 整段包 `try/except Exception → fail("K线数据暂不可用")`，**任何未预期异常都返回 JSON 友好提示而非 500**。修复后点击任意指数卡片稳定弹出「📭 K线数据暂不可用」（数据源确实无该 K 线时），不再崩溃。构建产物 `frontend_dist/assets/index-DK7qpwFY.js` + `index-BZ-7z_9F.css`（旧 `index-Sg8vXs71.js` 已清理）。
- **修复·中：录入交易「确认份额日期」跳过非交易日（周末+法定节假日）+ QDII 也可选交易时间（15:00 前/后）**：① **根因**：`Positions.jsx` 原 `nextTradingDay()` 仅跳过周六周日、**注释明确"不含法定节假日"**——确认份额日期遇到法定节假日会落在休市日（即用户所说"没有跳过非交易日"）。② **方案**：后端 `datasource.py` 新增 **中国 A股 休市安排**（2026 年，来源为**沪深北交易所官方休市安排公告**，经东方财富转发，权威真实；代码内标注来源链接 `https://stock.eastmoney.com/a/202512223598373130.html`），与 `is_cn_trading_day()` / `next_cn_trading_day()`（纯 `date` 运算，无时区漂移）；`main.py` 新增 `GET /api/fund/confirm-date?trans_date=&qdii=&time=`，按"境内 15:00 前 T+1 / 15:00 后 T+2、QDII 恒 T+2"计算并**跳过周末与法定节假日**返回 `confirm_date` + `available` + `holiday_source_note`。前端 `Positions.jsx` 移除本地 `nextTradingDay`，改为调用该接口（含 `available=False` 提示）；**QDII 不再隐藏交易时间选择器**（仍显示 15:00 前/后选择框，但仅作记录、不影响 T+2），提示文案同步更新为"跳过非交易日"。③ **真实数据约束**：休市安排属公开固定的官方规则（非行情虚构数据），以真实来源为准；**未收录年份（历史/未来）`available=False`**，前端显示"⚠️ 休市安排待更新仅跳周末"并提示手动核对法定节假日（遵守"无法获取真实数据时提醒用户"）。④ **构建产物**：已 `vite build` → `frontend_dist/assets/index-C55y3T9y.js`（CSS 签名不变 `index-BZ-7z_9F.css`，旧 `index-DK7qpwFY.js` 已清理）；新增后端接口 `/api/fund/confirm-date`。
- **新增·中：组合分析新增「收益分析」模块（日/月/年/累计 + 收益明细 + 柱状/日历图）**：① **需求**：组合分析页新增收益分析模块，可选日收益/月收益/年收益/累计收益（**默认日收益**）；收益明细展示所有持仓在所选区间内的收益数量；每个持仓当日净值更新后刷新模块；图表可选柱状图/日历图（**默认日历图**）。② **真实数据基础**：原只有组合级每日快照（`snapshots`，每天一条总市值），无按持仓拆分。**新增持仓级每日快照表 `position_snapshots`**（`market/code/market_value`，按 `(snap_date,market,code)` 唯一、幂等）——`portfolio_summary()` 写组合快照时同步 `ensure_position_snapshots_today()` 记录每个持仓当天市值（行情不可用者当天不记，避免脏 0）。③ **后端接口** `GET /api/portfolio/pnl-analysis?type=day|month|year|cum&range=`：**收益 = 区间末市值 − 区间首市值**，全部来自真实每日快照（无虚拟数据）。`day`=当日 vs 前一日、`month`=该月首末差值、`year`=该年首末差值、`cum`=全部历史首末差值；返回 `combo_pnl`/`combo_pnl_pct`/`details`（每持仓 `{market,code,name,pnl}` 按收益降序）/`calendar`（每日组合收益序列）。区间起止快照缺失或历史天数不足时 `available=False` 诚实提示（如"区间起止日期快照缺失，无法计算收益"）。④ **前端** `Analysis.jsx` 新增收益分析卡片：类型切换段控（默认日）、范围输入（日=date/月=month/年=text）、图表切换段控（默认日历）；日历图用 ECharts `calendar`+`heatmap`（红跌绿涨，按日收益着色），柱状图用每持仓区间收益；「收益明细」表展示每持仓区间收益（点击行看 K 线）；模块 `useEffect` 依赖 `refreshSec`，**跟随页面自动刷新频率实时更新**（当日净值更新到最新后自动刷新），手动刷新按钮也触发 `loadPnl()`。⑤ **构建产物**：已 `vite build` → `frontend_dist/assets/index-o7n_FjpZ.js` + `index-BtoJYtps.css`（旧 `index-C55y3T9y.js` 已清理）；新增后端表 `position_snapshots` 与接口 `/api/portfolio/pnl-analysis`。
- **修复·中（2 项）：① 组合分析饼图明细按市值排序 + K 线窗口层级；② 持仓明细排序切换界面后保持**：① **饼图明细**：组合分析「资产分配」饼图点击扇区弹出的持仓明细列表，原为后端返回原序；**现按持仓市值降序排序展示**（`Analysis.jsx` 渲染时对 `allocModal.items` 副本 `.sort((a,b)=>(b.market_value||0)-(a.market_value||0))`，不修改原数据）。**K 线窗口层级**：点击明细列表中产品名称/行弹出的 K 线行情窗口此前被明细弹窗遮挡（`.alloc-modal-mask` z-index=1000 高于全局 K 线弹窗 `.modal` 的 100）；**现把 `.alloc-modal-mask` z-index 由 1000 降为 90**（低于 `.modal` 的 100），使 K 线抽屉浮于明细列表窗口之上（alloc-modal 与录入/编辑等 `.modal` 不会同时出现，无冲突）。② **排序持久化**：持仓管理页「按持仓收益/市值/成本」三种排序，原仅组件 `useState` 保存，**跳转到其他界面再回来组件卸载重建即重置**。现排序选择**持久化到 `localStorage`（`fw_pos_sort`）**——`posSort` 初始值从 `localStorage` 读取（校验合法值），`changePosSort` 在切换时写入；重新进入持仓管理页仍沿用上次选择。③ **构建产物**：已 `vite build` → `frontend_dist/assets/index-D7dqpddD.js` + `index-BUvwpTmp.css`（旧 `index-o7n_FjpZ.js` 已清理）；无新增后端表/接口，无数据导入导出变更。
- **fix·中：数据导入导出补齐「持仓级每日快照 `position_snapshots`」（收益分析历史明细）**：① **排查**：全库共 11 张用户表，导入导出（`db.export_all_data` / `db.import_all_data`）此前覆盖 10 张——自选 `watchlist`、交易 `transactions`、净值快照 `snapshots`、实物黄金 `gold_holding`/`gold_transactions`、收益编辑覆盖 `asset_profit`、持仓覆盖 `position_override`、持仓置顶 `pinned_positions`、行情看板指数 `custom_indices`；**唯独遗漏 v23 新增的 `position_snapshots`**（持仓级每日快照）。② **影响**：收益分析模块的「日/月/年/累计收益 + 每个持仓区间收益明细」全部依赖此表；其历史天数**不可重生**（过去某天的持仓市值无法回算），若导入导出不带，换机器/恢复后收益分析历史明细全空、仅剩导入当日及以后自动累积的新数据。③ **修复**：`db.export_all_data()` 新增读取 `position_snapshots` 全部行（`snap_date/market/code/market_value`）写入 payload；`db.import_all_data()` 在清空阶段加 `DELETE FROM position_snapshots`，并在写入阶段按 `(snap_date,market,code)` 幂等 `INSERT OR REPLACE`，统计加 `position_snapshots` 计数。④ **验证（round-trip）**：注入 3 天×2 持仓共 6 条历史快照 → 导出 JSON 含 `position_snapshots:6` → 清空该表 → 导入 → 表恢复 6 条（`imported.position_snapshots=6`）✅；测试数据已清理，库干净。⑤ **数据真实性**：`position_snapshots` 全部来自组合汇总时的真实每日持仓市值（无虚拟数据），导入导出为原样搬运，不造假。⑥ **前端无改动**（纯后端 `database.py`），无需重新 `vite build`；后端已重启生效。
- **fix·高 + 修正·中：收益分析计算口径统一为「日收益」单元 + 配色红涨绿跌**：① **用户原话**：「【fix】收益分析里，红色表示赚，绿色表示亏。计算当日收益时，仅计算上一个交易日所有持仓产品的收益总和。月收益和年收益通过范围内日收益来计算。计算累计收益时，计算所有持仓产品的累计收益总和。」② **配色修正**：原日历图 visualMap `['#f85149'(红),灰,'#3fb950'(绿)]`（红跌绿涨）与柱状图 `v>=0?绿:红`，**与全站 K线「红涨绿跌」(v18) 相反**；现统一改为**红色=赚（正）、绿色=亏（负）**——日历图 `['#3fb950'(绿/亏),灰,'#f85149'(红/赚)]`、柱状图 `v>=0?红:绿`。③ **计算口径修正**（原 month/year/cum 均用「区间首末总市值差值」，day 用首末差）：重写为以「相邻交易日对（上一交易日→当日）的市值差值」为基本单元——**日收益**取 ≤target 的最近两条快照（当日 + 上一个交易日），combo = 当日−上一交易日、details = 各持仓当日−上一交易日；**月/年收益** = 区间内每一对相邻交易日的组合日收益**累加**（calendar 保留每日收益序列）；**累计收益** = 所有持仓「最新市值−最早市值」**加总**（持仓层面，与收益明细严格自洽，不再用组合级首末差值）。④ 重写 `app/main.py` `pnl_analysis()`：去掉首末差值分支，改为预计算 `pairs`（相邻交易日对）→ 累加 `combo_pnl` 与 `pos_pnl[k]`（每持仓日收益累加）→ details 由 `pos_pnl` 生成；day 仅一对、month/year/cum 取区间内全部相邻对。⑤ **构建产物**：已 `vite build` → `frontend_dist/assets/index-*.js`（旧 `index-BGEkeBei.js` 已清理）；颜色改动在 `Analysis.jsx` 的 `buildBarOption`/`buildCalendarOption`。⑥ **实测验证**：注入 3 天×2 持仓（茅台/苹果）连续快照，接口返回——day(27) combo=-1500、details 茅台-1000/苹果0；month(08) combo=+1500（日收益 3000+(-1500) 累加）、details 茅台+1000；year(2026)/cum 同 +1500、details 茅台+1000；**日收益确为「上一个交易日→当日」单对、月年确为日收益累加、累计确为持仓加总、明细与 combo 自洽** ✅；日历/柱状图颜色经构建产物源码确认红涨绿跌反转 ✅；测试快照已清理，库干净。

### v23（2026-08-27）

- **修复·中：自选基金与自选标的按日涨跌幅排序，点表头可切换升/降**：行情看板「自选基金」与「自选标的」两列表此前按后端返回的原始顺序展示，无涨跌幅排序。现改为：① `Quotes.jsx` 新增 `sortByPct(arr, dir)`（默认 `dir='desc'`），按 `pct`（日涨跌幅）排序，`pct` 缺失（行情不可用）的条目用 `-Infinity` 沉底；② 组件新增 `fundDir` / `watchDir` 状态（默认 `'desc'`），`fetchWatchlist` 只存**未排序**的 enrichment 数据，渲染时 `sortedFunds = sortByPct(funds, fundDir)` / `sortedWatch = sortByPct(watchlist, watchDir)` 实时排序（切换方向无需重新请求）；③ 「日涨跌幅」「涨跌幅」表头改为 `th.sortable`，点击 `setFundDir(d => d==='asc'?'desc':'asc')` 切换方向，并带 `↓/↑` 方向指示箭头（两表方向相互独立）；④ `index.css` 新增 `.sortable` / `.sort-arr` 样式（指针 + hover 高亮 + accent 箭头）。纯前端修复，已 `vite build` → `frontend_dist/assets/index-Sg8vXs71.js` + `index-BZ-7z_9F.css`（旧 `index-D3x52Q4Z.js` 已自动清理）。
- **调整·低：行情看板暂不展示「场内基金穿透」独立模块**：v23 起，行情看板中位于「指数」与「自选基金」之间的「🔍 场内基金穿透」模块**默认不展示**（保留代码，由 `Quotes.jsx` 顶部常量 `SHOW_PENETRATION = false` 控制；后续恢复置 `true` 即可）。穿透估算能力本身未移除，仍可在「基金详情弹窗」查看单只基金的场内穿透与折溢价。
- **修复·中：总览「持仓盈亏分布」柱状图按收益排序 + 最多展示 10 个 + 新增滑动条**：总览（Dashboard）「持仓盈亏分布」柱状图此前按后端返回的字典序展示，且持仓较多时横轴标签被挤压不可读。根因：原 `Dashboard.jsx` 直接 `positions.filter(...).map(...)` 渲染，无任何排序。修复为：在 `return` 前派生 `pnlAll = positions.filter(data_available !== false).slice().sort((a,b) => b.pnl - a.pnl)`（**降序：收益最高在左、收益最低/亏损在右**——v24 修正，原 v23 为升序，用户反馈方向反了），`pnlMaxScroll = max(0, len - 10)`，`pnlView = pnlAll.slice(pnlSafe, pnlSafe + 10)`，组件新增 `pnlOffset` 状态维护滑块位置。柱状图渲染 `pnlView`（最多 10 根），下方新增 `<input type="range" max={pnlMaxScroll}>` 滑块 + 窗口位置文案（`第 N–M 个 / 共 X 个持仓`）；`pnlMaxScroll === 0`（持仓 ≤10）时滑块整体不渲染。无行情数据的持仓仍按原 `data_available !== false` 过滤，行情不可用者不出现在分布图。纯前端修复，已 `vite build` → `frontend_dist/assets/index-FELqdbKj.js` + 新增样式 `index-YSwlzfm4.css`（`.chart-scroll` / `.chart-scroll-info`）。
- **修复·中：国内金价来源改用沪金 au9999（取不到回退沪金连续）+ 实物黄金 K 线也用 au9999**：`datasource.py` 金价源 `GOLD_CN_SYMBOL` 由 `nf_AU0`（沪金主力连续）改为**先判断能否取到沪金 `au9999`（SGE Au99.99 现货，`hf_AU9999`）**——可取则国内金价使用 au9999 价格；取不到（新浪 `hf_AU9999` 返回空）回退 `nf_AU0`（沪金连续），两者皆失败返回 `available=False`（绝不造假）。新增 `_gold_price_for_symbol(symbol)` / `gold_kline_symbol()`：`gold_cn_price()`（金价条、实物黄金估值、持仓市值/收益计算 `_gold_position` / `_positions_data` / `/api/gold`）与实物黄金 K 线均优先 au9999。**持仓管理中点击实物黄金（沪金）卡片的 K 线**此前因 `code=GOLD_PHYSICAL` 被直接当 symbol 永远取不到；`/api/kline` 新增 `mkt == "GOLD"` 分支：`code == GOLD_PHYSICAL` 时用 `gold_kline_symbol()`（au9999 优先）取 K 线，期货指数（纽约金 `hf_GC` / 沪金 / 伦敦金 `hf_XAU`）用各自 `code`。金价条新增来源名显示（如 `Au99.99` / `黄金连续`），tooltip 改为说明 au9999 来源与回退。

### v22（2026-08-26）

- **修复·高：新增（自定义）指数卡片 K 线只有当天、无历史**：行情看板里用户通过「＋ 新增指数」添加的美股/海外指数，点开 K 线详情后只显示 1 天数据、无历史记录。根因：`/api/kline` 对美股标的先走个股 `fqkline`（`us<CODE>.OQ`）路径，而腾讯 `fqkline` 对**指数**只返回 1 天（对个股/ETF 才返回完整历史）；内置美股指数此前靠写死的 `US_INDEX` 白名单（ixic/inx/dji/sox/ndx/rut/vix）才绕开这个坑，自定义指数进不了白名单，于是落入个股分支只拿到当天。现改为：**美股标的先走个股 `fqkline us<CODE>.OQ` 路径，若拿到 ≥2 根 K 线视为个股/ETF 直接使用；否则当作指数改用指数专用 `usfqkline`（`us<CODE>`）取完整历史**；腾讯确实无数据的指数仍诚实返回「该指数历史K线数据暂不可用」（不造假）。不再依赖写死白名单，内置与自定义的美股/海外指数均显示完整历史。
- **修复·高：新增国内指数 K 线只有当天、无历史（如北证50）**：行情看板里用户通过「＋ 新增指数」添加的**国内/港股**指数（典型如北证50 `bj899050`），点开 K 线详情后只显示 1 天、无历史。根因：`/api/kline` 的国内（`else`）分支以腾讯 `fqkline` 为主源，当腾讯对部分代码（北交所 `bj` 前缀、个别指数）返回 1 天空壳或 WAF 拦截（非异常）时，代码只在 `DataSourceError` 时回退新浪、1 天数据被直接使用；此外若代码格式偏差（如只填纯数字 `899050`），新浪返回 `null` 会抛 `TypeError` 冒泡致接口 500。现与美股分支对称地重构 `else` 分支：**腾讯拿到 ≥2 根 K 线直接使用；否则回退新浪 `sina_kline` 取完整历史；新浪解析异常（`null`/`空`）也当无数据处理，不再抛 TypeError**。新增北证50（`bj899050`）现稳定返回 120 天历史；错误代码诚实返回「K线数据为空」。
- **构建产物**：本次为纯后端逻辑修复，前端 `frontend_dist` 无需重新构建（构建签名保持 `index-Do2TGEw7.js` + `index-Ckd0pVwt.css`）。
- **数据库 / 接口**：无新增表/接口；`GET /api/kline` 国内/港股分支取数策略调整（对外行为不变）。
- **修复·中：清仓产品在明细与总览只显示代码、不显示名称**：持仓管理（明细）与总览（Dashboard）中「已清仓」行仅展示产品代码、无名称。根因：`_positions_data()` 对 `sold_out` 行直接 `p["name"] = p["code"]`（原注释"不拉行情"误将名称也置为代码），而前端用 `p.name || p.code` 渲染，于是清仓行只显示代码。修复：清仓行仍不拉行情价格（数量/成本/市值保持置空），但改为**尝试从行情取真实名称**（`sina_quotes` 取 `name`，失败/退市回退代码），与正常持仓的 name 行为一致。
- **修复·高：持仓明细排序"无反应" + 置顶方向反了（跑到尾部）**：
  - **置顶方向反了（实锤 bug）**：`Positions.jsx` 的 `sortedPositions` 置顶二次排序写成 `pinnedA - pinnedB`，JS `sort` 返回正数时 a 排后面，导致被置顶的产品反而沉到列表**尾部**。改为 `pinnedB - pinnedA`（置顶项比较函数返回负数 → 排最前）。
  - **排序"没反应"**：根因在后端——`_positions_data()` 行情不可用时把 `market_value` **回退成 `cost`**、`holdingPnl` 置 0，使「按市值」与「按成本」对不可用持仓完全混同、且「按收益」全是 0，三种排序大量坍缩、肉眼"不变"。修复：`market_value`/`price` 在行情不可用时保持 `None`（不伪装成成本），"按市值"对不可用持仓自然排在末尾，正常持仓按真实市值排序，三种顺序明显不同。
  - **构建产物**：本需求改了前端 `Positions.jsx` 与后端 `main.py`，已重新 `vite build` → `frontend_dist/assets/index-HC1fv1ss.js`（CSS 签名不变 `index-Ckd0pVwt.css`，旧 `index-Do2TGEw7.js` 已清理）。
- **修复·中：总览持仓概览卡片按市值由多到少排序**：总览（Dashboard）「持仓概览」卡片此前直接按后端返回顺序渲染（交易重放后的字典序），未按市值排序。现改为：渲染前对持仓按 `market_value` **降序**排序（市值 `null` 的清仓/行情不可用行排最后），次级键按名称稳定排序。纯前端修复，已重新 `vite build` → `frontend_dist/assets/index-Do1lrktw.js`（CSS 不变 `index-Ckd0pVwt.css`）。
- **新增·高：任意界面点击名称/卡片均可查看 K 线行情**：此前仅行情看板的指数卡片/自选标的能看 K 线。现把 K 线弹窗提升为**全局组件**（`src/kline.jsx` 的 `KlineProvider` + `useKline()`，挂载于 `App.jsx`），**总览持仓概览卡片、持仓管理卡片与表格行名称、组合分析饼图明细行、报表表格行**均加点击 → 弹出日K·MA·MACD 行情（清仓/行情不可用标的也可点开看历史 K 线）；行情看板原有 K 线入口统一改用全局弹窗（去掉局部重复实现）。纯前端新增，已重新 `vite build` → `frontend_dist/assets/index-BxF17-dM.js`（CSS 不变 `index-Ckd0pVwt.css`）。

### v21（2026-08-26）
对应 workbuddy 项目计划中 6 个进行中任务：

- **修复·低：持仓明细排序按钮"无反应"**：`Positions.jsx` 排序 comparator 增加**次级稳定键 `(market, code)`**——行情不可用时 `market_value` 回退为 `cost`、`holdingPnl` 置 0，三个排序键结果坍缩为同一顺序，列表肉眼无变化；加次级键后即使主键相同也能稳定区分。**顺带修复置顶排序方向 bug**：原代码 `pinnedSet[b...] - pinnedSet[a...]` 写反，置顶的产品反而被排到列表末尾（现已改为 `a - b`，置顶永远最前）。排序按钮全部补 `type="button"` 防御性处理。
- **修复·低：默认指数增加纽约黄金期货**：`app/database.py` `DEFAULT_INDICES` 新增 `("纽约黄金期货", "GOLD", "hf_GC")`（COMEX 黄金主力，新浪真实报价，与沪金 `nf_AU0` 并存）。首次启动自动填充（老库表非空不自动追加，可在行情看板手动添加）。
- **修复·高：实物黄金改为在持仓明细中编辑**：① 删除实物黄金卡片的「保存实物黄金」按钮与克数/成本价输入框（流水录入仍是增减克数的入口；保留「清空实物黄金」兜底重置）；② 持仓明细表格实物黄金行开放「编辑/置顶/删除」按钮，点击「编辑」直接改克数（数量）与总成本（元）；③ 后端 `POST /api/position/override` 兼容 `market=GOLD`：数量即克数、成本即总成本，转写为 `save_gold_holding(grams, cost/grams)`，不写 `position_override` 表；④ `_positions_data` 应用覆盖时跳过 `(GOLD, GOLD_PHYSICAL)`，避免黄金行重复。
- **修复·中：指数卡片可移动位置 + 新增指数名称自动识别**：① 每个指数卡左上角新增 `▲`/`▼` 按钮，顶部/底部自动禁用；新接口 `POST /api/indices/{idx_id}/move?dir=up|down`（与相邻行交换 `sort_order`，数据库 `swap_index_sort_order()`）；② 修复名称自动识别失效：前端 `addIndex` 不再用代码兜底名称（此前 `name.trim() || code` 让后端永远收不到空串），名称留空时后端自动从行情拉取真实名称（如 `sh601318` → 「中国平安」）。
- **修复·高：新增持仓/录入交易非交易时段回退最近价格**：`datasource.py` 新增 `_last_close(market, code)`——`sina_quotes` 当日无报价（周末/节假日/收盘后）时自动取最近一个交易日收盘价（基金走天天基金净值历史、美股指数走腾讯 `usfqkline`、其余走腾讯 K 线，腾讯接口偶发 501 自动重试 3 次、A 股追加新浪 K 线兜底）；`_manual_add_one` 取价失败时回退，返回结果新增 `price_source` 字段（`today` 当日价 / `last_close` 最近收盘价）。
- **修复·低：场外基金净值显示 4 位小数**：`Quotes.jsx` 自选基金表、基金详情弹窗「最新/累计净值」、`Positions.jsx` 持仓卡片/明细表现价、新增持仓结果区、`Dashboard.jsx` 总览持仓现价——基金（`market=FUND`）统一显示 4 位小数（后端本就保留原始精度，仅前端展示位数不足）。
- **顺带修复 v20 隐藏 bug（高）**：实物黄金已实现收益计算 `compute_gold_realized_pnl()` 此前复用 `_rebuild_gold_from_txns()` 会**按流水重放并写库覆盖 `gold_holding`**——无流水时每次拉取持仓/金价都会把手动设置的克数与成本价清零（即 v20 中"保存实物黄金"后刷新即失效）。现拆出纯计算函数 `_calc_gold_realized_pnl()`（不写库），`_rebuild` 仅在流水增/删时调用，手动编辑的克数/成本价不再被冲掉。
- **构建产物**：`frontend_dist/assets/index-Do2TGEw7.js`（1252.60 kB）+ `index-Ckd0pVwt.css`（15.26 kB），旧 `index-BzO96wyj.js` / `index-hXyws66x.css` 已清理。
- **数据库 / 接口**：无新增表；新接口 `POST /api/indices/{idx_id}/move`；`POST /api/position/override` 扩展支持 `market=GOLD`（克数/总成本转写）；`_manual_add_one` 返回值新增 `price_source` 字段。

### v20（2026-08-25）
对应项目计划系统中 6 个进行中任务（reQmeB / r8bQuM / rhuyTf / rH4zGj / rdYvn7 / rkxNVl）：

- **卖光产品保留持仓行**（task reQmeB，**fix·高**）：修复"卖出把现有产品持仓卖光后该行消失"的体验问题。`database.compute_positions_including_soldout()` 在保持原 `compute_positions()` 行为的同时新增聚合函数，**保留已卖光（qty≈0）产品**，输出新增 `sold_out=True` 标记、`quantity/avg_cost/cost=None`；`_positions_data()` 改用该函数，对售罄产品跳过行情拉取、市值置空，累计收益直接采用 `compute_realized_pnl()` 中的**全部已实现盈亏**，并由 `portfolio_summary()` 计入整体组合的累计收益。**前端兼容**：持仓卡片/表格行新增「已清仓」徽章，数量/持仓成本/现价/市值显示「—」，卡片新增「累计收益」行；表格行售罄产品**屏蔽「编辑」按钮**（保留「置顶」与「删除」），数据准确性与人性化兼顾。
- **输入框历史记忆**（task r8bQuM，feat·低）：所有可填输入框（产品代码/金额/收益/克数/价格/日期/备注/交易表单/批量导入前的代码/指数代码名称等）**记忆最近 5 个值**，下次点击输入框**下拉快速回填**，单项可 ✕ 单独删除。**前端 localStorage 实现**（命名空间 `fwt:hist:`），零后端改动；记忆**不跨机器迁移**（与本地运行、单用户免登录的架构一致）。
- **底部数据来源展示**（task rhuyTf，feat·低）：网站最底部开源地址旁**新增一行小字**：`数据来源：新浪财经行情 · 腾讯行情 · 天天基金 · 东方财富板块资金流 · 新浪财经资讯（实时数据，获取失败均提示「数据暂不可用」，绝不填充虚拟数据）`。声明性静态文本，无接口调用，零运行开销。
- **饼图点击查看持仓明细**（task rH4zGj，feat·低）：组合分析「资产分配」饼图**支持点击扇区弹窗**，展示该类型（市场/标的）下的**所有产品明细**：产品代码、产品名称、该产品市值、占该类型百分比。`EChart.jsx` 增强支持 `onEvents` 事件透传（`chart.on/off` 绑定，option 变化时重绑）；后端 `/api/portfolio/allocation` 每项增加 `items: [{code, name, market_value, pct}]`，`pct=该产品市值/该类型市值=占所有持仓百分比`（饼图按类型聚合，pct 即为整体占比）。旧前端仅用 `name/value` 不受影响。**v23 起**：① 明细列表**按持仓市值降序排序**展示；② 点击列表中产品名称/行弹出 **K 线行情窗口浮于明细列表窗口之上**（修复此前 K 线窗口被明细弹窗遮挡的问题）。
- **新闻市场/行业标签**（task rdYvn7，feat·低）：财经资讯每条新闻标题旁**新增小字标签**「国内·医药」「国际·芯片」等，市场（国内/国际）+ 行业（芯片/新能源/光伏/医药/银行/地产/券商/白酒/消费/能源/黄金/科技/军工/综合）。**后端 `/api/news` 路由新增 `_classify_news(title, summary)` 文本规则**（基于新浪真实标题/摘要关键词，**不改原文，仅附加标签**），国际关键词（美/美股/美联储/欧洲/欧股/日经/港股/全球/关税/非农/英伟达/特斯拉/谷歌/微软/华尔街/欧洲央行 等），行业关键词（芯片/半导体/新能源/锂电/电池/光伏/医药/疫苗/银行/地产/券商/白酒/消费/石油/煤炭/天然气/黄金/科技/ai/算力/军工 等）；未命中默认「国内·综合」（非虚拟数据）。
- **基金列表表头对齐**（task rkxNVl，fix·无）：修复自选基金列表「最新净值」「日涨跌幅」**表头左对齐、数据右对齐**的错位。`index.css` 新增 `.fund-table th.num, .fund-table td.num { text-align: right; }`（特异性 0,2,1 覆盖 `.fund-table th{text-align:left}` 的 0,1,1），统一表头与数据右对齐。
- **后端兼容性修复**：`portfolio_summary` 累加 `market_value/cost` 时改用 `p.get(x) or 0`（售罄产品为 None，key 存在时 `.get(k, 0)` 仍返回 None 会导致 TypeError）；`_positions_data` 售罄分支补齐 `data_available/sold_out/price/market_value/name` 字段。
- **构建产物**：`frontend_dist/assets/index-BzO96wyj.js`（1252.37 kB）+ `index-hXyws66x.css`（14.96 kB），旧 `index-CD5AA75F.js` / `index-bzP9Xtjl.css` / `index-faOVXw28.css` 已清理。
- **数据库 / 接口**：无新增表/接口（售罄聚合复用 `transactions` + `compute_realized_pnl`；输入记忆纯前端；新闻分类后端无新接口；饼图明细复用 `/api/portfolio/allocation` 扩展返回 items；表头对齐纯 CSS）。

### v19（2026-08-25）
- **持仓管理页布局调整**（task rtz48h）：**实物黄金模块移至页面最下方**，**数据导入/导出模块移至实物黄金下方**（之前均在页面顶部）。实物黄金买卖流水表**每页最多展示 5 条**，超过的**增加翻页按钮**（‹ 上一页 / 下一页 › + 共 N 条 · 第 x / y 页），仍保留全部数据，录入/删除后回到第 1 页。
- **持仓管理功能增强**（task rpwvqw）：① **默认按持仓收益降序**排序（之前默认按市值），保留**按市值 / 按持仓成本**切换（三按钮），与"按每个产品的持仓收益排序"任务一致。**v23 起排序方式持久化**：选择的排序（按收益/市值/成本）写入 `localStorage`，**跳转其他界面再回来仍保留上次选择**（修复此前切换界面后排序被重置的问题）。② **允许置顶产品**（置顶数量不限），**后端持久化**（新表 `pinned_positions`）并随数据导入导出跨机器迁移；置顶产品永远排在列表最前面（行内 📌 标记 + 表格上提示），**📌 已置顶 / 置顶** 按钮一键切换。③ 持仓明细（含顶部持仓卡片概览）**每页最多 20 条**，超过的增加翻页（每页 20 条 · 第 x / y 页）。
- **行情看板指数自定义**（task rUo1vv）：指数配置后端持久化（新表 `custom_indices`），可**点击「＋ 新增指数」**添加（市场 + 代码 + 名称，名称留空则自动识别），**每个指数卡片右侧 ✕** 一键删除；首次启动自动填充 9 个内置预置（上证 / 深证 / 创业板 / 科创50 / 沪深300 / 纳斯达克 / 标普500 / 道琼斯 / 费城半导体）。
- **A股板块资金流向面板**（task rUo1vv）：行情看板新增「💰 A股板块资金流向」面板，**当天前十流入 + 前十流出**行业板块（主力净流入 / 涨跌幅 / 占成交比），并排双表展示，标注数据更新时间。**数据源：东方财富 `push2delay.eastmoney.com` 真实接口**（`fid=f62` 主力净流入），数据获取失败时显示「数据暂不可用」，绝不填充虚拟数据。新接口 `/api/quotes/sector-flow`。
- **场内基金穿透位置调整**（task r3OVpV）：**场内基金穿透模块移至「指数」和「自选基金」之间**（之前在自选基金之后），更符合用户查看顺序。
- **新增持仓自动同步自选**（task r3OVpV）：录入交易 / 新增初始持仓（直接录入）时，**自动把该资产加入自选标的**（实物黄金除外——黄金有独立模块），无需再到行情看板重复添加。后端 `add_txn` 与 `_manual_add_one` 成功后调用 `db.add_watchlist`，幂等 `INSERT OR IGNORE`。
- **前端代表性页面截图**（task rV8Dxz）：用 Playwright（headless Chromium，1440×900 @2x）为 5 个代表性页面（总览 / 行情看板 / 持仓管理 / 组合分析 / 财经资讯）截图，保存至 `screenshots/`，**README 添加「界面预览」章节链接展示**，便于 GitHub 上直接看到前端效果。
- **后端新增表 / 接口**：数据库新增 `pinned_positions`（持仓置顶）与 `custom_indices`（指数配置）两张表，启动时 `init_default_indices()` 自动填充 9 个默认指数；数据导入/导出同步携带这两张表（跨机器迁移不丢失）。新接口：`GET/POST/DELETE /api/indices`、`GET /api/position/pins`、`POST /api/position/pin`、`DELETE /api/position/pin/{market}/{code}`、`GET /api/quotes/sector-flow`。

### v18（2026-08-25）
- **修复**：场内基金穿透列表「穿透数据不可用：无法获取持仓数据」+ 列表只显示代码不显示名称。根因：`fund_holdings` 只能解析 A股 6 位代码持仓行，QDII/跨境 ETF（如 513100 纳指）的重仓是**美股**（NVDA/AAPL 等字母代码）导致解析为 0 条持仓。改为**按 td 位置通用解析**（兼容 A股 / 美股 / 港股三类持仓行），重仓股行情按代码自动映射新浪 symbol（A股 `sh/sz`、港股 `hk`、美股 `gb_`）；穿透失败时尽力补齐基金名称（新浪 `fu_` 报价），列表卡片始终显示「名称 + 代码」。接口：`/api/fund/penetration/holdings`、`/api/fund/penetrate`。
- **修复**：K线 / 成交量 / MACD 红绿颜色与全站不一致。此前 K线阳线为绿色、阴线为红色（美股习惯），与全站 `--up` 红涨 / `--down` 绿跌相反。统一为**红涨绿跌**：K线阳线（上涨）红色、阴线（下跌）绿色，成交量与 MACD 柱随涨跌同色。
- **修复**：黄金 / 股票基金卖出后累计收益计算错误。两处根因：① 实物黄金已实现收益存于 `gold_transactions` 表，但 `_gold_position` 从股票/基金交易（`transactions` 表）的 realized 字典取值，恒为 0 → 卖出落袋收益丢失、累计收益随持仓减少而下降；改为直接调用 `compute_gold_realized_pnl()` 按黄金流水重放计算。② `compute_positions` 消费 `get_transactions` 的倒序结果（最新在前），卖出先于买入处理导致**持仓不减、卖出失效**；改为按 (成交日期, id) 升序重放。修复后：已到手收益单独记录（`realized_pnl`），当前持仓收益按剩余持仓实时重算，累计收益 = 已实现 + 实时持仓收益。
- **修复 / 改名**：Windows 启停脚本 `start.bat` 更名为 `operation.bat`（内容与用法提示同步，与 Linux `operation.sh` 命名对齐）；`setup_env.bat` 与全部文档（README / 功能说明文档 / 任务对话与产物记录 / 推送说明）引用同步更新。

### v17（2026-08-25）
- **新增**：自动刷新频率可配置。侧边栏新增"自动刷新"下拉选择器（可选 5 / 10 / 30 / 60 秒，默认 30 秒，`localStorage` 持久化）；总览 / 行情看板 / 组合分析 / 财经资讯四页的轮询间隔统一跟随该设置**实时生效**，页面文案动态显示当前频率。新增 `frontend/src/refresh.js`（RefreshContext + 持久化）。
- **新增**：网页 favicon。新增金融风图标（`frontend/public/favicon.svg`：深色卡片 + 上升折线 + 金色端点），`index.html` 引用，构建后由 FastAPI 托管在站点根目录 `/favicon.svg`。
- **新增**：前端底部显示开源地址。主区最底部固定 footer，展示 GitHub 开源路径 `https://github.com/Ar4enal/finance-dashboard`（点击新标签跳转）。
- **修复 / 改名**：Linux 启停脚本 `start.sh` 更名为 `operation.sh`（脚本内部用法提示同步更新）；`setup_env.sh` 的生成逻辑与全部文档（README / 功能说明文档 / 任务对话与产物记录 / 推送说明）中对 `start.sh` 的引用同步更新为 `operation.sh`。

### v16（2026-08-25）
- **新增**：实物黄金支持**买卖交易流水录入**（方向/克数/价格/日期/备注）。后端 `gold_transactions` 新表 + `add/delete` 接口；按**加权平均成本**自动更新克数与成本价（买入加仓、卖出按均价减仓、超卖忽略），并自动累计**已实现收益**（`(卖价−均价) × 克数`）。删除任意一笔交易后按剩余流水**重放重建**持仓与已实现收益。/api/gold 新增 `realized_pnl` 字段；持仓页「实物黄金」卡片新增流水表单与列表。接口：`GET/POST/DELETE /api/gold/transactions`。
- **新增**：场内基金穿透概览**按当天收益排序**（`/api/fund/penetration/holdings` 排序键：估算涨跌 `est_pct` 降序，缺失用实际净值涨跌 `pct`；穿透失败的基金垫底）。
- **修复**：通过「新增持仓」录入的基金（`position_override` 中 `market=FUND`）未出现在**场内基金穿透**与**组合分析饼图**中。`/api/fund/penetration/holdings` 改为合并 `position_override` 的 FUND；`/api/portfolio/allocation` 改用 `_positions_data()`（统一口径含覆盖/新增/黄金），保证饼图与汇总数据一致。
- **修复**：编辑累计收益后系统没有随实时收益自动更新。`asset_profit` 表新增 `cum_pnl_base_override` 列（历史收益基准）并 ALTER 迁移旧库；编辑累计收益时后端换算 `base = 编辑值 − 实时持仓收益`；展示时 `累计收益 = 历史基准 + 实时持仓收益`，随行情自动联动。旧数据无 base 时保留原「总额固定」兼容行为。导出/导入同步该字段。

### v15（2026-08-25）
- **新增**：组合分析页补全**净值曲线与最大回撤**（此前文档有描述但前端未实现、功能链路断裂）：
  - 后端新增**每日净值快照**机制：组合汇总接口每天首次计算时自动写入当日快照（`snapshots` 表，每日一条、幂等），随使用天数自然积累，快照口径与组合汇总一致（含实物黄金按成本兜底）；
  - `/api/portfolio/performance` 增加 `period` 参数（`all` 默认 / `30` / `90`，过滤最近 N 条）并返回 `count`；
  - 前端组合分析页新增「净值曲线（每日快照）」卡片：总市值 / 总成本双线图（ECharts），下方展示最大回撤、累计收益率、快照天数、最新总市值，30s 轮询刷新；无快照时提示"每天首次打开自动记录"。

### v14（2026-08-24）
- **新增**：**数据导入导出（跨机器迁移）**。持仓管理页新增"数据导入 / 导出"卡片：
  - **导出**：把全部用户数据（自选 / 交易 / 净值快照 / 实物黄金 / 收益覆盖 / 持仓覆盖）汇总为 JSON，写入项目根目录 `持仓数据_export.json` 并触发浏览器下载；
  - **导入**：把导出文件放到项目根目录（同路径）后点击导入，先清空当前数据再整体恢复，导入前二次确认、导入后展示各类数据统计；
  - 新增接口 `GET /api/data/export`、`GET /api/data/export/info`、`POST /api/data/import`；导出文件已加入 `.gitignore` 不进版本控制。

### v13（2026-08-24）
- **新增**：新增持仓支持**批量导入**。新增持仓弹窗内置"单条 / 批量"两种模式切换；批量模式整批指定市场后**多行粘贴**（每行 `代码 持有金额 [持有收益]`，支持空格 / Tab / 逗号 / 顿号分隔，持有收益可留空），实时显示解析条数；逐条调用后端批量接口，并**展示每条导入结果**（✅ 成功含现价 / 数量 / 成本，❌ 失败含原因）。新增接口 `POST /api/position/manual/batch`（接收 JSON 数组，返回 `{total, success, failed, results}`）。

### v12（2026-08-24）
- **新增**：持仓管理支持**新增初始持仓**（直接录入，不用通过交易记录）。输入产品代码 + 持有金额（当前市值）+ 持有收益（浮动盈亏），后端用**当日价格自动计算**持仓数量（= 持有金额 / 价格）与成本（= 持有金额 - 持有收益），存入 `position_override`，与交易聚合持仓统一展示。新增接口 `POST /api/position/manual`。

### v11（2026-08-24）
- **新增**：自选基金详情弹窗支持**净值 K 线**（天天基金单位净值历史，含 MA/MACD/成交量），基金详情页可查看净值走势。
- **改进**：低覆盖率基金穿透估算采用**分档策略**：覆盖率 ≥ 60% 直接重仓加权；覆盖率 < 60% 用基金近20日平均日涨跌填充未覆盖持仓（`calibrated` 模式），消除"未覆盖当0"的系统性偏差；前端显示校准提示。
- **改进**：QDII 代码清单从 60 个扩充到 **119 个**；录入基金交易时提供**手动勾选"非国内基金（QDII）"兜底**，即使代码不在内置清单中也可正确标记 T+2；自动识别命中时复选框禁用并标注"已自动识别"。

### v9（2026-08-24）
- **新增**：QDII（非国内）基金自动识别，录入基金交易时确认份额固定 T+2（不受 15:00 前后影响）；内置 60 个常见 QDII 代码清单，新增接口 `/api/fund/qdii`、`/api/fund/qdii/check`。
- **新增**：局域网访问支持，启动脚本自动探测并打印局域网访问地址。
- **新增**：Windows 部署脚本 `setup_env.bat` + `operation.bat`，实现跨平台（Linux/Windows）部署。
- **新增**：本功能说明文档（功能清单 + 更新日志）。
- **修复**：`setup_env.sh` 不再覆盖现有 `operation.sh`（保留局域网访问支持）。

### v8（2026-08-24）
- **新增**：指数 K 线三面板标注图名（`K线 · MA` / `成交量` / `MACD`）。
- **修复**：自选基金全部删除后不再自动重刷默认基金（`localStorage` 首次初始化标记）；自选可添加场外基金（`0` 开头代码优先按场外基金识别）。
- **新增**：录入基金交易支持"交易时间"选择（15:00 前/后），自动计算确认份额日期（T+1/T+2，跳过周末）。

### v7（2026-08-24）
- **修复**：美股指数 K 线只显示 1 天的问题（改用腾讯 `usfqkline` 接口，纳指/标普/道琼斯返回 120 天）。
- **修复**：费城半导体无 K 线数据，明确提示"该指数历史K线数据暂不可用"。
- **新增**：持仓明细支持直接删除、编辑数量/成本、按持仓成本排序。
- **新增**：`operation.sh` 启停脚本（start/stop/restart/status）；README 更新启停脚本示例。
- **修复**：国内金价数据源确认（上期所黄金期货），新增 `asof` 字段显示更新时间。

### v6（2026-08-24）
- **新增**：场内基金穿透计算（季报前十大重仓股 + 盘中实时股价加权估算），含估算涨跌、覆盖率、折溢价、重仓股贡献归因。
- **新增**：持仓管理新增实物黄金卡片（克数与成本价编辑、实时金价估值）。
- **新增**：组合分析、报表导出、财经资讯等模块完善。
- **说明**：本记录从本版本起详细维护；更早版本（v1–v5）为项目基础功能逐步成型阶段。

---

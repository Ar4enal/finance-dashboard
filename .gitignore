# 依赖与虚拟环境
node_modules/
.venv/
__pycache__/
*.pyc

# 运行时数据（SQLite、导出文件、日志）
data/
# 自动导出备份（v34 需求5）：data/ 已整体忽略，此处显式声明以防未来调整上层规则时误将备份入库
#（自动导出文件含持仓明细，属于隐私数据，绝不能提交）
data/auto_export/
# 持仓数据_export.json
server.log
*.log

# IDE
.idea/

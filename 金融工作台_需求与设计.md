# 金融工作台 — 需求确认与技术方案（V1）

> 状态：**需求已确认可行，待你批准后开始编码**
> 目标用户：个人投资者 · 单用户免登录 · 本机 Linux 本地运行

---

## 一、业务定位

面向个人投资者的 **Web 金融工作台**，核心是两个能力：**投资/交易记录** 与 **资产组合管理**。
不接券商、不做真实下单，只做"记账 + 看行情 + 组合分析"。

| 维度 | 确认结果 |
|---|---|
| 业务场景 | 投资/交易工作台 + 资产组合管理 |
| 目标用户 | 个人投资者（单用户、免登录） |
| 应用形态 | Web 网页应用（本地 localhost 运行） |
| 数据来源 | 免费公开行情接口 + 手动录入 |
| 交易方式 | 仅记录 / 模拟，不接券商 |
| 市场覆盖 | A股/场内、美股、港股、黄金、公募基金、债券（可转债） |
| 功能深度 | 行情看板 + **K线/技术指标** + 持仓记录 + 组合分析 + 报表 + **财经资讯** |

---

## 二、核心功能模块

1. **行情看板**
   - 自选列表（可增删标的）
   - 实时报价：最新价、涨跌额、涨跌幅、更新时间
   - 支持市场：A股/场内、美股、港股、黄金（现货 + 黄金ETF）、基金、债券
   - 前端定时轮询刷新，默认**每 30 秒**（可调，非交易时段自动降频）
   - **K 线蜡烛图**：日 K（A股/美股/港股/黄金ETF），叠加 **MA 均线** 与 **MACD** 技术指标（本地计算）；**v32 起默认视野按周期对齐自然日期**（日K近6个月/周K近1年半/月K近5年/年K近10年，所有标的一致，可拖动回看更早历史）

2. **持仓 / 交易记录**
   - 手动录入买卖：标的、市场、方向（买/卖）、数量、价格、日期、手续费
   - 自动计算持仓：成本、现价、持仓市值、浮动盈亏、收益率
   - 交易流水记录与查询

3. **组合分析**
   - 多资产组合总览：总资产、总收益、今日盈亏
   - 资产分配（按市场 / 按标的）
   - 绩效与回撤（基于记录的成本与实时市值）

4. **报表 / 可视化**
   - 资产分布饼图
   - 组合净值曲线（基于每日快照）
   - 收益报表，支持查看与导出

5. **财经资讯**
   - 近期可能影响金融行业的**国际 + 国内**大新闻（新浪财经滚动要闻）
   - 列表展示标题、摘要、时间，可点击跳转原文
   - 支持关键词筛选（如"美联储""A股""黄金"）

---

## 三、数据接口方案（已实测可用 ✅）

统一由**后端代理**抓取，规避浏览器跨域（CORS），并对 GBK 编码做转码。

| 市场 | 接口 | 说明 |
|---|---|---|
| A股/场内 | 腾讯 `qt.gtimg.cn` / 新浪 `hq.sinajs.cn` | 实时报价，需 GBK 转码 |
| 美股 | 新浪 `hq.sinajs.cn`（`gb_` 前缀） | 实时报价 |
| 港股 | 新浪 `hq.sinajs.cn`（`hk` 前缀） | 实时报价 |
| 黄金 | 新浪 `hf_GC`(纽约金) / `nf_AU0`(沪金连续) / `hf_XAU`(伦敦金) | 国内外金价（K线用黄金ETF `518880` 代理） |
| 公募基金 | 新浪 `hq.sinajs.cn`（`fu_` 前缀） | 基金净值（日更），如 `fu_161725` |
| 债券 | 腾讯 `qt.gtimg.cn`（可转债代码） | 如 `sh113050` 南银转债 |
| K 线 | 腾讯 `web.ifzq.gtimg.cn/appstock/app/fqkline/get` | A股/美股/港股/黄金ETF 日 K（OHLC） |
| 财经新闻 | 新浪 `feed.mix.sina.com.cn/api/roll/get` | 滚动要闻 JSON（含国际/国内财经） |

> 实测结论：本环境可正常取数，无需 API Key、无成本。
> K 线技术指标（MA 均线、MACD）由 K 线 OHLC 在本地计算，无外部依赖。
> ⚠️ 天天基金（fundgz / fundapi）在本环境被拦截（返回 404），故**公募基金改用新浪 `fu_` 接口**；基金为日净值（非盘中逐笔），刷新粒度按日即可。
> Yahoo 在本环境被拦，但未影响覆盖（新浪/腾讯已覆盖全部目标市场）。
> 部署到你本机 Linux 后，拿到的是真实当前行情。
> V1 **不做 K 线蜡烛图**（你选择了"实时看板 + 常规图表"），净值曲线由本地记录推算，不依赖外部历史接口。

---

## 四、技术架构（保证 Linux 可运行）

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | **FastAPI** (Python 3.11) | 行情代理、持仓计算、SQLite 读写；纯 Python，Linux 原生支持 |
| 前端 | **React + ECharts** | 仪表盘、图表；构建后由 FastAPI 静态托管 |
| 存储 | **SQLite** | 单文件本地库，零部署、免数据库服务 |
| 运行 | 本地 `localhost` | 单条命令启动，数据不出本机 |

**统一启动方式（目标）**：一条命令拉起前后端，浏览器打开 `localhost:8000` 即用。
后端用 uvicorn 提供 API + 托管前端静态资源；行情走后端代理。
**v33 页面性能优化**：后端进程级 TTL 缓存（实时行情 3s / 基金净值 5min，净值日频更新）+ 持仓聚合单飞缓存（持仓/交易写请求自动失效）+ 同一请求内行情批量拉取、持仓基金净值 8 线程并发获取——页面数据加载由数秒级降至亚秒级（冷缓存约 3-4s 预热、热缓存 <0.5s、30s 轮询约 1s），纯后端改动、前端零重建。

依赖均为跨平台标准包（fastapi / uvicorn / requests / react / echarts），在 Linux 上安装即用，无特殊系统库。

---

## 五、页面 / 模块草图

- **总览 Dashboard**：总资产、今日盈亏、净值曲线、资产分布饼图
- **行情看板**：自选列表 + 实时报价表 + 手动加自选（v32：国内金价条有实物黄金持仓时展示 持仓盈亏/累计盈亏/当日盈亏，正红负绿，**盈亏与持仓明细实物黄金同源（含手动编辑收益）；金价条更新时间旁显示交易状态小字、悬停看完整交易时段**；**v32：纽约金/沪金/伦敦金等黄金指数分时=本地记录当日实时价**——无外部逐分钟源，系统每分钟采样真实报价作分时数据源，卡片"记录中"、弹窗满2点自动显示当日分时）
- **持仓管理**：持仓卡片/表格 + 录入交易弹窗 + 盈亏实时计算（v32：持仓卡片两栏并列展示「当前持仓收益 | 累计收益」并文字区分，正红负绿；**v33：数据管理卡内置「一键清空」危险按钮**——两层确认（第一层提示 + 可选连自选/指数一起清、第二层输入关键字「确认清空」），清空全部持仓/交易/收益覆盖/历史快照，成功后自动重写根目录持仓 json；**v34：持仓卡片与持仓明细表格新增「单位成本」显示**——加权平均单位成本（= 持仓成本 ÷ 持有数量；基金 4 位小数、其余 2 位小数），表格中该列插在「持仓成本」与「现价」之间，实物黄金卡片显示 `¥x /克`，覆盖股票/基金/实物黄金全部持仓类型；**v34：新增「自动导出」——默认每天 06:00 自动把全部数据备份到本地 `data/auto_export/持仓数据_YYYY-MM-DD.json`，导出时间可在导出数据模块内配置（24 小时制 HH:MM），服务未运行时下次启动自动补导一次，默认保留最近 30 份**）
- **组合分析**：分配图、绩效指标、回撤（**净值曲线 v34：新增第三条「累计收益（含已实现）」折线，采用双 Y 轴——左轴 总市值/总成本、右轴 累计收益（两者量级差约 5 倍），线色黄色 `#d29922`，历史无数据段自动断点、不虚构填充**；收益分析日历图 v33：每个收益格子上居中显示当天收益数字，取整千分位、≥1万缩略「万」，悬停 tooltip 显精确值；**v34：日历每行改为「周一 → 周日」排列，且每个格子左上角以数字标注当天日期（月视图下整月每一天均标注，含尚无收益的空白格）**；**v34：收益明细「区间收益」与组合总收益的可编辑输入框——正数显示「+」号为红字、负数显示「-」号为绿字（修复此前正负配色被 CSS 覆盖而不生效的问题；符号与颜色同由正负判据驱动）**；**v34 口径修复：「总资产 / 总成本 / 当前持仓收益」三项改为全实时计算**——此前 `portfolio_summary()` 会把当日快照读回来覆盖这三项，导致总览**冻结在「当天首次打开时」的旧值**（行情走动、在持仓管理改数量/成本、手工覆盖收益都不反映，与持仓页长期对不上）；现已移除该回读覆盖，三项与持仓管理页同源并满足「总资产 − 总成本 = 当前持仓收益」自洽；**行情/净值取不到的持仓不计入这三项并在页面显示警示条，且此时不写当日净值快照、待数据恢复后自动补写**；基金官方净值取不到时的口径统一为「**数据暂不可用**」（市值置空、收益计 0），**绝不再把净值失败当成价格 0 而算成「亏光」**；**v34.2：净值曲线「当天那一点」改为随行情实时更新**——原先只在「当天首次打开时」写一次，导致同一张卡片里图上的「最新总市值」与上方「绩效指标 · 总市值」是两个不同的数（实测差 56 元，且越晚差越大）；现改为每次刷新都更新当天快照，**曲线最后一点、卡片「最新总市值」与顶部「总市值」三者恒等**，当日最后一次刷新即为该日最终值，**历史日期永不改写**）
- **报表**：收益报表、导出（CSV/打印）（持仓汇总表 v33：表头按 市值/成本/盈亏/收益率 点击升降序，默认市值降序；**v34 修复：① 合计行只统计行情可用持仓、盈亏改用 Σ 各持仓盈亏（含手工收益覆盖），与总览「当前持仓收益」一致；② 「导出 CSV」此前只重放交易记录、会漏掉全部「直接新增的初始持仓」（实测页面 25 条、导出 1 条），现改为与页面表格同源，列扩为 名称/市场/代码/数量/市值/成本/盈亏/收益率%，数值为原始数字**）

---

## 六、V1 范围与边界（明确不做）

- ❌ 不接券商、不真实下单
- ❌ 不做多用户、登录、权限
- ❌ 不部署到公网服务器（仅本机）
- ❌ 不依赖付费数据源 / API Key
- ❌ 不做分钟级高频刷新与量化回测引擎（V1 仅日 K + 实时报价轮询）

---

## 七、可行性结论

✅ 所有需求在免费公开接口 + 本地 Web 架构下**均可实现**，已实测数据通路可用、技术栈 Linux 可运行。
待你确认后，我将按上述方案开始编写代码，先交付可运行的 V1（行情看板 + 持仓记录 + 组合分析 + 报表）。

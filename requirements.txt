# Finance Dashboard - Python 3.7-compatible dependencies (minimal set)
# NOTE: Versions are pinned for Python 3.7 compatibility. Do NOT upgrade casually (newer versions require py>=3.8).
#
# Compatibility notes (all verified in practice):
#   1) fastapi 0.99.1 is the last stable release supporting py3.7; 0.100+ requires py>=3.8.
#   2) fastapi 0.99.1 dependency constraints:
#        - pydantic <2.0 (i.e. v1) -> use pydantic 1.10.14 (final v1, supports py3.7)
#        - starlette <0.28.0, >=0.27.0 -> NOT pinned manually; let pip resolve it (fastapi pulls the right one)
#   3) Therefore starlette is NOT pinned below, to avoid version conflicts.
fastapi==0.99.1          # Web framework (last py3.7-compatible stable release; auto-pulls matching starlette)
uvicorn==0.22.0          # ASGI server (py3.7-compatible)
pydantic==1.10.14        # Data validation v1 (final py3.7 release, matches fastapi 0.99.x <2.0 constraint)
requests==2.31.0         # HTTP client for quotes/news

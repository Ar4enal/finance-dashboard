# 金融工作台 — 任务对话与产物记录

> 文档用途：将本会话的完整任务对话与全部产物固化到项目内，方便在不同机器 / 不同时间回溯开发过程与交付内容。
> 生成日期：2026-08-24
> 项目根目录：`/workspace`（即"金融工作台"项目本身）
> 当前 Git 分支：`v1.0`（含 3 次提交，见文末"六、Git 与推送状态"）

---

## 一、项目概览

| 项目 | 说明 |
|---|---|
| **名称** | 金融工作台（finance-dashboard） |
| **定位** | 面向个人投资者的**投资/交易工作台 + 资产组合管理**，仅做记账和行情查看，**不接券商真实下单** |
| **目标用户** | 个人投资者（单用户、免登录） |
| **形态** | Web 网页应用，本地 `localhost` 运行，支持局域网其他终端访问 |
| **技术栈** | FastAPI 0.99.1 + React 18 + ECharts + SQLite（Python 3.7+ 兼容约束） |
| **市场覆盖** | A股/场内、美股、港股、黄金、公募基金、债券、指数 |
| **免费行情源** | 新浪 `hq.sinajs.cn`、腾讯 `qt.gtimg.cn`、天天基金 `fundf10.eastmoney.com` / `pingzhongdata` |

**核心目录结构**：
```
/workspace
├── app/                 # 后端（FastAPI）
│   ├── main.py          # 主应用 + 全部 API
│   ├── database.py      # SQLite 持久层（自选/交易/快照/黄金/收益覆盖/持仓覆盖 + 导出导入）
│   ├── datasource.py    # 行情数据源代理（新浪/腾讯/天天基金）
│   ├── indicators.py    # 技术指标（MA/MACD）
│   └── __init__.py
├── frontend/            # 前端源码（React 18 + ECharts）
│   └── src/
│       ├── api.js
│       ├── App.jsx
│       ├── pages/        # Dashboard/Quotes/Positions/News/Analysis/Reports
│       └── components/  # KlineChart/PenetrationView/ProfitDisplay/EChart
├── frontend_dist/       # 构建产物（FastAPI 直接托管，开箱即用）
├── data/                # SQLite 数据库（运行时生成，已 gitignore）
├── README.md            # 跨平台部署说明
├── 功能说明文档.md        # 功能清单 + 版本更新记录（每次功能更新同步维护）
├── 金融工作台_需求与设计.md
├── 金融工作台_详细设计.md
├── 金融工作台_v*.zip    # 各版本交付包（v3 ~ v14）
├── finance-dashboard-v1.0.bundle  # Git bundle 备份（用于有网环境推送）
├── 推送说明.md           # GitHub 推送说明（本地辅助文档，不进仓库）
├── 持仓数据_export.json  # 数据导出文件（含用户数据，运行时生成，不进仓库）
├── verify_*.py          # 各版本端到端验证脚本
├── verify_screens/      # 各版本 UI 截图
├── setup_env.sh / operation.sh        # Linux 部署/启停脚本
├── setup_env.bat / operation.bat      # Windows 部署/启停脚本
└── requirements.txt
```

---

## 二、任务对话记录（按时间顺序）

### ① QDII（T+2）自动判断 + 局域网访问

**用户需求**：
1. 非国内相关基金（QDII）按 T+2 规则，录入交易时需特殊判断；
2. 启动服务时探测并打印局域网 IP，允许其他终端用非 `localhost` 地址打开前端，并写入 README。

**处理与产物**：
- `datasource.py` 新增 `QDII_FUND_CODES`（初始 60 个），后续扩充到 **119 个**常见 QDII 代码，并提供 `is_qdii_fund()` 判断函数；前端录入/编辑交易弹窗增加"非国内基金（QDII，确认份额固定 T+2）"复选框（自动识别命中时禁用并标注"已自动识别"，并提供手动勾选兜底）。
- `operation.sh` 增加 `get_lan_ip()` 探测局域网 IP，在 `start` / `status` 时打印局域网访问地址；同步更新 README 局域网访问章节。
- 境内基金按 15:00 前 **T+1**、15:00 后 **T+2**；QDII 确认份额固定 **T+2**（不受 15:00 影响），自动计算并写入备注。

---

### ② Windows 部署脚本 + 功能说明文档

**用户需求**：
1. 允许在 Windows 与 Linux 上部署，新增 Windows 部署脚本并更新 README；
2. 维护一份 MD 文档记录所有功能，每次功能更新同步更新。

**处理与产物**：
- 新增 `setup_env.bat`（定位 Python、建 venv、装依赖、构建前端）与 `operation.bat`（启/停/重启/状态，PowerShell 探测局域网 IP）。
- `setup_env.sh` 修复幂等：不再无条件覆盖 `operation.sh`，仅在其缺失时生成。
- 新增 `功能说明文档.md`：功能清单 + 版本更新记录（v6–v12 起），约定"每次功能更新必须同步更新"。

---

### ③ 自选基金 K 线 + 低覆盖率穿透改进 + 录入 QDII 提示

**用户需求**：
1. 自选基金详情显示 K 线；
2. 重仓覆盖率低的基金需调整穿透计算方式，否则误差大；
3. 录入交易时未提示是否为非国内基金（T+2）。

**处理与产物**：
- `datasource.py` 新增 `fund_kline(fund_code)`：用天天基金 `pingzhongdata/{code}.js` 的 `Data_netWorthTrend` 净值历史构造等值 K 线（open=前日 close，high/low/close=当日净值，volume=0）。`main.py` 的 `get_kline()` 对基金市场改用此接口（避开沙箱受限的腾讯/新浪 K 线）。
- 场内基金穿透分档策略：覆盖率 ≥ 60% 用 `direct`（重仓加权）；< 60% 用 `calibrated`（基金近 20 日平均日涨跌填充未覆盖部分，`fund_recent_avg_return`）；无覆盖 `no_coverage`。`PenetrationView` 在 `calibrated` 模式显示校准提示。
- `Quotes.jsx` 基金详情弹窗增加"净值 K 线"区块（`<KlineChart market="FUND" .../>`）。
- 对照 ① 的 QDII 清单扩充到 119 个 + 手动勾选兜底，修复"录入未提示"问题。

---

### ④ 推送到 GitHub（v1 分支）

**用户需求**：推送到 `https://github.com/Ar4enal/finance-dashboard.git` 的 `v1` 分支，并写 commit，提供了 token。

**结果**：**沙箱网络受限，无法推送**（见 `六、Git 与推送状态`）。改为本地完成 `c438762` commit，打包 `finance-dashboard-v1.bundle`，生成 `推送说明.md`，供用户在能访问 GitHub 的环境执行。

---

### ⑤ 新增持仓功能

**用户需求**：持仓管理中增加"新增持仓"，允许用户直接录入初始持仓（输入产品代码、持有金额、持有收益），后端用当日价格自动计算。

**处理与产物**：
- `main.py` 新增 `POST /api/position/manual`，公共函数 `_manual_add_one()`：数量 = 持有金额 / 当日价格；成本 = 持有金额 − 持有收益，复用 `position_override` 机制。
- `frontend/src/api.js` 新增 `addManualPosition()`；`Positions.jsx` 新增持仓弹窗（页头"＋ 新增持仓"按钮，实时展示计算后的数量/成本/市值/盈亏）。
- 打包 `金融工作台_v12.zip`，更新 `功能说明文档.md` 与 README。

---

### ⑥ 新增持仓支持批量导入

**用户需求**：新增持仓支持批量导入。

**处理与产物**：
- 后端 `POST /api/position/manual/batch`：接收 JSON 数组 `[{market, code, amount, pnl}, ...]`，返回 `{total, success, failed, results}`，逐条含成功/失败及原因；抽取 `_manual_add_one` 供单条/批量复用。
- 前端 `api.js` 新增 `addManualPositionBatch()`；`Positions.jsx` 新增持仓弹窗内置「单条 / 批量」模式切换，批量模式整批指定市场后多行粘贴（每行 `代码 持有金额 [持有收益]`，空格/Tab/逗号分隔，收益可留空），实时显示解析条数，逐条展示导入结果。
- 浏览器端到端实测：粘贴 3 行 → 点击"批量导入（3 条）" → 正确返回成功 1 / 失败 2，逐条展示。
- 打包 `金融工作台_v13.zip`，更新文档。

---

### ⑦ 判断能否推送到 GitHub（v1.0 分支）

**用户需求**：先判断当前是否可推送到 GitHub；若可，推送到 `v1.0` 分支并写 commit（再次提供 token）。

**结果**：**确认仍无法推送**（github.com TLS 被拦截、SSH 22/443 均被切断）。本地完成的工作：
- 提交 `079f630  feat: 新增持仓支持批量导入（v13）`；
- 创建并切到 `v1.0` 分支（含 `079f630`、`c438762`）；
- 生成 `finance-dashboard-v1.0.bundle` 并 `git bundle verify` 通过；
- 更新 `推送说明.md`（v1.0 分支、两次 commit、bundle 文件名）。

---

### ⑧ 数据导入导出功能（跨机器迁移）

**用户需求**：网页新增数据导入导出，让用户能在不同机器快速导入自己的持仓状态。
- **导出**：把当前机器保存的所有信息保存到项目同路径下的本地文件供下载；
- **导入**：把导出文件放在同路径下即可导入数据。

**处理与产物**（任务 #51–#54）：
- 后端 3 个接口（均在 `main.py`）：
  - `GET /api/data/export`：汇总全部用户数据（自选 / 交易 / 净值快照 / 实物黄金 / 收益覆盖 / 持仓覆盖）为 JSON，写入项目根目录 `持仓数据_export.json`，并 `FileResponse` 触发浏览器下载；
  - `GET /api/data/export/info`：扫描项目根目录备份文件信息，供导入前预览；
  - `POST /api/data/import`：从项目根目录导出文件**整体恢复**数据库（先清空当前数据再写入，导入前二次确认），返回各类数据统计。
- `database.py` 新增 `export_all_data()` / `import_all_data()`。
- 前端 `Positions.jsx` 新增"数据导入 / 导出（跨机器迁移）"卡片：导出下载、导入一键恢复、扫描预览；`.gitignore` 已排除 `持仓数据_export.json`。
- 验证：导出生成根目录文件并下载；先新增测试数据（自选 7）→ 导入后恢复为自选 6，测试数据被正确清除；无文件时友好报错。
- 提交 `7a9077b`（v14），更新 `finance-dashboard-v1.0.bundle`、`功能说明文档.md`（新增第 10 节 + v14 记录）、README；打包 `金融工作台_v14.zip`（不含 `data/` 与导出文件，用户首次启动自动建空库）。

> 关于"项目同路径"：导出文件固定写入项目根目录 `/workspace/持仓数据_export.json`，用户拷贝到另一台机器的项目根目录后，前端"导入数据"按钮即可一键恢复——完全符合"放在同路径下即可导入"的需求。

---

### ⑨ 本次：把任务对话与所有产物复制到项目（当前任务）

**用户需求**：把本任务对话和所有产物全部复制到"金融工作台"项目中。

**处理**：经核实全机器搜索未发现另一个同名独立目录——`/workspace` 即"金融工作台"项目本体，源码、交付包、设计文档均已在其内。唯一缺失的是把聊天中的**任务对话**固化为项目文件。本文件（`任务对话与产物记录.md`）即为此产物，落于项目根目录，并配套"三、产物清单"与"六、Git 与推送状态"。

---

## 三、产物清单（全部位于项目内）

### 交付包（各版本，可在目标机器解压即用）

| 文件 | 大小 | 说明 |
|---|---|---|
| `金融工作台_v14.zip` | 510 KB | **最新交付包**：含数据导入导出 + 批量导入 + 新增持仓 + QDII/局域网/穿透等全部功能（不含 `data/` 与用户导出文件） |
| `金融工作台_v3`~`v13.zip` | 440–507 KB | 历史版本交付包（v3–v9、v10、v11、v12、v13），用于回溯 |
| `finance-dashboard-v1.0.bundle` | 1.1 MB | Git bundle 备份，含 `v1.0` 分支完整 3 次提交，可用于有网环境恢复并推送 |

### 文档

| 文件 | 说明 |
|---|---|
| `README.md` | 跨平台（Linux/Windows）部署、局域网访问、功能概述、常见问题 |
| `功能说明文档.md` | 功能清单 + 版本更新记录（v6–v14），每次功能更新同步维护 |
| `金融工作台_需求与设计.md` | 需求与设计总览 |
| `金融工作台_详细设计.md` | 详细设计文档 |
| `推送说明.md` | GitHub 推送说明（含 token 用法、源码推送 / bundle 恢复两种方式） |
| `任务对话与产物记录.md` | **本文件**：任务对话 + 产物清单 + Git/推送状态 |
| `持仓数据_export.json` | 数据导出文件（当前全部用户数据快照，含用户数据，不进仓库） |

### 源码与运行产物

| 路径 | 说明 |
|---|---|
| `app/` | 后端（FastAPI）：`main.py` / `database.py` / `datasource.py` / `indicators.py` |
| `frontend/` + `frontend_dist/` | 前端源码与构建产物（FastAPI 直接托管） |
| `data/` | SQLite 数据库（运行时生成，`gitignore`，承载用户自选/交易数据） |
| `setup_env.sh` / `operation.sh` | Linux 部署 / 启停脚本（含局域网 IP 探测） |
| `setup_env.bat` / `operation.bat` | Windows 部署 / 启停脚本 |
| `requirements.txt` | Python 依赖 |

### 验证脚本与截图（均在 `.gitignore`，不进仓库）

| 路径 | 说明 |
|---|---|
| `verify_*.py` | v9 / v11 / v12 / 端到端 验证脚本 |
| `verify_screens/` | v9–v12 各版本 UI 截图（positions / qdii / addpos / kline / penetration / news 等共 25 张） |

---

## 四、功能演进时间线

| 版本 | 核心功能 |
|---|---|
| v1 | 金融工作台基础：投资记账 + 行情看板（FastAPI + React + ECharts + SQLite） |
| v3–v9 | 行情看板、持仓管理、穿透计算、QDII 判断、资讯、分析、报表等迭代 |
| v10–v12 | 新增持仓（单条）、基金 K 线、低覆盖率穿透校准、QDII 录入提示 |
| **v13** | 新增持仓**批量导入** |
| **v14** | **数据导入导出**（跨机器迁移持仓状态） |

---

## 五、关键设计决策与约束

- **Python 3.7 兼容**：FastAPI 0.99.1 / uvicorn 0.22.0 / pydantic 1.10.14（v1）/ starlette 0.27.0。
- **免费公开行情**：新浪/腾讯实时行情、天天基金净值与重仓（`pingzhongdata` 在沙箱内可用；腾讯/新浪 K 线在沙箱受限，故基金 K 线改用天天基金）。
- **QDII 识别**：内置 119 个常见 QDII 代码 + 前端手动勾选兜底；QDII 份额固定 T+2。
- **场内基金穿透**：覆盖率高用重仓加权（direct），低覆盖率用近 20 日动量校准（calibrated）。
- **新增/导入持仓**：复用 `position_override` 机制，数量 = 金额/价格，成本 = 金额 − 收益。
- **数据迁移**：导出/导入用固定文件名 `持仓数据_export.json` 置于项目根目录，满足"同路径下即可导入"。
- **安全**：token 不写入任何提交；`.gitignore` 排除数据库、`data/`、zip 包、bundle、导出文件、推送说明、验证产物。

---

## 六、Git 与推送状态

**当前分支 `v1.0`**（共 3 次提交）：
```
7a9077b  feat: 数据导入导出支持跨机器迁移持仓状态（v14）
079f630  feat: 新增持仓支持批量导入（v13）
c438762  feat: 金融工作台 v1 - 个人投资者投资记账与行情工具
```

**GitHub 推送结论**：当前沙箱环境**无法推送**。已全面探测：

| 通道 | 结果 |
|---|---|
| `github.com:443`（HTTPS + token） | ❌ TLS 握手被拦截（`gnutls_handshake() failed`） |
| `api.github.com`（HTTPS） | ❌ 同样被拦截 |
| `github.com:22`（SSH） | ❌ TCP 不通 |
| `ssh.github.com:443`（SSH over TLS） | ❌ 握手被切断（`Connection closed`） |

**在有网环境执行推送**（详见 `推送说明.md`）：

方式一（源码推送）：
```bash
git init && git branch -m v1.0
git remote add origin https://github.com/Ar4enal/finance-dashboard.git
git push -u origin v1.0
```

方式二（bundle 恢复）：
```bash
git clone finance-dashboard-v1.0.bundle finance-dashboard
cd finance-dashboard
git remote remove origin
git remote add origin https://github.com/Ar4enal/finance-dashboard.git
git push -u origin v1.0
```

> 注：以上命令中的 token 认证请在 `推送说明.md` 内按文档拼接远程地址；本记录不重复写入明文 token。

---

## 七、如何运行 / 验证

```bash
# Linux
bash setup_env.sh     # 首次：建环境 + 构建前端
bash operation.sh start   # 启动（自动打印局域网访问地址）

# Windows
setup_env.bat
operation.bat

# 浏览器打开
http://localhost:8000        # 本机
http://<局域网IP>:8000       # 同局域网其他终端
```

最新功能入口：持仓管理页 →「数据导入 / 导出（跨机器迁移）」卡片（导出下载备份、导入一键恢复）、「＋ 新增持仓」弹窗（单条 / 批量导入）。

---

## 九、v19 改动记录（2026-08-25）

对应项目计划中 5 个进行中任务（rtz48h / rpwvqw / rUo1vv / r3OVpV / rV8Dxz）：

1. **持仓管理页布局调整（rtz48h）**：实物黄金模块移至页面最下方，数据导入/导出模块移至实物黄金下方（之前均在页面顶部）；实物黄金买卖流水表每页最多 5 条 + 翻页（‹ 上一页 / 下一页 › + 共 N 条 · 第 x / y 页），录入/删除后回到第 1 页。
2. **持仓管理功能增强（rpwvqw）**：① 默认按持仓收益降序排序（之前按市值），保留按市值 / 成本切换（三按钮）。② 置顶产品（数量不限）后端持久化（新表 `pinned_positions`）并随数据导入导出跨机器迁移；置顶产品永远在列表最前（行内 📌 + 表格上提示），一键切换。③ 持仓明细（含顶部持仓卡片概览）每页最多 20 条 + 翻页。
3. **指数自定义 + A股板块资金流向（rUo1vv）**：指数配置后端持久化（`custom_indices` 表），可点击「＋ 新增指数」添加（市场 + 代码 + 名称，名称留空则自动识别），每个指数卡片 ✕ 一键删除；首次启动自动填充 9 个内置预置（上证/深证/创业板/科创50/沪深300/纳斯达克/标普500/道琼斯/费城半导体）。新增「💰 A股板块资金流向」面板：当天前十流入 + 前十流出行业板块（主力净流入 / 涨跌幅 / 占成交比），并排双表，标注更新时间；数据源东方财富 `push2delay.eastmoney.com`（`fid=f62`），失败明确提示「数据暂不可用」，绝不填充虚拟数据。
4. **场内基金穿透位置 + 新增持仓同步自选（r3OVpV）**：场内基金穿透模块移至「指数」与「自选基金」模块之间（之前在自选基金之后）；新增持仓（录入交易 / 直接新增初始持仓）时自动加入自选标的（实物黄金除外——黄金有独立模块），幂等 `INSERT OR IGNORE`，后端 `add_txn` 与 `_manual_add_one` 成功后调用。
5. **前端代表性页面截图（rV8Dxz）**：用 Playwright（headless Chromium，1440×900 @2x）为 5 个代表性页面（总览 / 行情看板 / 持仓管理 / 组合分析 / 财经资讯）截图，保存至 `screenshots/`，README 新增「界面预览」章节链接展示，便于 GitHub 直接看到前端效果。
6. **后端新增表 / 接口**：数据库新增 `pinned_positions`（持仓置顶）与 `custom_indices`（指数配置）两张表，启动时 `init_default_indices()` 填充 9 个默认指数；数据导入/导出同步携带（跨机器迁移不丢失）。新接口：`GET/POST/DELETE /api/indices`、`GET /api/position/pins`、`POST /api/position/pin`、`DELETE /api/position/pin/{market}/{code}`、`GET /api/quotes/sector-flow`。前端 `api.js` 与 `Pages/{Quotes,Positions}.jsx` 同步适配。`Positions.jsx` 增 5 个 state（posSort/pins/posPage/goldPage + togglePin）、`Quotes.jsx` 增 indices/sectorFlow/showAddIndex 状态及板块资金流格式化函数（亿/万）、`index.css` 加 `.idx-del` 删除按钮样式。

构建产物：`frontend_dist/assets/index-CtE-9Ug9.js`（1247.67 kB）+ `index-bzP9Xtjl.css`（12.73 kB），旧 `index-CD5AA75F.js` / `index-faOVXw28.css` 已清理。

## 八、v18 改动记录（2026-08-25）

对应项目计划中 4 个进行中任务（rZiwsf / r8U1tb / rHOJwA / rf73SI）：

1. **基金穿透修复（rZiwsf）**：`fund_holdings` 改为按 td 位置通用解析，兼容 A股 / 美股（QDII/跨境 ETF 重仓）/ 港股持仓行；重仓股行情按代码自动映射新浪 symbol（`sh/sz` / `hk` / `gb_`）；穿透失败时补基金名称。列表卡片始终显示「名称 + 代码」。
2. **K线颜色统一（r8U1tb）**：K线 / 成交量 / MACD 统一红涨绿跌（`#f85149` 红 / `#3fb950` 绿），与全站 `--up`/`--down` 一致。
3. **卖出收益修复（rHOJwA）**：`compute_positions` 按 (成交日期, id) 升序重放（修复卖出早于买入导致持仓不减）；`_gold_position` 改用 `compute_gold_realized_pnl()` 计算黄金已实现收益，累计收益 = 已实现 + 实时持仓收益。
4. **脚本改名（rf73SI）**：`start.bat` → `operation.bat`（与 Linux `operation.sh` 对齐），`setup_env.bat` 与全部文档同步。

## 十、v20 改动记录（2026-08-25）

对应项目计划中 6 个进行中任务（reQmeB / r8bQuM / rhuyTf / rH4zGj / rdYvn7 / rkxNVl）：

1. **卖光产品保留持仓行（reQmeB）**：根因 `database.compute_positions()` 用 `if p["qty"]<=1e-9: continue` 过滤售罄产品，导致 `_positions_data()` 拿不到该行、累计收益无法展示。**方案**：`database.py` 新增 `compute_positions_including_soldout()` 保留售罄 key，输出 `sold_out=True` 标记，`quantity/avg_cost/cost=None`；`main.py` `_positions_data()` 改用该函数，售罄条目跳过行情拉取，`cumPnl=realized[(market,code)]`（全部已实现盈亏），由 `portfolio_summary()` 累加进整体 `totalCumPnl`；`_apply_profit_overrides` 兼容 `cost=None`（售罄时 `holdingPnlPct=0` 避免除零）；`portfolio_summary` 累加用 `p.get(x) or 0` 修复售罄 None 导致的 500。**前端**：`Positions.jsx` 持仓卡片新增「已清仓」徽章 + 数量/成本/市值显示「—」+ 累计收益行；表格行同样加徽章并**屏蔽售罄行的「编辑」按钮**（保留「置顶」「删除」）；`Dashboard.jsx` 持仓概览兼容售罄行展示「已清仓 · 累计收益 X」。
2. **输入框历史记忆（r8bQuM）**：新增 `frontend/src/history.js`（localStorage 命名空间 `fwt:hist:`，每字段最近 5 个去重值），导出 `useInputHistory/clearHistory/loadHistory`；`Positions.jsx` 定义 `HistInput` 组件（带 ✕ 删除单项）并包装「新增持仓代码/金额/收益」「交易/编辑交易代码/数量/价格/手续费」「实物黄金克数/成本价」「黄金交易克数/价格/日期/备注」等所有可填输入框；`Quotes.jsx` 同样定义 `HistInput` 包装「指数代码/名称」输入框。**前端实现，不动后端**。
3. **底部数据来源展示（rhuyTf）**：`App.jsx` footer 在开源地址下方新增 `<div className="src-line">` 一行小字「数据来源：新浪财经行情 · 腾讯行情 · 天天基金 · 东方财富板块资金流 · 新浪财经资讯（实时数据，获取失败均提示「数据暂不可用」，绝不填充虚拟数据）」；`index.css` 加 `.app-footer .src-line` 样式（muted 小字，line-height 1.5）。
4. **饼图点击显示持仓明细（rH4zGj）**：`EChart.jsx` 增强支持 `onEvents` prop（`chart.on/off` 绑定，option 变化时重绑，事件 ref 持有最新回调避免闭包陈旧）；`main.py` `portfolio_allocation` 扩展返回 `items: [{market,code,name,market_value,pct}]`（pct=该产品市值/分组市值=占所有持仓百分比），售罄产品 `market_value<=0` 自动排除；`Analysis.jsx` 饼图传 `onEvents={{click: onPieClick}}` 触发弹窗 `alloc-modal` 展示代码/名称/市值/占该类型百分比；`index.css` 加 `.alloc-modal-mask` `.alloc-modal` 暗色弹窗样式。
5. **新闻市场/行业标签（rdYvn7）**：`main.py` `news()` 路由对每条 `ds.sina_news` 项调用新增 `_classify_news(title, summary)`，**仅附加标签不修改原文**；规则词典：国际关键词（美/美股/美联储/欧洲/欧股/日经/港股/全球/关税/非农/英伟达/特斯拉/谷歌/微软/华尔街/欧洲央行 等 24 个）、行业关键词（芯片/半导体/新能源/光伏/医药/银行/地产/券商/白酒/消费/能源/黄金/科技/军工 13 类）；未命中默认「国内·综合」（非虚拟数据）。`News.jsx` 标题旁渲染 `<span className="news-tag">{market}·{sector}</span>`；`index.css` 加 `.news-tag` 暗色小字标签样式。
6. **基金列表对齐修复（rkxNVl）**：根因 `.fund-table th`(index.css:166) `text-align:left` 特异性 (0,1,1) 覆盖 `.num{text-align:right}`(0,1,0) → 表头左对齐；`td.num` 未被覆盖 → 右对齐。**方案**：`index.css` 新增 `.fund-table th.num, .fund-table td.num { text-align: right; }`（特异性 0,2,1 生效），统一表头与数据右对齐，最小改动。

构建产物：`frontend_dist/assets/index-BzO96wyj.js`（1252.37 kB）+ `index-hXyws66x.css`（14.96 kB），旧 `index-CD5AA75F.js` / `index-bzP9Xtjl.css` / `index-faOVXw28.css` 已清理。

## 十一、v21 改动记录（2026-08-26）

对应 workbuddy 项目计划中 6 个进行中任务（task #1–#6），基于 v20 压缩包（`release/金融工作台_v20.zip`）在 `/workspace/金融工作台` 重建基线开发：

1. **持仓明细排序按钮"无反应"（task #1，fix·低）**：根因并非事件绑定——行情不可用时 `_positions_data` 把 `market_value` 回退为 `cost`、`holdingPnl` 置 0，「按市值」≡「按持仓成本」、收益全 0，三键结果坍缩成同一顺序，列表看起来没变。**修复**：`Positions.jsx` 排序 comparator 增加次级稳定键 `(market, code)`；**顺带修复置顶方向 bug**（原 `pinnedSet[b]-pinnedSet[a]` 写反导致置顶产品反而在末尾）；排序按钮补 `type="button"`。
2. **默认指数增加纽约黄金期货（task #2，fix·低）**：`DEFAULT_INDICES` 新增 `("纽约黄金期货","GOLD","hf_GC")`，新浪真实报价（实测 4717.49 美元，+0.49%）。
3. **实物黄金改为持仓明细编辑（task #3，fix·高）**：删除「保存实物黄金」按钮与输入框（保留「清空」）；持仓明细黄金行开放编辑；`save_position_override` 兼容 `market=GOLD`（数量→克数、总成本→cost/grams 写 `gold_holding`）；`_positions_data` 跳过 `(GOLD,GOLD_PHYSICAL)` 覆盖避免重复行。**顺带修复 v20 隐藏 bug**：`compute_gold_realized_pnl` 复用 `_rebuild_gold_from_txns` 会在每次拉取持仓时把 gold_holding 按流水重放清零，拆出纯计算函数 `_calc_gold_realized_pnl`（不写库），`_rebuild` 仅流水增删时调用——实测：编辑 80 克/600 元保存后刷新不再被重置为 0。
4. **指数卡片可移动 + 名称自动识别（task #4，feat·中）**：新接口 `POST /api/indices/{idx_id}/move?dir=up|down` + `db.swap_index_sort_order`（相邻行交换 sort_order）；前端卡片 ▲/▼ 按钮（首尾禁用）；`addIndex` 不再用代码兜底名称，留空交给后端自动识别（实测 `sh601318` → 「中国平安」）。
5. **非交易时段回退最近价格（task #5，feat·高）**：`datasource._last_close(market, code)`（基金→天天基金净值历史；美股指数→`usfqkline`；其他→腾讯 K 线，501 重试 3 次，A 股新浪 K 线兜底）；`_manual_add_one` 无当日价时回退，返回 `price_source`（`today`/`last_close`）。实测模拟当日无报价：A 股回退 1304.0、基金 0.5505、纳指 26151.3。
6. **场外基金净值 4 位小数（task #6，fix·低）**：基金（`market=FUND`）价格统一 4 位显示（`Quotes` 自选基金表/详情弹窗、`Positions` 卡片/明细/新增结果、`Dashboard` 总览），后端本就保留原始精度。

**端到端验证（Playwright + 真实接口）**：9/9 通过——纽约黄金期货卡片、▲▼ 按钮（20 个）、名称自动识别、排序按钮高亮切换、实物黄金编辑生效（80 克/600 元）、无「保存」按钮有「清空」、指数 ▲ 移动顺序变化，页面无 JS 错误。

构建产物：`frontend_dist/assets/index-Do2TGEw7.js`（1252.60 kB）+ `index-Ckd0pVwt.css`（15.26 kB），旧 `index-BzO96wyj.js` / `index-hXyws66x.css` 已清理。交付包：`release/金融工作台_v21.zip`（不含 `data/`、`.venv/`、`node_modules/`、`持仓数据_export.json`）。

## 十二、v22 改动记录（2026-08-26）

本轮基于 v21 压缩包（`release/金融工作台_v21.zip`）在 `/workspace/金融工作台` 重建基线开发，按需求逐项实现（每完成一项同步本文档与功能说明文档）。

### 需求 1（fix·高）：新增指数卡片 K 线只有当天、无历史
- **根因**：`/api/kline` 对美股标的先走个股 `fqkline` 路径（`us<CODE>.OQ`），腾讯 `fqkline` 对**指数**只返回 1 天（对个股/ETF 才返回完整历史）；内置美股指数靠写死的 `US_INDEX` 白名单（ixic/inx/dji/sox/ndx/rut/vix）才能绕开，用户自定义新增的指数进不了白名单，于是落入个股分支只拿到当天数据，K线/成交量/MACD 三个面板都只有 1 个点。
- **方案**：修改 `app/main.py` `get_kline()` 美股分支——先走个股 `fqkline us<CODE>.OQ`，若拿到 ≥2 根 K 线视为个股/ETF 直接使用；否则当作指数改用指数专用 `usfqkline`（`us<CODE>`）取完整历史。不再依赖写死白名单，内置与自定义美股/海外指数均显示完整历史；腾讯确实无数据的指数（如费城半导体 sox）仍诚实返回「该指数历史K线数据暂不可用」（不造假）。纯后端逻辑修复，前端 `frontend_dist` 无需重建。
- **实测验证（真实接口，启动后端直连 `/api/kline`）**：
  - 自定义美股指数 `ixic` → 120 天（2026-03-05 ~ 2026-08-25）✅（修复前仅 1 天）
  - 内置 `inx` / `dji` → 120 天 ✅
  - 无数据指数 `sox` → 诚实提示「该指数历史K线数据暂不可用」✅（不造假）
  - 美股个股 `AAPL` → 完整历史（走个股路径）✅

### 需求 2（fix·高）：新增国内指数 K 线只有当天、无历史（如北证50）
- **根因**：`/api/kline` 的国内（`else`）分支以腾讯 `fqkline` 为主源，当腾讯对部分代码（北交所 `bj` 前缀如 `bj899050`、个别指数）返回 1 天空壳或 WAF 拦截（非异常）时，代码仅在 `DataSourceError` 时回退新浪、1 天数据被直接使用；此外若代码格式偏差（如只填纯数字 `899050`），新浪 `sina_kline` 返回 `null` 会抛 `TypeError` 冒泡致接口 500。
- **方案**：修改 `app/main.py` `get_kline()` 的 `else` 分支，与美股分支对称——腾讯 `tencent_kline` 拿到 ≥2 根 K 线直接使用；否则回退新浪 `sina_kline` 取完整历史；新浪解析异常（`null`/空）也当无数据处理（捕获 `Exception`），不再抛 `TypeError`。纯后端逻辑修复，前端 `frontend_dist` 无需重建。
- **实测验证（真实接口，启动后端直连 `/api/kline`）**：
  - 新增北证50 `bj899050` → 120 天（2026-03-04 ~ 2026-08-25）✅（修复前仅 1 天 / 腾讯 WAF 拦截）
  - 内置上证 `sh000001` / 创业板 `sz399006` / 科创50 `sh000688` / 沪深300 `sh000300` → 120 天（腾讯源，无回归）✅
  - 错误代码 `899050`（新浪 null）→ 诚实返回「K线数据为空」，不再 TypeError 崩溃 ✅
  - 港股 `00700`（腾讯限流 + 新浪无港股 K线）→ 诚实返回「K线数据为空」✅（不造假）
- **文档同步**：`功能说明文档.md` 新增 v22 版本记录 + 更新「美股指数 K 线」功能行；`金融工作台_详细设计.md` 修正美股指数 K 线机制说明（v22 起自动切换 `usfqkline`）。

### 需求 3（fix·中）：清仓产品在明细与总览只显示代码、不显示名称
- **根因**：`_positions_data()` 对 `sold_out`（已清仓）行直接 `p["name"] = p["code"]`——原注释「不拉行情」误把名称也置成了代码；前端 `Positions.jsx`（明细）与 `Dashboard.jsx`（总览）均用 `p.name || p.code` 渲染，于是清仓行只显示代码、无产品名。
- **方案**：修改 `app/main.py` `_positions_data()` 的 `sold_out` 分支——清仓行仍不拉行情价格（数量/成本/市值保持置空 `None`），但改为**尝试从行情取真实名称**：`to_sina_symbol` + `sina_quotes` 取 `name`，`DataSourceError` 或取不到时回退代码，与正常持仓的 name 行为一致。纯后端逻辑修复，前端 `frontend_dist` 无需重建。
- **实测验证（构造清仓场景临时库 + 启动后端直连 `/api/positions`）**：
  - 清仓行 `A/sh600519`（买100全卖）→ `name='贵州茅台'`、`sold_out=True`、`price=None`、`market_value=None` ✅（修复前 `name='sh600519'`）
  - 正常持仓 `A/sz000001` → `name='平安银行'`、数量/市值正常 ✅（无回归）
  - 名称源验证：`sh600519`→贵州茅台、`sz000001`→平安银行、`US/AAPL`(`gb_aapl`)→苹果、`HK/00700`(`hk00700`)→腾讯控股，均真实可取 ✅

### 需求 4（fix·高）：持仓明细排序"无反应" + 置顶方向反了（跑到尾部）
- **根因**：
  1. **置顶方向反了（实锤）**：`Positions.jsx` `sortedPositions` 置顶二次排序 `list.sort((a,b) => Number(pinnedA) - Number(pinnedB))`。JS `Array.sort` 比较函数返回**正数时 a 排后面**，故 `pinned(1)-unpinned(0)=1>0` 让被置顶的 a 沉到尾部——与 v21 注释「已修复为 a-b」相反（v21 那个修复方向实际写反）。用 Node 单测复现：`a-b` 结果 `[平安银行, 贵州茅台, 中国平安, 苹果]`（置顶项在尾），`b-a` 结果 `[中国平安, 苹果, 平安银行, 贵州茅台]`（置顶项在最前）。
  2. **排序"没反应"**：根因在后端 `_positions_data()`——行情不可用（`DataSourceError`）时把 `market_value` 回退成 `round(cost)`、`holdingPnl` 置 0。这使「按市值」与「按成本」对不可用持仓完全混同、「按收益」全是 0，三种排序大量坍缩、列表肉眼"不变"。
- **方案**：
  1. 前端 `Positions.jsx` 置顶二次排序改为 `Number(pinnedB) - Number(pinnedA)`（置顶项比较函数返回负数 → 排最前）。
  2. 后端 `main.py` `_positions_data()` 行情不可用分支：`market_value`/`price` 保持 `None`（不伪装成 cost），`holdingPnl` 置 0、`data_available=False`；组合汇总 `portfolio_summary` / `allocation` 已用 `or 0` 容错（None→0），不计入实时市值，符合"行情未知"语义。
  3. 已 `vite build` 重建 `frontend_dist/assets/index-HC1fv1ss.js`（CSS 不变 `index-Ckd0pVwt.css`，旧 `index-Do2TGEw7.js` 清理）。
- **实测验证（Playwright + Chromium 真实浏览器，构造含正常+行情不可用持仓的库）**：
  - 置顶第一行 → 该行排到**最前**（`置顶项在最前?: True`）✅（修复前沉到尾部）
  - 三种排序顺序明显不同，列表会变化：pnl=`[上证指数, 苹果, zz999999, 贵州茅台]` / mv=`[上证指数, 贵州茅台, 苹果, zz999999]` / cost=`[上证指数, 贵州茅台, zz999999, 苹果]` ✅（修复前行情不可用时三者坍缩）
  - 行情不可用持仓 `zz999999` 不再与"按成本"混同、排在末尾 ✅

### 需求 5（fix·中）：总览持仓概览卡片按市值由多到少排序
- **根因**：`Dashboard.jsx`「持仓概览」卡片直接 `positions.map(...)` 按后端返回顺序渲染，后端 `_positions_data()` 返回的是 `compute_positions_including_soldout` 的 key 顺序（交易重放后字典序），未按市值排序。
- **方案**：渲染前对 `positions` 派生副本按 `market_value` **降序**排序（`null` 的清仓/行情不可用行排最后，次级键按名称稳定）。改 `frontend/src/pages/Dashboard.jsx`，已重新 `vite build` → `frontend_dist/assets/index-Do1lrktw.js`（CSS 不变 `index-Ckd0pVwt.css`）。不影响「持仓盈亏分布」柱状图（仍按原 `positions` 顺序）。
- **实测验证（Playwright + Chromium 真实浏览器，构造 4 只不同市值持仓）**：总览持仓概览卡片顺序 `['上证指数', '贵州茅台', '苹果', '平安银行']`，与后端市值降序 `19582462 > 130331 > 3099 > 1173` 完全一致 ✅。

### 需求 6（feat·高）：任意界面点击名称/卡片均可查看 K 线行情
- **原状**：K 线弹窗是 `Quotes.jsx` 的局部 state（仅行情看板的指数卡片/自选标的按钮能用），总览/持仓管理/组合分析/报表的名称与卡片均无法点开 K 线。
- **方案**：新增全局 K 线组件 `frontend/src/kline.jsx`——`KlineProvider`（管理弹窗 state，渲染全局模态框复用 `KlineChart`）+ `useKline()` 暴露 `openKline(market, code, name)`；`App.jsx` 用 `KlineProvider` 包裹。各页面 `import { useKline }` 后给名称/卡片加 `onClick`：
  - `Dashboard.jsx`：持仓概览 `.pos-card` 加 `onClick` + `cursor:pointer`；
  - `Positions.jsx`：持仓卡片整个卡片 + 表格行名称 `td` 加 `onClick`；
  - `Analysis.jsx`：饼图明细弹窗的 `it` 行加 `onClick`（`it` 含 `market/code/name`）；
  - `Reports.jsx`：报表表格行加 `onClick`；
  - `Quotes.jsx`：去掉局部 `kline` state 与重复弹窗 JSX，改用全局 `openKline`（基金详情弹窗内的净值 K 线 `KlineChart market="FUND"` 保留）；
  - `News.jsx`：资讯无具体标的 `market/code`，不接。
  - 已重新 `vite build` → `frontend_dist/assets/index-BxF17-dM.js`（CSS 不变 `index-Ckd0pVwt.css`）。
- **实测验证（Playwright + Chromium 真实浏览器）**：
  - 总览点持仓卡片「贵州茅台」→ 弹窗 `贵州茅台 (sh600519) · 日K · MA · MACD` ✅
  - 持仓管理点表格名称「贵州茅台」→ 弹窗同上 ✅
  - 报表点表格行「苹果」→ 弹窗 `苹果 (AAPL) · 日K · MA · MACD` ✅
  - 组合分析点饼图→明细弹窗→点明细行「贵州茅台」→ 弹窗同上 ✅
  - 全局弹窗统一，关闭后可继续在任意页点击 ✅

## 十三、v23 改动记录（2026-08-27）

本轮在 v22 工作副本（`/workspace/金融工作台`）基础上继续迭代，按需求逐项实现（每完成一项同步本文档与功能说明文档）。

### 需求 1（fix·中）：自选基金与标的按日涨跌幅降序排序 + 行情看板暂不展示场内基金穿透模块
- **用户原话**：「【fix】自选基金和标的按日涨跌幅的顺序排序显示。行情看板里单独的场内基金穿透模块先不展示。」
- **根因**：
  1. 行情看板（`Quotes.jsx`）「自选基金」（`funds`）与「自选标的」（`watchlist`）两列表此前直接 `funds.map(...)` / `watchlist.map(...)` 按后端返回原始顺序渲染，无任何涨跌幅排序。
  2. 行情看板「指数」与「自选基金」之间存在一个独立的「🔍 场内基金穿透」模块（`penetrations` 渲染），用户要求 v23 起暂不展示。
- **方案**：
  1. `Quotes.jsx` 新增工具函数 `sortByPctDesc(arr)`：按 `pct`（日涨跌幅；基金与标的统一字段）**降序**（`return pb - pa`）排序，`pct` 缺失（行情不可用）的条目用 `Number.NEGATIVE_INFINITY` 沉底；在 `fetchWatchlist` 拿到行情后 `setFunds(sortByPctDesc(enrichedFunds))` / `setWatchlist(sortByPctDesc(enrichedWatch))`。
  2. `Quotes.jsx` 顶部新增常量 `SHOW_PENETRATION = false`，原「场内基金穿透」模块用 `{SHOW_PENETRATION && (...)}` 包裹（代码全保留，后续恢复置 `true` 即可）。`fetchPenetrations` 仍调用（不影响其他逻辑）。
  3. 已 `vite build` 重建 `frontend_dist/assets/index-D3x52Q4Z.js`（CSS 不变 `index-Ckd0pVwt.css`，旧 `index-BxF17-dM.js` 因 `emptyOutDir` 自动清理）。
- **实测验证（Playwright + 系统 Chromium 真实浏览器，临时灌入 5 条真实自选后删除）**：
  - 自选标的表按涨跌幅降序：`苹果 +1.15% → 腾讯控股 +0.67% → 贵州茅台 -0.63% → 平安银行 -1.02%`（序列 `[1.15, 0.67, -0.63, -1.02]`，降序 ✅）
  - 自选基金表（1 只）：`招商中证白酒指数A -0.57%`，与标的表同源 `sortByPctDesc` 逻辑一致 ✅
  - 场内基金穿透模块：页面不含「场内基金穿透」文案、标题节点不存在（`False / False`）✅
  - 浏览器控制台无错误 ✅
  - 验证后已 `DELETE /api/watchlist/{id}` 清空临时数据，数据库恢复原状（剩余 0 条）✅
- **文档同步**：`功能说明文档.md` 新增 v23 版本记录 + 行情看板章节补充排序/穿透模块说明；`金融工作台_详细设计.md` §5.2 补充 v23 排序与展示规则。

### 需求 1 补充（fix·低）：自选基金/标的「日涨跌幅」表头点击切换升/降序
- **用户原话**：「关于上一个需求'按日涨跌幅的顺序排序显示'，未指定升/降。补充一下，做成'点表头可切换升/降'」
- **背景**：需求 1 初版按"降序"固定实现，用户希望改为点击表头在升/降之间切换。
- **方案**：
  1. `Quotes.jsx` 排序函数由固定降序的 `sortByPctDesc(arr)` 改为支持方向的 `sortByPct(arr, dir='desc')`（`dir==='asc' ? pa-pb : pb-pa`）；行情不可用（`pct` 缺失）仍按 `-Infinity` 沉底。
  2. 组件新增 `fundDir` / `watchDir` 状态（默认 `'desc'`）；`fetchWatchlist` 改为只存**未排序**的 enrichment 数据（`setFunds(enrichedFunds)` / `setWatchlist(enrichedWatch)`），渲染前 `const sortedFunds = sortByPct(funds, fundDir)` / `const sortedWatch = sortByPct(watchlist, watchDir)` 实时排序——**切换方向无需重新请求行情**。
  3. 「自选基金」表头「日涨跌幅」与「自选标的」表头「涨跌幅」改为 `th.sortable`，`onClick={() => setFundDir(d => d==='asc'?'desc':'asc')}`（两表方向独立），并带 `↓`/`↑` 方向指示（`.sort-arr`）。
  4. `index.css` 新增 `.sortable`（指针 + hover 高亮）、`.sort-arr`（accent 箭头）样式。
  5. 已 `vite build` → `frontend_dist/assets/index-Sg8vXs71.js` + `index-BZ-7z_9F.css`（旧 `index-D3x52Q4Z.js` 因 `emptyOutDir` 自动清理）。
- **实测验证（Playwright + 系统 Chromium，拦截 `/api/watchlist` `/api/positions` `/api/quotes/batch` 等返回 6 条测试持仓，注入可控 `pct`，**仅验证组件交互、不写真实库**）**——11/11 全过**：
  - 两个表头均为 `.sortable` 可点击 ✅
  - 初始箭头 `↓`（降序）✅
  - 自选基金默认降序 `[招商中证白酒(0.5), 易方达蓝筹精选(-0.3)]`；点击→升序 `[易方达蓝筹精选, 招商中证白酒]`，箭头变 `↑`；再点→恢复降序 ✅
  - 自选标的默认降序 `[苹果(4.0), 贵州茅台(2.5), 平安银行(-1.0), 腾讯控股(-2.5)]`；点击→升序 `[腾讯控股, 平安银行, 贵州茅台, 苹果]`，箭头变 `↑` ✅
  - 点击标的表头切换后，基金表仍保持降序（两表方向相互独立）✅
  - 浏览器控制台无报错 ✅
  - 截图：`/tmp/quotes_sort_toggle.png`
- **未改动**：后端无修改；持仓/自选数据无变更（全程接口桩，未真实增删）。
- **文档同步**：`功能说明文档.md` 功能清单（自选基金小字列表 / 自选标的列表）+ v23 版本记录条目改为"点表头切换升/降（默认降序）"；`金融工作台_详细设计.md` §5.2 同步；本记录补充本条目。

### 需求 2（fix·中）：总览「持仓盈亏分布」柱状图按收益升序 + 最多展示 10 个 + 新增滑动条
- **用户原话**：「【fix】总览里的持仓盈亏分布的柱状图中，横坐标由左至右按收益排序，最多展示10个持仓，同时下方新增滑动条允许用户向右滑动查看更多持仓柱状图。」
- **根因**：`Dashboard.jsx` 第 59–77 行的「持仓盈亏分布」柱状图直接 `positions.filter(p => p.data_available !== false).map(p => p.name || p.code)` + `.map(p => p.pnl || 0)` 渲染——既未排序也未限制条数；持仓较多时横轴标签被挤压不可读，且用户无法快速定位「亏最多 / 赚最多」两端。
- **方案**：
  1. `Dashboard.jsx` 组件内新增状态 `const [pnlOffset, setPnlOffset] = useState(0)`。
  2. `return` 前派生 4 个常量：
     - `pnlAll = positions.filter(p => p.data_available !== false).slice().sort((a,b) => { const va = a.pnl == null ? -Infinity : a.pnl; const vb = b.pnl == null ? -Infinity : b.pnl; if (va !== vb) return va - vb; const ka = a.name || a.code || '', kb = b.name || b.code || ''; return ka < kb ? -1 : ka > kb ? 1 : 0; })`（**升序：亏最多在左、赚最多在右**；同级用名称稳定排序）。
     - `pnlMaxScroll = Math.max(0, pnlAll.length - 10)`。
     - `pnlSafe = Math.min(pnlOffset, pnlMaxScroll)`（持仓减少时夹紧防越界）。
     - `pnlView = pnlAll.slice(pnlSafe, pnlSafe + 10)`。
  3. ECharts `option.xAxis.data` / `series.data` 改用 `pnlView.map(...)`（最多 10 根柱）。
  4. 柱状图下方新增 `<div className="chart-scroll">`，仅当 `pnlMaxScroll > 0` 时渲染：`<input type="range" min={0} max={pnlMaxScroll} step={1} value={pnlSafe} onChange={e => setPnlOffset(Number(e.target.value))} />` + 窗口位置文案 `第 {pnlSafe+1}–{pnlSafe+pnlView.length} 个 / 共 {pnlAll.length} 个持仓`。
  5. `index.css` 新增 `.chart-scroll` / `.chart-scroll input[type="range"]` / `.chart-scroll-info` 样式（accent-color 用 `--accent`）。
  6. 已 `vite build` → `frontend_dist/assets/index-FELqdbKj.js`（旧 `index-D3x52Q4Z.js` 因 `emptyOutDir` 自动清理）+ 新增 `index-YSwlzfm4.css`。
- **实测验证（双层）**：
  - **Node 逻辑单测**（`/tmp/verify_pnl_logic.mjs`）：精确复刻 `Dashboard.jsx` 的 `pnlAll`/`pnlMaxScroll`/`pnlSafe`/`pnlView` 算法，对 13 条乱序持仓测试——**16/16 全过**：
    - `offset=0` 窗口 pnl 序列 `[-5000,-3000,-1000,-200,0,150,800,1200,3400,5600]`（升序 ✅）
    - `offset=3`（尽头）窗口 pnl 序列 `[-200,0,150,800,1200,3400,5600,7200,9100,12500]`（衔接正确 ✅）
    - 滑块 `max=3`（13-10=3 ✅）、窗口长度恒为 10、`offset` 越界自动夹紧、≤10 条时 `maxScroll=0`、`data_available=false` 的持仓被过滤。
  - **Playwright 真实浏览器**（`/tmp/verify_pnl_ui.py`，系统 `/usr/bin/chromium` + `no-sandbox`，拦截 `/api/positions` 与 `/api/portfolio-summary` 返回 13 条测试持仓，**仅用于验证组件渲染，非产品数据**）——**8/8 全过**：
    - 「持仓盈亏分布」卡片渲染、柱状图 canvas 已绘制（图表无崩溃）✅
    - 下方滑动条出现、滑块 `max=3` ✅
    - 初始窗口文案「第 1–10 个 / 共 13 个持仓」✅
    - 拖动滑块到尽头（用 React 原生 setter 绕过 value tracker）后窗口文案变为「第 4–13 个 / 共 13 个持仓」✅
    - 浏览器控制台无报错 ✅
  - 截图：`/tmp/dashboard_pnl.png`（滑块在右、信息「第 4–13 个」、柱值从 -200 升序至 12500）。
- **未改动**：后端 `app/main.py` / `app/data_sources/*` / 其它页面均无修改；持仓数据无变更（无新增/删除）。
- **文档同步**：`功能说明文档.md` 追加 v23 需求 2 条目（v22 不动）；`金融工作台_详细设计.md` §5.1 追加「v23 持仓盈亏分布」说明。

### 需求 3（fix·中）：国内金价源改用沪金 au9999 + 实物黄金 K 线用 au9999
- **用户原话**：「【fix】先判断是否可以获取到沪金au9999的数据。如果可以，国内金价的价格来源使用沪金au9999的价格。持仓管理中点击实物黄金（沪金）的时候，显示的K线图也使用au9999的数据。持仓计算的金价也使用au9999价格。」
- **根因**：
  - 金价源 `datasource.py` 的 `GOLD_CN_SYMBOL = "nf_AU0"`（沪金主力连续，上期所黄金期货），并非用户要的沪金 Au99.99 现货（SGE，`hf_AU9999`）。
  - 持仓管理中点击实物黄金卡片时的 K 线：`Positions.jsx` 用 `openKline(p.market, p.code)`，`_gold_position` 返回的 `code = "GOLD_PHYSICAL"`；`/api/kline` 对 `mkt == "GOLD"` 走 `else` 分支，`sym = to_sina_symbol("GOLD", "GOLD_PHYSICAL") = "GOLD_PHYSICAL"`（不是真实 symbol），腾讯/新浪均取不到 → 一直 "K线数据为空"。
- **方案**：
  1. `datasource.py`：`GOLD_CN_SYMBOL` 拆为 `GOLD_CN_SYMBOL_AU9999 = "hf_AU9999"`（优先）与 `GOLD_CN_SYMBOL_LEGACY = "nf_AU0"`（回退）；新增 `_gold_price_for_symbol(symbol)`（取单个黄金品种实时价，含 `asof` 解析）；`gold_cn_price()` 改为**先判断 `hf_AU9999` 是否可取，可取则国内金价用 au9999，否则回退 `nf_AU0`，两者皆失败 `available=False`**；新增 `gold_kline_symbol()`（au9999 可取返回 `hf_AU9999`，否则 `nf_AU0`）。
  2. `main.py` `/api/kline` 新增 `mkt == "GOLD"` 分支：`code == "GOLD_PHYSICAL"`（实物黄金卡片）用 `ds.gold_kline_symbol()`（au9999 优先）取 K 线；期货指数（纽约金 `hf_GC` / 沪金 `nf_AU0` / 伦敦金 `hf_XAU`）用各自 `code`。
  3. 持仓计算金价链路（`_gold_position` / `_positions_data` / `/api/gold`）均调用 `gold_cn_price()`，故"持仓计算的金价也使用 au9999"自然满足。
  4. `Quotes.jsx` 金价条新增来源名显示（`gold.price_name`，如 `Au99.99` / `黄金连续`），tooltip 改为说明 au9999 来源与回退。
- **实测验证**：
  - **单元级**（`/tmp` 内联脚本 + FastAPI `TestClient`）：au9999 可取 → `gold_cn_price().name=="Au99.99"`、`gold_kline_symbol()=="hf_AU9999"`；au9999 不可取 → 回退 `黄金连续`(`nf_AU0`)、`gold_kline_symbol()=="nf_AU0"`；`tencent_kline` 遇异常行数据抛 `DataSourceError`（被捕获，不冒泡）✅。
  - **后端 curl**：`/api/gold` 返回 `price_name`（沙箱 `hf_AU9999` 取不到 → 回退 `黄金连续` 996.x，可用，证明"先判断 au9999、取不到回退"逻辑成立；用户环境可达 au9999 时自动用 `Au99.99`）；`/api/kline?market=GOLD&code=GOLD_PHYSICAL` 走 au9999 符号，沙箱不可达返回 `{"code":1,...,"msg":"K线数据暂不可用"}`（HTTP 200 JSON，不再 500）✅。
  - **未改动持仓数据**（金价仅改取值源，未增删持仓）。
- **文档同步**：`功能说明文档.md` v23 版本记录追加本条目 + 功能清单「实时国内金价条」改为 au9999 优先；`金融工作台_详细设计.md` 行情路由表 GOLD 行 + K线说明补充 v23 金价与 K线加固。

### 需求 4（fix·高 + feat·中）：指数看板新增期货指数数据源 + 修复 K 线端点 500 崩溃
- **用户原话**：「【feat】指数看板新增期货指数数据源，允许添加国内外期货指数（纽约金/沪金/伦敦金），默认新增纽约金指数。添加A股板块指数后点击卡片查看k线等数据，显示"Unexpected token 'I', "Internal S"... is not valid JSON"」
- **feat 部分（指数看板新增期货指数数据源）**：
  - `Quotes.jsx` 「＋ 新增指数」表单的市场下拉新增「期货指数」选项（`value="GOLD"`）；新增 **纽约金 / 沪金 / 伦敦金** 三个快捷预设按钮（`addFuturesIndex(name, 'GOLD', code)`，code 分别为 `hf_GC` / `nf_AU0` / `hf_XAU`），提示文案补充期货指数说明。
  - **默认纽约金指数**：`DEFAULT_INDICES` 已有 `("纽约黄金期货", "GOLD", "hf_GC")`（id=10，当前库已含），首次启动即填充，无需重复添加。
- **bug 根因**：点击指数卡片看 K 线时前端报 `Unexpected token 'I', "Internal S"... is not valid JSON`。根因在 `/api/kline`：其只对 `tencent_kline` 用 `except ds.DataSourceError` 包裹；当腾讯对个别指数代码（如某些 A股板块指数）返回 HTTP 200 但行数据异常（非数字 / `day` 为字典而非列表）时，`float(row[1])` 抛 `ValueError` / 遍历字典键 `row["day"]` 抛 `TypeError`，**未被 `DataSourceError` 捕获而冒泡成 500**；Starlette 非调试态对未处理异常返回纯文本 `Internal Server Error`，前端 `request()` 中 `res.json()` 解析该文本即报 `Unexpected token 'I', "Internal S"... is not valid JSON`。
- **方案**：
  1. `datasource.py`：`tencent_kline` / `tencent_us_index_kline` / `sina_kline` 解析时校验 `rows`/`data` 为列表（非列表显式抛 `DataSourceError`），逐行跳过/容错非数字字段（非法行 `continue`，不再让 `float()` 抛未捕获异常）。
  2. `main.py` 抽出 `_fetch_kline(sym, period, count)`：统一「腾讯 `fqkline` 优先 → 不足 2 根回退新浪 `sina_kline`」，且两层均用 `except Exception` 兜底（不只 `DataSourceError`）。
  3. `get_kline` 整段包 `try / except ds.DataSourceError / except Exception → fail("K线数据暂不可用")`：**任何未预期异常都返回 JSON 友好提示而非 500**，彻底消除该崩溃。
  4. 已 `vite build` → `frontend_dist/assets/index-DK7qpwFY.js` + `index-BZ-7z_9F.css`（旧 `index-Sg8vXs71.js` 自动清理）。
- **实测验证（双层）**：
  - **单元测试**（`TestClient`，monkeypatch `ds._get` 返回异常行数据）：`tencent_kline` 抛 `DataSourceError`（被捕获）；`GET /api/kline?market=A&code=sh000037` 对恶意响应返回 `HTTP 200`、`{"code":1,...,"msg":"K线数据暂不可用"}`（**非 500**）✅。
  - **Playwright 真实浏览器**（`/tmp/verify_gold_futures.py`，系统 `/usr/bin/chromium`，对**实际发布产物** localhost:8000 测试）——全过：
    - 金价条显示来源名（沙箱 au9999 不可达回退 `黄金连续`）✅
    - 「＋ 新增指数」市场下拉含「期货指数」、含「纽约金/沪金/伦敦金」三个预设按钮、提示含「期货指数」✅
    - 点「沪金」预设 → 沪金指数加入列表 → 再删除（✕）恢复 ✅
    - 点击「纽约黄金期货」指数卡片 → K 线弹窗稳定显示「📭 K线数据暂不可用」（数据源沙箱不可达，诚实提示）✅
    - **控制台错误含 `Unexpected token`：False；总控制台错误数：0** ✅（"Internal Server Error" 崩溃已根除）
- **数据真实性**：沪金 au9999（`hf_AU9999`）、纽约金（`hf_GC`）、伦敦金（`hf_XAU`）均为新浪真实品种报价；沙箱网络对 `hf_AU9999` 取不到，已按"先判断、取不到回退"设计诚实降级，**绝不填充虚拟数据**。
- **文档同步**：`功能说明文档.md` v23 版本记录追加本条目（feat + bug）；`金融工作台_详细设计.md` 行情路由表 GOLD 行 + K线说明补充 v23 加固；本记录补充本条目。




### 需求 5（fix·中）：录入交易确认份额日期跳过非交易日 + QDII 也可选交易时间（15:00 前/后）
- **用户原话**：「【fix】录入交易中，确认份额日期计算没有跳过非交易日。非国内基金QDII，也需要添加交易时间是15点之前还是之后，同时确认份额日期计算也要跳过非交易日。」
- **根因**：
  - `Positions.jsx` 的 `nextTradingDay(dateStr, offsetDays)` 仅 `if (day !== 0 && day !== 6) added++` 跳过周六周日，**注释明确写着"不含法定节假日"**——确认份额日期遇到法定节假日会落在休市日（正是用户所说的"没有跳过非交易日"）。
  - QDII 的 `fund_time` 选择框被 `{!isQdii && (...)}` 包裹隐藏，QDII 无法选择 15:00 前/后。
- **真实数据约束处理**：确认份额日期需跳过"法定节假日"，但**节假日不能硬编码为虚拟数据**。探查沙箱数据源：GitHub raw（`NateScarlet/holiday-cn`）、`api.jijo.cn`、`oiowa.com` 均不可达（000/404）；新浪 `StockCalendarService` 已 301 失效；东方财富 `datacenter-web.eastmoney.com` 可达但 `RPT_TRADEDAY_*` 等报表名均返回 `code:9501 报表配置不存在`（需精确报表名，靠猜效率低）。最终采用**沪深北交易所官方休市安排公告**（经东方财富转发，权威真实）作为数据源：2026 年休市区间取自 [`stock.eastmoney.com/a/202512223598373130.html`](https://stock.eastmoney.com/a/202512223598373130.html) 与 [`finance.eastmoney.com/a/202512223598652817.html`](https://finance.eastmoney.com/a/202512223598652817.html)。该数据属公开固定的官方规则（非行情虚构数据），以内置集合形式载入，代码内标注来源链接。未收录年份（历史/未来）按"无法获取真实数据需提醒"原则诚实降级。
- **方案**：
  1. `datasource.py`：导入 `from datetime import datetime, timedelta`；新增 `HOLIDAY_SOURCE_NOTE`、`CN_A_SHARE_HOLIDAYS`（2026 年休市日集合，含元旦/春节/清明/劳动/端午/中秋/国庆，调休周末已并入）、`_parse_date()`、`is_cn_trading_day(date_str)->(is_td, covered)`、`next_cn_trading_day(date_str, offset)->(result, covered)`（纯 `date` 运算，杜绝 `toISOString()` 时区漂移）。
  2. `main.py`：新增 `GET /api/fund/confirm-date?trans_date=&qdii=&time=`——`tplus = 2 if qdii else (2 if time=='after' else 1)`，`next_cn_trading_day` 计算；`covered=False` 时返回 `available=False` + "仅按周末推算，请手动核对法定节假日"，否则 `available=True` + `holiday_source_note`。
  3. `Positions.jsx`：删除本地 `nextTradingDay`，新增 `fundConfirm` state + `useEffect`（依赖 `isFundTxn/trans_date/isQdii/fund_time`）调用 `api.fundConfirmDate`（含 `catch` 兜底）；`fundConfirmDate` 取 `fundConfirm.confirm_date`。录入与编辑两个弹窗均改为**统一显示交易时间选择框（15:00 前/后）+ QDII 标签**，提示文案更新为"跳过非交易日"；`available=False` 时显示"⚠️ 休市安排待更新仅跳周末"黄字提醒。`openEdit` 从备注还原 `fund_time`（解析"15:00后"标签），避免编辑态丢失 15:00 状态。
  4. `api.js`：新增 `fundConfirmDate(transDate, qdii, time)`。
  5. `vite build` → `frontend_dist/assets/index-C55y3T9y.js`（CSS 签名不变 `index-BZ-7z_9F.css`，旧 `index-DK7qpwFY.js` 已清理）。
- **实测验证（双层）**：
  - **后端单元级**（`PYTHONPATH=. python -c`）：`next_cn_trading_day('2026-04-30',1)->2026-05-06`（跳过劳动节 5/1-5/5）、`('2026-02-13',1)->2026-02-24`（跳过整个春节）、`('2026-05-08',1)->2026-05-11`、`('2026-10-09',1)->2026-10-12`；`('2024-12-31',1)` 返回 `covered=False`（2024 未收录）；`is_cn_trading_day('2026-01-01')->(False,True)`、`('2026-08-27'今日)->(True,True)` ✅。
  - **后端 curl**：`/api/fund/confirm-date?trans_date=2026-04-30&qdii=false&time=before` -> `confirm_date=2026-05-06, available=true`；`?trans_date=2026-02-13&qdii=true` -> `confirm_date=2026-02-25`（QDII T+2 跳过春节）✅。
  - **Playwright 真实浏览器**（`verify_fix.py`，系统 `/usr/bin/chromium`，对发布产物 localhost:8000）——全过：
    - 境内基金 15:00前 2026-04-30 → 显示 **2026-05-06**（跳过劳动节，T+1）✅
    - 切 15:00后 → 显示 **2026-05-07**（T+2）✅
    - 勾选 QDII → 15:00 选择框**仍可见**（visible=True），确认日期 **T+2 = 2026-05-07** ✅（满足"QDII 也添加交易时间选择"）
    - 日期改 2024-12-31（未收录年份）→ 显示"⚠️ 休市安排待更新仅跳周末" + "该年份休市安排未收录真实数据，仅按周末推算，请手动核对法定节假日" ✅（遵守真实数据约束提醒）
    - **控制台错误：0** ✅
- **数据真实性**：休市安排源自沪深北交易所官方公告（经东方财富转发），非虚构数据；未覆盖年份诚实降级并提示用户核对。
- **文档同步**：`功能说明文档.md` ① 功能清单「基金确认份额规则」行更新（跳过非交易日、QDII 仍显示交易时间选择框仅作记录）；② v23 版本记录追加本需求条目；③ 第三章「数据源与数据真实性」新增"中国 A股 休市安排"来源说明。`金融工作台_详细设计.md` 第96行 v23 区块追加「v23 确认份额日期（跳过非交易日）」设计说明（数据源/算法/接口/前端/`available` 降级）。本记录追加本条目。

### 需求 6（feat·中）：组合分析新增「收益分析」模块（日/月/年/累计 + 收益明细 + 柱状/日历图）
- **用户原话**：「【feat】组合分析中新增收益分析模块，可选择展示日收益，月收益，年收益，累计收益，默认展示日收益。收益分析模块新增收益明细功能，展示所有持仓在选择范围内的收益数量。每个持仓在当日净值更新到最新之后，刷新收益分析模块的数据。收益分析展示允许用户选择柱状图还是日历图。默认使用日历图。」
- **真实数据约束处理**：收益分析需按持仓的历史收益，但原系统**只有组合级每日快照**（`snapshots`，每天一条总市值），无按持仓拆分。为真实计算"每个持仓在区间内的收益明细"，经与用户确认**新增持仓级每日快照表 `position_snapshots`**（组合汇总时自动记录每个持仓当天市值，非虚拟数据）；若历史天数不足则 `available=False` 诚实提示，绝不填充虚拟收益。月/年范围按"自然月/自然年"首末快照差值（用户确认），非滚动窗口。
- **方案**：
  1. `database.py`：新增 `position_snapshots` 表（`snap_date,market,code,market_value`，`UNIQUE(snap_date,market,code)` 幂等）；`ensure_position_snapshots_today(items)`（当天首次写入每个持仓市值，`market_value=None` 跳过）、`get_position_snapshots(market,code)`、`get_position_snapshots_on(snap_date)`、`get_all_position_snapshot_dates()`。`init_db` 建表。
  2. `main.py` `portfolio_summary()`：写组合快照后，收集每个持仓 `(market,code,market_value)` 调 `ensure_position_snapshots_today()`（实物黄金写 `("GOLD","GOLD_PHYSICAL",mv)`）。
  3. `main.py` 新增 `GET /api/portfolio/pnl-analysis?type=day|month|year|cum&range=`：区间首末快照差值算组合收益；每持仓用 `position_snapshots` 区间首末市值差值算明细（区间内任一天缺快照的持仓跳过）；`calendar` 为每日组合收益序列。注意参数名原为 `range` 会遮蔽内置 `range()` 导致 `TypeError`，已改名 `range_val` 修复。
  4. `frontend/src/api.js`：新增 `pnlAnalysis(type, range)`。
  5. `frontend/src/pages/Analysis.jsx`：新增收益分析卡片（类型段控默认日、范围输入、图表段控默认日历）；`buildCalendarOption`（ECharts calendar+heatmap 红跌绿涨）/ `buildBarOption`（每持仓区间收益柱状）；`pnl-detail` 收益明细表（每持仓区间收益，点击行看 K 线）；`useEffect(loadPnl,[pnlType,pnlRange,refreshSec])` 跟随刷新频率实时更新，手动刷新按钮也触发。
  6. `frontend/src/index.css`：新增 `.pnl-detail` 表格样式。
  7. `vite build` → `frontend_dist/assets/index-o7n_FjpZ.js` + `index-BtoJYtps.css`（旧 `index-C55y3T9y.js` 已清理）。
- **实测验证（双层）**：
  - **后端单元级**（临时库注入多天历史快照）：day(27) 组合 -50、明细 [AAPL -20, 茅台 -30]；cum 组合 +50、明细 [茅台 +30, AAPL +20]；month(08) +50、calendar 含 26/27 两天；year(2026) +50 —— 全部与预期一致 ✅。
  - **Playwright 真实浏览器**（系统 `/usr/bin/chromium`，对发布产物 localhost:8000）：默认日收益+日历图渲染（区间 2026-08-27、当日收益 -1,500、持仓数 2、日历 canvas）；切柱状图 canvas 渲染+明细表 2 行；累计收益区间 2026-08-25~27、累计 +1,500；类型按钮(4)+图表按钮(2) 均存在；**控制台错误 0** ✅。
  - **库洁净**：为验证注入的测试历史快照已删除，交付库 `snapshots`/`position_snapshots` 恢复为 0 条。
- **数据真实性**：收益全部来自真实每日快照（组合级 `snapshots` + 持仓级 `position_snapshots`），无虚拟数据；历史天数不足时诚实提示。
- **文档同步**：`功能说明文档.md` ① 第5章组合分析表新增「收益分析」行；② v23 版本记录追加本需求条目；`金融工作台_详细设计.md` 第4.4节组合分析新增 `/api/portfolio/pnl-analysis` 接口与 `position_snapshots` 表说明。本记录追加本条目。

### 需求 7（fix·中 ×2）：① 饼图明细按市值排序 + K线窗口层级；② 持仓排序切换界面后保持
- **用户原话**：「【fix】组合分析中点击饼状图显示持仓明细时，列表按持仓市值排序显示。点击列表中的产品名称显示k线图时，窗口在列表窗口之上，现在是在列表窗口的下面。」「【fix】持仓明细中点击3种排序按钮排序之后，跳转到其他界面再回来，排序方式重置了，没有继续使用之前选择的排序方式。改为仍使用之前选择的排序方式。」
- **根因①**：组合分析饼图明细列表（`Analysis.jsx` 的 `allocModal.items.map`）按后端返回原序渲染，未按市值排序；且 `.alloc-modal-mask` CSS `z-index:1000`，高于全局 K 线弹窗 `.modal`（`z-index:100`），导致点击明细行弹出的 K 线窗口被明细弹窗遮挡（显示在下面）。
- **根因②**：持仓管理页排序 `posSort` 仅用 `useState('pnl')`，组件卸载（跳转其他界面）即丢失，回来重建重置为默认「按持仓收益」。
- **方案**：
  1. `Analysis.jsx`：饼图明细渲染改为 `[...allocModal.items].sort((a,b)=>(b.market_value||0)-(a.market_value||0))`（市值降序，不修改原数据）；点击行仍 `openKline` 看 K 线。
  2. `index.css`：`.alloc-modal-mask` z-index 由 `1000` 改为 `90`（低于 `.modal` 的 `100`），使 K 线抽屉浮于明细列表窗口之上（alloc-modal 与录入/编辑 `.modal` 不会同时出现，无冲突）。
  3. `Positions.jsx`：排序持久化——`posSort` 初始值从 `localStorage.getItem('fw_pos_sort')` 读取（校验 `'pnl'|'mv'|'cost'` 合法值，否则默认 `'pnl'`）；新增 `changePosSort(s)` 在切换时 `localStorage.setItem('fw_pos_sort', s)` 并 `setPosSort(s)`；3 个排序按钮 `onClick` 改用 `changePosSort`。
  4. `vite build` → `frontend_dist/assets/index-D7dqpddD.js` + `index-BUvwpTmp.css`（旧 `index-o7n_FjpZ.js` 已清理）。
- **实测验证（Playwright 真实浏览器，系统 `/usr/bin/chromium，对发布产物 localhost:8000）**：
  - **修复②**：持仓管理页点「按市值」→ 标记「按市值 ✓」；`page.reload()`（模拟跳界面再回来，组件重建）→ 重新进入持仓管理仍显示「按市值 ✓」（localStorage 持久化生效）✅。
  - **修复①**：UI 录入 2 笔真实交易（贵州茅台市值≈129230、另一 A股≈23180）产生持仓；组合分析点击饼图扇区弹出明细 → 市值序列 `[129230.0, 23180.0]` **降序**✅；点击首行 → K 线弹窗出现、关闭按钮可见可点（在明细窗口之上）、关闭后明细窗口仍在✅。
  - **控制台错误：0** ✅。
  - **库洁净**：验证注入的 2 笔测试交易与持仓快照已通过接口删除，交付库 `transactions`/`positions`/`position_snapshots` 恢复为空（仅保留今日 1 条组合快照）。
- **数据真实性**：验证用真实行情录入交易（贵州茅台等真实标的），无虚拟数据；删除测试数据后库干净。
- **文档同步**：`功能说明文档.md` ① 第5章饼图明细说明补充"v23 起按市值降序 + K线窗口浮于其上"；② 持仓管理功能增强说明补充"v23 起排序方式持久化（localStorage）"；③ v23 版本记录追加本需求条目（2 项修复 + 构建产物）。`金融工作台_详细设计.md` 第4.4节组合分析补充 allocation 明细排序/层级与持仓排序持久化说明。本记录追加本条目。

### 需求 1（fix·低）：总览「持仓盈亏分布」柱状图横坐标排序方向反转（收益高→低）
- **用户原话**：「【fix】总览里的持仓盈亏分布的柱状图中，横坐标由左至右按收益排序，顺序从左至右按收益高到低排序，现在反了。」
- **根因**：`Dashboard.jsx` 第 40 行排序比较为 `return va - vb`（升序），导致横坐标左低右高（亏在左、赚在右），与用户预期的"左高右低"相反。v23 该需求实现时即按升序，本次修正。
- **方案**：将比较符号由 `va - vb` 改为 `vb - va`（降序），横坐标由左至右为收益最高 → 收益最低/亏损；同级仍按名称稳定排序。仅改一行。
- **构建产物**：`vite build` → `frontend_dist/assets/index-BGEkeBei.js`（CSS 签名不变 `index-BUvwpTmp.css`，旧 `index-D7dqpddD.js` 已清理）。
- **实测验证**：通过 `POST /api/position/override` 写入 3 个真实持仓（贵州茅台/苹果/平安银行，真实行情 pnl 分别为 129230/31345/1159），用与前端完全相同的比较器 `vb - va` 排序，结果从左到右严格降序（茅台→苹果→平安银行）✅；测试持仓与 `position_snapshots` 已删除，交付库 `positions`/`position_override`/`position_snapshots` 恢复为空（仅保留今日 1 条组合快照）。注：ECharts 实例未挂全局、canvas 不可直接读 DOM，故采用"接口取真实持仓 + 复刻前端比较器"的方式验证排序方向，等价确认。
- **数据真实性**：验证用真实行情标的（贵州茅台等），无虚拟数据；清理后库干净。
- **文档同步**：`功能说明文档.md` ① v23 需求1 条目排序方向描述由"升序"更正为"降序（v24 修正）"并注明原因为用户反馈方向反；② 新增 `### v24（2026-08-27）` 版本记录。 `金融工作台_详细设计.md` 第4.x 节持仓盈亏分布说明同步改为降序 `b.pnl - a.pnl` 并标注 v24 修正。本记录追加本条目。

### 需求 2（fix·中）：数据导入导出补齐「持仓级每日快照 position_snapshots」
- **用户原话**：「【fix】检查数据导入导出功能是否覆盖系统所有的看板数据，如果有遗漏的，加入进去。不确定的数据可以问我。」
- **排查过程**：全库共 11 张用户表（init_db 建表）。逐一核对 `db.export_all_data` / `db.import_all_data` 的覆盖情况：
  - 已覆盖 10 张：watchlist / transactions / snapshots / gold_holding / gold_transactions / asset_profit / position_override / pinned_positions / custom_indices，以及由 asset_profit、position_override 字典转出的列表。
  - **遗漏 1 张：`position_snapshots`**（v23 需求6 新增的持仓级每日快照表，收益分析模块历史明细数据源）。
- **影响评估（关键）**：收益分析模块「日/月/年/累计收益 + 每个持仓区间收益明细」全部来自 `position_snapshots`（区间首末各持仓市值差值）。其数据为每天打开组合分析时自动记录，**历史天数不可重生**（过去某天的持仓市值无法回算）。若导入导出不带该表，换机器/恢复后收益分析历史明细全空，仅剩导入当日及以后自动累积的新数据——属"看板数据遗漏"，需补齐。
- **是否需问用户**：无需。该表为不可重生的历史看板数据，纳入导入导出是唯一正确做法（其余 10 张已覆盖，前端 localStorage 偏好不纳入、与单用户本地架构一致，已在文档注明）。
- **方案**：`app/database.py` —— ① `export_all_data()` 新增读取 `position_snapshots` 全表（`snap_date/market/code/market_value`）写入 payload；② `import_all_data()` 清空阶段加 `DELETE FROM position_snapshots`，写入阶段按 `(snap_date,market,code)` 幂等 `INSERT OR REPLACE`，统计加 `position_snapshots` 计数；③ docstring 统计说明同步。纯后端改动，前端无改动、无需 `vite build`。
- **实测验证（round-trip）**：注入 3 天×2 持仓共 6 条历史快照 → `GET /api/data/export` 返回 JSON 含 `position_snapshots:6`；`DELETE` 清空该表模拟换机器 → `db.import_all_data(payload)` → 表恢复 6 条（返回 `imported.position_snapshots=6`）。完整闭环通过 ✅。注：中间一次测试因"先清空库再导出"导致导出 0 条、误判，重做完整闭环后确认逻辑正确。
- **库洁净**：验证注入的 6 条测试快照已删除，交付库 `position_snapshots` 恢复为空（仅保留今日组合快照）。后端已重启（pid=554191）使改动生效。
- **数据真实性**：`position_snapshots` 为组合汇总时的真实每日持仓市值，无虚拟数据；导入导出为原样搬运。
- **文档同步**：`功能说明文档.md` ① 第10章数据导入导出「导出」行范围补充"持仓级每日快照（收益分析历史明细）"；② v24 版本记录追加本需求条目（排查+影响+修复+验证）。`金融工作台_详细设计.md` 新增「4.6.1 数据整体导入导出（跨机器迁移）」小节，说明 11 表覆盖范围与 `position_snapshots` 纳入原因。本记录追加本条目。

### 需求 3（fix·高 + 修正·中）：收益分析计算口径统一为「日收益」单元 + 配色红涨绿跌
- **用户原话**：「【fix】收益分析里，红色表示赚，绿色表示亏。计算当日收益时，仅计算上一个交易日所有持仓产品的收益总和。月收益和年收益通过范围内日收益来计算。计算累计收益时，计算所有持仓产品的累计收益总和。」
- **根因①（配色）**：原日历图 visualMap `['#f85149'(红),灰,'#3fb950'(绿)]`（红跌绿涨）与柱状图 `v>=0?绿:红`，**与全站 K线「红涨绿跌」(v18) 相反**，用户要求红=赚绿=亏。
- **根因②（计算口径）**：原 `pnl_analysis()` 对 day/month/year/cum 一律用「区间首末总市值差值」，不符合用户要求的「日收益单元」语义——日收益应只算上一交易日→当日、月年应累加日收益、累计应为持仓加总。
- **方案**：① 前端 `Analysis.jsx` 颜色反转：`buildBarOption` 改为 `v>=0?'#f85149'(红):'#3fb950'(绿)`；`buildCalendarOption` visualMap `['#3fb950'(绿/亏),灰,'#f85149'(红/赚)]`。② 重写 `app/main.py` `pnl_analysis()`：去掉首末差值分支，改为先构造 `pairs`（相邻交易日对：day 取 ≤target 最近两条、month/year/cum 取区间内全部相邻对）→ 遍历 pairs 累加 `combo_pnl`（组合日收益求和）与 `pos_pnl[k]`（每持仓日收益累加，任一端无快照则跳过该日）→ `details` 由 `pos_pnl` 生成（与 combo 自洽）；`calendar` 保留每日组合收益序列。③ `vite build` 重新产出前端（旧 `index-BGEkeBei.js` 清理）。
- **实测验证（注入 3 天×2 持仓连续快照）**：day(27) combo=-1500、details 茅台-1000/苹果0（确为「上一交易日→当日」单对）；month(08) combo=+1500（日收益 3000+(-1500) 累加）、details 茅台+1000；year(2026)/cum 同为 +1500、details 茅台+1000（累计=持仓加总，与明细自洽）；calendar 显示 [08-26:+3000, 08-27:-1500] 印证月年为日收益累加。**四项全部符合用户要求** ✅；日历/柱状图颜色经构建产物源码 grep 确认红涨绿跌反转 ✅；前端 Playwright 进入组合分析→收益分析卡片渲染正常、当日收益 -1500、明细与后端一致、控制台 0 错误 ✅。
- **库洁净**：验证注入的 3 天组合/持仓快照已删除，交付库 `snapshots`/`position_snapshots` 恢复为空。后端已重启（pid=589176）加载新逻辑。
- **数据真实性**：全部来自真实每日快照（组合级 `snapshots` + 持仓级 `position_snapshots`），无虚拟数据；区间内缺快照持仓自动跳过避免误导。
- **文档同步**：`功能说明文档.md` ① 第5章收益分析行重写为 v24 口径（日收益单对 / 月年日收益累加 / 累计持仓加总 / 红涨绿跌），并补充数据前提与提示文案；② v24 版本记录追加本需求条目。`金融工作台_详细设计.md` 第4.4节 `pnl-analysis` 接口说明改为「日收益单元」口径 + 配色说明。本记录追加本条目。

### 需求 1（fix·高）：纽约金/沪金/伦敦金 K 线「数据暂不可用」修复
- **用户原话**：「【fix】纽约金，沪金，伦敦金为什么显示K线数据暂不可用。」
- **根因排查（实测）**：`/api/kline` GOLD 分支对期货代码（纽约金 `hf_GC`/沪金 `nf_AU0`/伦敦金 `hf_XAU`）走腾讯 `fqkline`，腾讯对这类期货代码返回 `data:[]`（空 list），`tencent_kline` 在 list 上 `.get(symbol)` 抛 `AttributeError`，被 `_fetch_kline` 兜底空、再回退新浪 `sina_kline` 对期货代码 456 → "K线数据暂不可用"。沪金此前也因同因不可用。
- **真实数据源确认**（遵守"无法获取真实数据需提醒"）：实测新浪期货日K线接口 `InnerFuturesNewService.getDailyKLine`——**沪金 `AU0` 稳定可用**（2008 至今 4540+ 条）；**纽约金 `GC0`/伦敦金 `XAUUSD`** 在新浪/腾讯/东财当前均返回空（COMEX GC、伦敦金 XAU/USD 历史K线源在沙箱不可达）。
- **方案**：`datasource.py` 新增 `sina_futures_kline(sym)`（解析 `{d,o,h,l,c,v}`）；`get_kline` GOLD 分支按品种映射——实物黄金(沪金)→`AU0`、纽约金 `hf_GC`→`GC0`、伦敦金 `hf_XAU`→`XAUUSD`、沪金 `nf_AU0/AU0`→`AU0`；纽约金/伦敦金真实源不可达时 `fail("该黄金品种K线数据源暂不可用")` 诚实提示，绝不回退造假。
- **验证**：沪金 `AU0` 返回 5 根真实数据（2026-08-21~27）✅；实物黄金(沪金)K线可用✅；纽约金 `hf_GC`/伦敦金 `hf_XAU` 返回友好提示而非崩溃✅。
- **文档同步**：`功能说明文档.md` 新增 v25 版本记录需求1条目；`金融工作台_详细设计.md` 第3章数据源表 GOLD 行更新 K线源说明。本记录追加本条目。

### 需求 2（fix·中 + 新增·中）：收益分析收益数值可自定义编辑 + 日历点击刷新 + 新增持仓不计日收益
- **用户原话**：「【fix】收益分析中的收益数值允许自定义编辑。如果新增的产品在添加之前没有持仓，那么新增的收益不计算在日收益中。点击日历框选择日期后数据没有刷新到目标日期。」
- **子点A（可编辑，已与用户确认"两者都要+持久化"）**：新增表 `pnl_override(market,code,pnl_type,range_val,detail_pnl,combo_pnl,UNIQUE(market,code,pnl_type,range_val))`（`__COMBO__` 为组合总收益行）；`database.py` 增 `get_pnl_override/save_pnl_override/get_all_pnl_overrides`；`pnl_analysis` 套用覆盖（detail 用 `detail_pnl`、combo 用 `__COMBO__` 行，返回 `edited` 标记）；新增 `POST /api/pnl-override`（保存/清除）。前端顶部收益与明细「区间收益」列改为可编辑 `input`（失焦/回车保存 `api.savePnlOverride`），刷新保留。
- **子点B（日历点击刷新）**：日历图 EChart 加 `onEvents={{click:onCalClick}}`，点击某天 → 切 day + 设 `pnlRange` + 显式 `loadPnl()`，刷新到目标日期。
- **子点C（新增持仓不计日收益）**：`pnl_analysis` day 模式遍历相邻交易日对时，对"前一天无快照"持仓（`pv is None`）跳过——当日才新增的持仓"从0到市值"不计入当日收益；month/year/cum 同理。
- **附带 bug 修复**：前端 `api.pnlAnalysis` 原拼 `&range=`，后端参数名是 `range_val`（v23 改 `range`→`range_val` 防遮蔽内置但没同步前端），导致范围恒空、收益分析恒用今天；前端改为 `&range_val=`。
- **验证（全过）**：编辑茅台 detail=9999 + combo=8888 → 返回 9999/8888 且 `edited=true`、清除后恢复 -1000/-1500 ✅；新增持仓（仅当日有快照）day 模式不计入 ✅；前端渲染编辑 input 存在、图表切换无错、控制台 0 错误 ✅；测试数据已清理，库干净。
- **文档同步**：`功能说明文档.md` 新增 v25 需求2条目；`金融工作台_详细设计.md` 第4.4节 pnl-analysis 返回结构与编辑/日历说明补充。本记录追加本条目。

### 需求 3（fix·中）：新增基金持仓用单位净值（NAV）计算市值
- **用户原话**：「【fix】新增基金持仓的时候，如果当日没有闭市，则使用前一交易日的净值计算。如果已经闭市而且当日净值已更新到最新，则使用当日净值计算。」
- **根因**：`_positions_data` FUND 分支用 `sina_quotes` 实时价（盘中估值）算基金市值，非净值。
- **方案**：FUND 分支改取天天基金单位净值 `fund_kline(code, count=1)` 最新 NAV 作 `price` 计算市值。NAV 每日收盘后才更新——取 NAV 最新一条即天然满足"当日已更新用当日、否则前一交易日"，无需单独判断闭市时间（加仓录入时仍按当日实时价算份额数量，二者分工正确）。
- **验证**：加基金 161725 持仓，查 `/api/positions` 返回 `price=0.5463`（NAV 最新净值）而非实时价 0.5464 ✅；NAV 来自天天基金真实接口，源不可达时 `data_available=False` 诚实提示。
- **文档同步**：`功能说明文档.md` 新增 v25 需求3条目；`金融工作台_详细设计.md` 第3章数据源表 FUND 行更新净值说明。本记录追加本条目。

## 十六、v26 改动记录（2026-08-28）

### 需求 1（feat·中）：总览卡片点击查看近一个月收益走势折线图
- **用户原话**：「【fix】总览中的当前持仓收益和累计收益卡片，允许点击查看详情，详情显示近一个月的收益折线图，最右边的当前日期，允许折线图向左滑动，直至有收益记录的第一天。」
- **方案**：
  - 后端 `app/main.py` 新增 `GET /api/portfolio/pnl-series?days=30&kind=holding|cum`：取最近 N 天 `snapshots`，相邻交易日对累加 `daily_pnl = cur.total_market_value − prev.total_market_value`；`cumulative_pnl` 在 `holding` 模式为「每日总市值 − 总成本」、`cum` 模式为「累计日收益累加（含已实现）」，返回 `{dates, daily_pnl, cumulative_pnl, latest_date}`。
  - 前端 `Dashboard.jsx` 新增 `pnlDetail` 状态 + `openPnlDetail(kind)` + `buildPnlSeriesOption(dates, daily, cumulative)`（ECharts line + `dataZoom` 滑块/滚轮，可向左滑至首条记录）；`api.js` 新增 `pnlSeries`；`index.css` 新增 `.alloc-modal-mask`/`.modal-card`/`.chart-lg`/`.pnl-series-foot`。两张 stat 卡片（当前持仓收益 / 累计收益）`onClick` 弹窗。
- **验证**：重启后端加载新接口；总览卡片点击弹出折线图 canvas，含「最右为当前日期（2026-08-28）」提示；快照不足时诚实提示「暂无收益记录」，不造假。
- **数据真实性**：全部来自真实每日 `snapshots` 快照（与组合汇总同源），无虚拟数据。

### 需求 2（feat·中 + fix·中）：行情看板「预估日收益」列 + 涨跌幅异动跟踪
- **用户原话**：「【fix】行情看板的自选基金和标的板块，新增"预估日收益"列，通过日涨跌幅和当前持仓计算当日收益。"预估日收益"列允许排序，可选择正序或倒序。允许用户在自选基金和标的的模块设置涨跌幅异动跟踪值，可选择1%～10%。默认5%。当该产品当日涨幅超过5%或跌幅超过-5%时，标亮该产品并提醒用户产品异动。」
- **方案**：
  - **预估日收益列**：「自选基金」「自选标的」两表新增「预估日收益」列 = 当日涨跌幅(%) × 当前持仓市值；`fetchWatchlist` 在 `api.positions()` 时构建 `(market:code) → 持仓市值` 映射 `mvMap`（基金 code 经 `normCode` 去 `fu_` 前缀以匹配自选列表），`valOf(it,'pnl') = pct!=null && mv!=null ? pct/100*mv : null`，无持仓显示「—」。
  - **排序重构**：原 `fundDir/watchDir` 升级为 `fundSort/watchSort`（`{key:'pct'|'pnl', dir}`），`toggleSort` 同列翻转、异列默认降序；「日涨跌幅」「预估日收益」两表头均可点击切换（独立箭头），缺失值沉底。
  - **异动跟踪**：两模块头部均提供阈值下拉 `select.anomaly-sel`（1%~10%，默认 5%，`localStorage('fw_anomaly_pct')` 持久化）；`isAnomaly(it) = pct>阈值 || pct<−阈值` 时该行加 `.anomaly` 高亮 + 「异动」徽标，并在看板顶部渲染 `.anomaly-banner` 横幅列出所有触发产品，回落阈值内自动取消。
- **验证（Playwright + 系统 chromium 真实浏览器）**：① 贵州茅台（sh600519）行「预估日收益」显示 `¥+90.52`（pct +0.07% × 市值 129318 ≈ 90.52，计算正确）✅；② 点击「预估日收益」表头后首行由「平安银行」变为「贵州茅台」（按估值降序）✅；③ 阈值设 1% 后「招商国证生物医药」(−1.80%) 被标亮并出现在异动横幅 ✅；④ 总览卡片点击弹窗渲染折线图 canvas ✅；⑤ 控制台 0 错误 ✅。
- **库洁净**：验证注入的 2 笔测试交易（sh600519 / fu_161725）与 2 条测试快照已删除，交付库 `transactions` 恢复为空、`snapshots`/`position_snapshots` 清空（随使用自动重建），`watchlist` 保留 5 条原始自选。
- **数据真实性**：预估日收益基于真实持仓市值（基金按 NAV）与真实行情涨跌幅，无虚拟数据；无持仓者诚实显示「—」。
- **文档同步**：`功能说明文档.md` ① 行情看板功能表新增「自选基金/标的『预估日收益』列」「涨跌幅异动跟踪」两行；② 版本记录新增 `### v26（2026-08-28）` 两条需求条目。`金融工作台_详细设计.md` ① 第4.4节新增 `pnl-series` 接口契约；② 第5.1节补充总览卡片点击弹窗说明；③ 第5.2节补充 v26 预估日收益列 + 异动跟踪设计说明。本记录追加本条目。

## 十七、v27 改动记录（2026-08-28）

### 修复（fix·高）：收益分析编辑日收益后，月/年/累计与日历图未联动重算
- **用户反馈**：「收益分析中修改完日收益后，后面月收益、年收益和累计收益没有基于修改后的日收益重新计算。日历图里的值也没有同步过去。」
- **根因**：`app/main.py` 的 `pnl_analysis`（约第 1130–1154 行累加段）在聚合 month/year/cum 时，直接对相邻交易日对的快照市值差值求和（`combo_pnl` 用 `cur.total_market_value - prev.total_market_value`、`pos_pnl` 用 `cv - pv`），拼接日历图 `calendar` 也只用原始 `day_combo`。两处都未读取 `pnl_override` 表中 `pnl_type='day'` 的覆盖——该覆盖只在 day 视图自身的 `details`/`combo` 套用（第 1168/1175 行按当前 `type` 查询）。因此编辑日收益仅当天的 day 视图生效，向上聚合与日历图不变。
- **方案**：重写累加段——对 pairs 中每一天 `day_date`：① 每个持仓优先采用 `db.get_pnl_override(m, c, 'day', day_date).detail_pnl` 作为当日收益单元（存在且非 None 时替代 `cv-pv`）；② 组合当日收益 `day_combo` 先由各持仓修正值求和，再优先采用组合级 `db.get_pnl_override(COMBO_CODE, COMBO_CODE, 'day', day_date).combo_pnl`；③ 该 `day_combo` 向上累加进 month/year/cum 的 `combo_pnl`，修正后持仓值累加进 `pos_pnl`，并写入 `calendar`。即「日收益」编辑被视为该日收益单元的最终值，自动向上传播到所有聚合层级与日历图。`month`/`year`/`cum` 自身被直接编辑时仍按原层级覆盖（不反向分解到日），语义不变。
- **验证（注入 3 天快照 + 持仓 sh600519 + 编辑 2026-08-27 日收益=9999）**：日视图该持仓 `pnl=9999, edited=true` ✅；月视图该持仓 `pnl=14999`（9999 已传播叠加 08-27→08-28 的原始 5000）✅；累计视图 `pnl=14999`（含传播）✅；日历图 `2026-08-27` 当天 `pnl=9999`（同步）✅；控制台无异常。测试数据（transactions/snapshots/position_snapshots/pnl_override）已全部清理，`snapshots` 归零，库干净。
- **前端无需改动**：`Analysis.jsx` 的保存（`savePnlOverride` 传当前 `pnlType`+`range`，day 视图 `range=pnlRange=当天日期`）与保存后 `loadPnl()` 刷新逻辑本就正确；本次纯后端聚合逻辑修复，前端代码与已构建的 `frontend_dist` 均不变，仅需重启后端加载新 `main.py`。
- **文档同步**：`功能说明文档.md` 版本记录新增 `### v27（2026-08-28）` 修复条目；`金融工作台_详细设计.md` 第4.4节「收益可编辑（v25）」后补充「日收益编辑向上传播（v27 修复）」说明。本记录追加本条目。

## 十八、v28 改动记录（2026-08-28）

### 需求：纽约金/伦敦金 K 线接入真实数据（攻关，结果：改用沪金 AU0 真实K线代理）
- **用户原话**：「攻关纽约金/伦敦金K线源」——继续完成"纽约金/伦敦金历史K线数据源暂不可用"这一未完成项。
- **调研（真实数据源实测）**：① 新浪期货日K `InnerFuturesNewService.getDailyKLine`：纽约金 `GC0` / 伦敦金 `XAUUSD` 均返回 `[]`；对照沪金 `AU0` 返回 4540+ 条真实数据（2008 至今）。② 腾讯 `fqkline`/`usfqkline`：对 `usGC`/`hf_GC` 返回 `{"day":[],"qt":{...}}`——实时报价有值但历史 `day` 为空；`hf_GC`/`hf_XAU` 直接 `param error`。③ 东方财富 `push2his.eastmoney.com`：连 A 股茅台 `1.600519` 都返回空，确认该接口整体不可达。④ 结论：在可访问的免费公开源中，纽约金(COMEX GC)/伦敦金(XAU/USD) **历史日K线均不可得**；唯一可得的黄金真实历史K线是沪金 `AU0`。
- **方案（经用户确认：沪金AU0替代并标注）**：纽约金/伦敦金 K 线回退到沪金 `AU0` 真实K线。`app/main.py` `get_kline` GOLD 分支：对 `hf_GC`/`hf_XAU` 先试各自真实源 `GC0`/`XAUUSD`，`DataSourceError` 时改取 `sina_futures_kline("AU0")`，并置 `proxied=True` + `proxy_note`（"纽约金(COMEX GC)/伦敦金(XAU/USD) 历史K线真实数据源暂不可达，以下为沪金 AU0 真实K线（同为黄金、走势强相关；单位为元/克，与纽约金/伦敦金的美元/盎司计价不同，仅供参考）"）；`ok()` 返回新增 `proxied`/`proxy_note` 字段。前端 `KlineChart.jsx` 解构 `proxied`/`proxy_note`，`proxied` 为真时在图表上方渲染 `⚠️ {proxy_note}` 提示条；`index.css` 新增 `.kline-proxy-note` 样式（琥珀色提示框）。沪金 `nf_AU0`/`AU0`、实物黄金 `GOLD_PHYSICAL` 逻辑不变。
- **验证（Python 直连 `datasource.sina_futures_kline` 复现 GOLD 分支）**：沪金 `AU0` 真实K线可取（末3日 2026-08-26/27/28，最新收盘 999.28 元/克）✅；纽约金 `GC0` 为空触发回退 ✅；`hf_GC`/`hf_XAU` 模拟返回 `proxied=True`、天数 5 ✅；`AU0`/`nf_AU0` `proxied=False` ✅。前端已重新 `vite build` 生成 `frontend_dist`。
- **数据真实性**：代理 K 线为沪金 `AU0` 真实历史数据（非虚拟）；`proxy_note` 明确声明纽约金/伦敦金本身历史源不可达、当前为同属黄金的沪金真实K线且计价单位不同——符合项目"不允许虚拟数据、无法获取须提醒"约束。
- **文档同步**：`功能说明文档.md` 版本记录新增 `### v28（2026-08-28）` 条目；`README.md` 行情看板黄金K线说明补充"纽约金/伦敦金K线暂以沪金 AU0 真实K线代理展示并标注"；本记录追加本条目。

## 十九、v29 改动记录（2026-08-28）

### 需求：修复 Windows 一键脚本中文乱码报错 + 部署机 Python 3.13 兼容性问题
- **用户现象**：① 在 `E:\Programs\finance-dashboard` 双击 `operation.bat` 报 `'口' is not recognized as an internal or external command` 与 `'n.bat' is not recognized`；② 此前 `setup_env.bat` 在 Python 3.13 上建出的 `.venv` 启动时 `import fastapi` 即崩，错误 `TypeError: ForwardRef._evaluate() missing 1 required keyword-only argument: 'recursive_guard'`。
- **根因**：① `.bat` 脚本含中文（注释、`echo` 提示、`start` 窗口标题「金融工作台后端」、全角冒号等），`cmd` 用系统 OEM 代码页 GBK 解析 UTF-8 无 BOM 脚本，中文多字节被误拆为乱码命令；`chcp 65001` 只改输出不改解析，故仍崩。`operation.bat` 第 60 行 `start "金融工作台后端" ...` 的中文标题与行内中文是最易被误读的触发点。② 锁定依赖 `fastapi 0.99.1 + pydantic 1.10.14(v1)` 从未支持 Python 3.13；`setup_env.bat` 旧版仅校验 `v[1]>=7`，对 3.13 放行，导致在 3.13 机器上建出必崩的 venv。
- **方案**：① 将两个 `.bat`（`operation.bat`、`setup_env.bat`）重写为**纯 ASCII**——去全部中文，提示/标题/注释改用英文（窗口标题改为 `FinanceDashboard Backend`），与代码页无关，彻底消除 GBK 误读。② `setup_env.bat` 版本校验由「≥3.7」收为「**3.7–3.12**」（3.13+ 直接失败并提示安装 3.12）；并新增**优先探测 `py -3.12/3.11/3.10/3.9/3.8/3.7` 启动器**，本机若装有 3.12 则自动绕过 3.13 建 venv。③ 同步更新 `README.md` 的 Windows 前置与依赖兼容性说明（明确 3.7–3.12、不支持 3.13）。
- **影响面核查**：仅脚本层（`.bat`）改动；未触及 `app/main.py`、持仓导入导出、港股持仓备份恢复逻辑、数据库结构、`frontend_dist`——故本次**无需**改动数据导入导出功能（符合"每次改动评估是否需同步导入导出"的要求，结论：不需要）。
- **验证**：仓库与 `E:\Programs\finance-dashboard` 四份 `.bat` 副本经字节检测均为 0 非 ASCII 字节（`non-ASCII bytes: 0`）；部署副本已就地覆盖，用户可直接重跑。
- **部署指引（E: 环境）**：① 删除旧 `.venv`（3.13 建的，已不可用）；② 安装 Python 3.12（勾选 Add to PATH，注册 `py -3.12`）；③ 双击 `setup_env.bat` 重建 venv 并装依赖（脚本自动选 3.12）；④ 双击 `operation.bat` 启动，访问 `http://localhost:8000`。
- **文档同步**：`功能说明文档.md` 版本记录新增 `### v29（2026-08-28）`；`README.md` 收紧 Windows Python 版本表述并补充 3.13 兼容陷阱说明；本记录追加本条目。

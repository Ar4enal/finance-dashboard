# 金融工作台 — 详细设计（页面原型 + 接口契约）

> 配套文档：`金融工作台_需求与设计.md`（已确认可行）
> 本文件只做**设计**，不含实现代码。确认后据此开发 V1。

---

## 1. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│  浏览器 (React + ECharts)  localhost:8000                    │
│   页面: 总览 / 行情看板 / 持仓 / 组合分析 / 报表 / 财经资讯   │
└───────────────┬─────────────────────────────────────────────┘
                │  HTTP / JSON  (同源，无 CORS)
┌───────────────▼─────────────────────────────────────────────┐
│  FastAPI 后端  (uvicorn, localhost:8000)                     │
│  ├─ 行情代理层: 统一解析各免费源 → 标准 JSON                  │
│  ├─ 业务层: 持仓/盈亏计算、组合绩效、技术指标(MA/MACD)        │
│  └─ 存储层: SQLite (单文件 finance.db)                       │
└───────────────┬─────────────────────────────────────────────┘
                │  HTTPS  (后端服务器调用)
┌───────────────▼─────────────────────────────────────────────┐
│  免费公开数据源 (无需 Key)                                    │
│  新浪行情 hq.sinajs.cn | 腾讯行情 proxy.finance.qq.com        │
│  （v31 起腾讯K线/分钟K/分时统一走 proxy.finance.qq.com：      │
│    web.ifzq.gtimg.cn 会触发 WAF 501）                         │
│  新浪基金 fu_ | 新浪新闻 feed.mix.sina.com.cn                │
└─────────────────────────────────────────────────────────────┘
```
- 前端构建后由 FastAPI 以静态文件托管，单一端口 `8000`，一条命令启动。
- 所有外部行情/新闻均由后端代理，前端只与本地后端通信。

---

## 2. 数据模型与数据库 Schema（SQLite）

### 2.1 自选表 `watchlist`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增 |
| market | TEXT | A / US / HK / GOLD / FUND / BOND |
| code | TEXT | 市场内代码，如 `sh600519`、`aapl`、`00700`、`hf_GC`、`161725`、`sh113050` |
| name | TEXT | 名称（首次拉取时缓存） |
| created_at | TEXT | 添加时间 |

唯一约束：`(market, code)`

> 指数（如上证综指 `sh000001`、纳斯达克 `gb_ixic`）归入 **A / US** 市场，复用个股行情与 K线通道，无需新增类型。

### 2.2 交易记录表 `transactions`
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增 |
| market | TEXT | 同上 |
| code | TEXT | 同上 |
| side | TEXT | BUY / SELL |
| quantity | REAL | 数量（股/份/张） |
| price | REAL | 成交单价 |
| fee | REAL | 手续费（默认 0） |
| trans_date | TEXT | 成交日期 YYYY-MM-DD |
| note | TEXT | 备注 |
| created_at | TEXT | 录入时间 |

### 2.3 净值快照表 `snapshots`（供净值曲线/回撤）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增 |
| snap_date | TEXT | 日期 YYYY-MM-DD（每日一条） |
| total_market_value | REAL | 当日持仓市值合计 |
| total_cost | REAL | 持仓成本合计 |
| cash | REAL | 现金（可选，默认 0） |
| created_at | TEXT | 写入时间 |

> 持仓（position）不单独建表，由 `transactions` 聚合计算得出；净值曲线由 `snapshots` 绘制。

---

## 3. 行情符号约定（Symbol Convention）

系统内部统一用 `market + code` 标识一个标的，后端按市场路由到对应免费源。**指数与个股共用 A股/美股通道**（如 `sh000001`=上证综指、`gb_ixic`=纳斯达克）：

| market | code 示例 | 实时报价源 | K线源 | 说明 |
|---|---|---|---|---|
| A | `sh600519` / `sh000001`(上证) / `sh000688`(科创50) | 新浪 `hq.sinajs.cn` | `sh{x},day,,,N,qfq` | A股个股/指数/场内 |
| US | `aapl` / `ixic`(纳斯达克) / `sox`(费城半导体) | 新浪 `gb_{code}` | `us{CODE}.OQ,day,,,N,qfq` | 美股个股/指数 |
| HK | `00700` | 新浪 `hk{code}` | `hk{code},day,,,N,qfq` | 港股 |
| GOLD | `hf_GC`(纽约金) / `nf_AU0`(沪金连续) / `hf_XAU`(伦敦金)；实物黄金 `GOLD_PHYSICAL` | 新浪 `hq.sinajs.cn`（金价） | 实物黄金/沪金 K线=新浪期货日K线 `AU0`（沪金连续，真实可用）；纽约金 `hf_GC`→新浪 `GC0`、伦敦金 `hf_XAU`→新浪 `XAUUSD` 当前源不可达时诚实提示「该黄金品种K线数据源暂不可用」（v25 修复：此前期货代码走腾讯 `fqkline` 返回空 list 导致全部"暂不可用"，现沪金已可用） | 国内外金价 |
| FUND | `161725` | 新浪 `fu_{code}`（实时价，用于加仓算数量） | **持仓市值用天天基金单位净值 NAV（`fund_kline(code,1)` 最新一条）计算**（v25）：NAV 收盘后才更新，取最新一条即「当日已更新用当日、否则自动为前一交易日」；实时价仅用于录入时算份额 | 公募基金(日净值) |
| BOND | `sh113050` | 腾讯 `qt.gtimg.cn` | `sh{code},day,,,N,qfq` | 可转债 |

**常用指数快捷码（V1 内置，可一键添加自选）：**
- A股：上证综指 `sh000001`、深证成指 `sz399001`、创业板指 `sz399006`、科创50 `sh000688`
- 美股：纳斯达克 `gb_ixic`、标普500 `gb_inx`、道琼斯 `gb_dji`、**费城半导体 `gb_sox`**

> V1 的 **K 线图** 覆盖 A股/美股/港股/北交所/黄金ETF/可转债（均实测可用）；**指数**复用行情通道：**A股/港股指数**走腾讯 `fqkline`（`sh000001` 等，实测返回完整历史），**但腾讯对北交所 `bj` 前缀（如北证50 `bj899050`）只返回 1 天空壳或 WAF 拦截**，故 v22 起 `/api/kline` 的国内/`else` 分支与美股分支对称处理——腾讯拿到 ≥2 根直接用，否则回退新浪 `sina_kline` 取完整历史（北证50 `bj899050` 稳定 120 天），新浪解析异常（代码格式偏差如纯数字 `899050`）也当无数据返回"K线数据为空"而非崩溃；**美股/海外指数**走腾讯 `usfqkline`（`usIXIC` 等，完整历史）——注意 `fqkline` 对美股指数（`usIXIC.OQ`）只返回 1 天，v22 起 `/api/kline` 对美股标的先尝试个股 `fqkline` 路径、不足 2 根时自动改用 `usfqkline` 取指数完整历史，不再依赖写死白名单，内置与自定义美股/海外指数均显示历史；**基金**以净值折线替代、**纽约金/沪金**以黄金ETF(518880)蜡烛图作近似（免费源无独立黄金 K线）。
>
> **v23 金价与 K 线加固**：① 国内金价源 `gold_cn_price()` **优先用沪金 Au99.99 现货（`hf_AU9999`，SGE）**，新浪取不到 `hf_AU9999`（返回空）时**回退沪金连续（`nf_AU0`，上期所黄金期货）**，两者皆失败返回 `available=False`（绝不造假）；实物黄金估值/持仓市值/收益（`_gold_position` / `_positions_data` / `/api/gold`）与实物黄金 K 线均用该 au9999 优先逻辑。② `/api/kline` 新增 `mkt == "GOLD"` 分支：`code == GOLD_PHYSICAL`（实物黄金卡片）用 `gold_kline_symbol()`（au9999 优先）取 K 线；期货指数（纽约金 `hf_GC` / 沪金 `nf_AU0` / 伦敦金 `hf_XAU`）用各自 `code`。③ **端点健壮性**：`tencent_kline` / `tencent_us_index_kline` / `sina_kline` 解析时校验 `rows` 为列表、跳过/容错非数字行、非列表数据显式抛 `DataSourceError`；抽出 `_fetch_kline()` 统一「腾讯优先→不足 2 根回退新浪」并以 `except Exception` 兜底；`get_kline` 整段包 `try/except Exception → fail("K线数据暂不可用")`。**任何未预期异常都返回 JSON 友好提示而非 500**，彻底消除点击指数卡片时前端 `JSON.parse` 报 `Unexpected token 'I', "Internal S"... is not valid JSON` 的崩溃（个别板块/自定义指数代码腾讯返回 200 但行数据异常曾导致 `float()` 抛 `ValueError` 冒泡成 500）。
>
> **v23 确认份额日期（跳过非交易日）**：① 原 `Positions.jsx` 本地 `nextTradingDay()` 仅跳周末、不含法定节假日（用户反馈"确认份额日期没有跳过非交易日"）。② 新增**中国 A股 休市安排**（`datasource.py` 的 `CN_A_SHARE_HOLIDAYS`，2026 年，来源为沪深北交易所官方休市安排公告并经东方财富转发，权威真实；调休上班日已并入对应节假日区间，故只维护连续休市日集合），配套 `is_cn_trading_day(date_str) -> (is_td, covered)` 与 `next_cn_trading_day(date_str, offset) -> (result, covered)`（纯 `datetime.date` 运算，无时区漂移）。③ 新增 `GET /api/fund/confirm-date?trans_date=&qdii=&time=`：**境内基金 15:00 前 T+1 / 15:00 后 T+2；QDII（非国内）恒 T+2**（不受 15:00 前后影响），计算全程**跳过周末与法定节假日（非交易日）**；返回 `confirm_date` / `rule` / `available` / `holiday_source_note`。`covered=False`（年份未被 `CN_A_SHARE_HOLIDAYS` 收录，历史/未来）时 `available=False` 且 `rule` 标注"仅跳过周末，法定节假日未知"，前端显示"⚠️ 休市安排待更新仅跳周末"并提示手动核对。④ 前端 `Positions.jsx` 移除本地 `nextTradingDay`，改在 `trans_date`/`isQdii`/`fund_time` 变化时调用 `/api/fund/confirm-date`（`useEffect` 防抖式拉取，含 `catch` 兜底）；**QDII 不再隐藏交易时间选择器**（仍显示 15:00 前/后选择框，仅作记录、不影响 T+2）。⑤ 真实数据约束：休市安排属公开固定官方规则（非行情虚构数据），以真实来源为准；未收录年份诚实降级并提醒。

---

## 4. 后端 API 接口契约（FastAPI）

基础路径 `/api`。统一返回 `{ "code": 0, "data": ..., "msg": "" }`。

### 4.1 行情
- `GET /api/quotes?market=A&code=sh600519` → 单标的实时报价
  ```json
  { "code":0, "data": {
      "market":"A","code":"sh600519","name":"贵州茅台",
      "price":1306.93,"prevClose":1272.83,"change":34.10,
      "changePct":2.68,"updateTime":"2026-08-24 14:20:37" } }
  ```
- `GET /api/quotes/batch?items=A:sh600519,US:aapl` → 批量报价（看板用）
- `GET /api/kline?market=A&code=sh600519&period=day&count=1000` → K线（v31 起 period 支持 `day|week|month|year|m1|m5|m15|m30|m60`；日K默认档 1000、周K 640、月K 500、年K 60、分钟 240；周/月/年优先腾讯原生、年K=月K聚合；响应含 `period` 字段）
- `GET /api/kline/intraday?market=A&code=sh600519` → 当日分时（v31 新增；返回 `{available, times, price, avg_price, volume, amount, prev_close, note}`）
  ```json
  { "code":0, "data": {
      "dates":["2026-08-17",...],
      "ohlc":[[open,close,low,high],...],   // 与 ECharts candlestick 对齐
      "volume":[...],
      "ma5":[...],"ma10":[...],"ma20":[...],
      "macd":{"dif":[...],"dea":[...],"bar":[...]} } }
  ```

### 4.2 自选
- `GET /api/watchlist` → 列表
- `POST /api/watchlist` body `{market, code}` → 新增（自动校验代码可解析）
- `DELETE /api/watchlist/{id}` → 删除

### 4.3 交易与持仓
- `GET /api/transactions?market=&code=&page=` → 流水
- `POST /api/transactions` body `{market,code,side,quantity,price,fee,trans_date,note}` → 录入
- `PUT /api/transactions/{id}` / `DELETE /api/transactions/{id}`
- `GET /api/positions` → 持仓（聚合计算）
  ```json
  { "code":0, "data":[ {
      "market":"A","code":"sh600519","name":"贵州茅台",
      "quantity":100,"avgCost":1250.0,"cost":125000.0,
      "price":1306.93,"marketValue":130693.0,
      "pnl":5693.0,"pnlPct":4.55 } ] }
  ```

### 4.4 组合分析
- `GET /api/portfolio/summary` → `{totalMarketValue,totalCost,totalPnl,totalPnlPct,todayPnl}`
- `GET /api/portfolio/allocation?by=market|code` → 资产分布（饼图）
- `GET /api/portfolio/performance?range=30|90|all` → 净值曲线 + 最大回撤
  ```json
  { "code":0, "data":{
      "dates":[...],"equity":[...],"cost":[...],
      "maxDrawdown":-8.32,"totalReturn":12.5 } }
  ```
- `GET /api/portfolio/pnl-analysis?type=day|month|year|cum&range=YYYY-MM-DD|YYYY-MM|YYYY` → **收益分析**（v23 新增，v24 修正计算口径与配色）。统一以「相邻交易日对（上一交易日→当日）的市值差值」为日收益基本单元，全部来自真实每日快照：
  - **日收益** `day`：取 ≤target 的最近两条快照（当日 + 上一个交易日），combo = 当日−上一交易日、details = 各持仓当日−上一交易日（仅这一对，非首末差值）；`range` 缺省今天。
  - **月/年收益** `month`/`year`：取所选范围（YYYY-MM / YYYY）内全部相邻交易日对，combo = 每对组合日收益**累加**、details = 每持仓日收益累加；`range` 缺省本月/今年。
  - **累计收益** `cum`：全部相邻交易日对累加，combo = **所有持仓「最新市值−最早市值」加总**（持仓层面，与 details 自洽，非组合级首末差值）。
  - 返回 `{type, range, start_date, end_date, combo_pnl, combo_pnl_pct, combo_edited, available, note, details:[{market,code,name,pnl,edited}], calendar:[{date,pnl}]}`。`details` 为每持仓区间收益（含 `edited` 标记是否用户编辑过）；`calendar` 为每日组合收益序列（日历图用）。区间内某持仓某天无快照则跳过该日避免误导（**新增持仓当日才首次出现 → 不计入日收益**）；相邻交易日不足（day 需 ≥2 条）或区间无快照时 `available=false` + `note` 提示。
  - **收益可编辑（v25）**：`details[].pnl` 与 `combo_pnl` 可被用户手动覆盖并持久化——表 `pnl_override(market,code,pnl_type,range_val,detail_pnl,combo_pnl,UNIQUE(market,code,pnl_type,range_val))`，`code='__COMBO__'` 为组合总收益行；接口 `POST /api/pnl-override`（保存/清除）。前端收益分析顶部「当日/本月/本年/累计收益」与明细「区间收益」列均为可编辑 input，失焦即存，刷新/重载保留。**日历图点击某天 → 切日收益模式并定位到该日期并刷新**（前端 `onCalClick`）。
  - **日收益编辑向上传播（v27 修复）**：`pnl_override` 按层级（`day`/`month`/`year`/`cum`）独立生效——`day` 级覆盖视为「该日收益单元的最终值」，在 `pnl_analysis` 聚合 month/year/cum 时，对每一天每个持仓优先采用 `pnl_override(pnl_type='day', range_val=当天)` 的 `detail_pnl` 替代原始市值差值；组合当日收益先由持仓修正值求和、再优先采用组合级 `day` 覆盖；该值向上累加进月/年/累计，并写入 `calendar` 字段（日历图同步）。`month`/`year`/`cum` 自身被直接编辑时仍按原层级覆盖（不反向分解到日）。**修复前**：day 覆盖仅在 day 视图自身生效（第 1168/1175 行只在该 type 下套用），导致月/年/累计聚合与日历图不联动。
  - **配色（v24）**：与全站 K线一致——**红色=赚（正收益）、绿色=亏（负收益）**；日历图 visualMap `['#3fb950'(绿/亏), 灰, '#f85149'(红/赚)]`、柱状图 `v>=0?红:绿`（前端 `Analysis.jsx` 的 `buildBarOption`/`buildCalendarOption`）。
  - **数据基础**：`position_snapshots` 表（`snap_date,market,code,market_value`，`UNIQUE(snap_date,market,code)` 幂等）；`portfolio_summary()` 写组合快照时同步 `ensure_position_snapshots_today()` 记录每个持仓当天市值（行情不可用者当天不记）。
- `GET /api/portfolio/pnl-series?days=30&kind=holding|cum` → **总览卡片近一个月收益走势**（v26 新增）。取最近 N 天 `snapshots` 快照（`snaps[-days:]`），相邻交易日对累加：
  - `daily_pnl[i]` = `cur.total_market_value − prev.total_market_value`（每日组合收益）。
  - `cumulative_pnl[i]`：`kind=holding` 时为 `cur.total_market_value − cur.total_cost`（当前持仓收益走势）；`kind=cum` 时为「累计日收益累加（含已实现）」。
  - 返回 `{dates, daily_pnl, cumulative_pnl, latest_date}`；`latest_date` 为最右当前日期。前端 `Dashboard.jsx` 以 ECharts line + `dataZoom`（滑块/滚轮）渲染，可向左滑动至有记录的第一天；天数不足（<2 快照）时弹窗诚实提示「暂无收益记录」，绝不造假。
- `GET /api/portfolio/allocation?by=market|code` → 资产分布饼图（含 `items` 明细）。**v23 修复**：饼图点击扇区弹出的持仓明细列表，前端渲染时对 `items` 副本按 `market_value` 降序排序（`[...items].sort((a,b)=>(b.market_value||0)-(a.market_value||0))`，不改原数据）；点击明细行弹出的全局 K 线窗口层级修复——`.alloc-modal-mask` z-index 由 1000 降为 90（低于全局 K 线弹窗 `.modal` 的 100），使 K 线抽屉浮于明细列表窗口之上。
- 持仓管理页排序持久化（v23 修复）：`posSort`（`pnl`/`mv`/`cost`）初始值从 `localStorage('fw_pos_sort')` 读取、切换时写入，**跳转界面再回来仍保留上次选择**（组件卸载重建不丢失）。

### 4.5 报表与导出
- `GET /api/reports/export?type=csv` → 持仓+交易 CSV 下载

### 4.6.1 数据整体导入导出（跨机器迁移）
- `GET /api/data/export` → 汇总全部用户数据为 JSON（`app:finance-workbench, version:1`），写入项目根目录 `持仓数据_export.json` 并下载。覆盖 11 张用户表：
  `watchlist`、`transactions`、`snapshots`、`position_snapshots`、`gold_holding`、`gold_transactions`、`asset_profit`、`position_override`、`pinned_positions`、`custom_indices`，以及由 `asset_profit`/`position_override` 字典转出的列表。
  - **`position_snapshots`（持仓级每日快照）**：v24 起纳入导出——收益分析模块「日/月/年/累计收益 + 每持仓区间明细」依赖此表，历史天数不可重生（过去某天持仓市值无法回算），必须随迁移带走，否则换机器后历史收益明细全空。
- `POST /api/data/import` → 整体恢复：先 `DELETE` 清空全部用户表，再按 payload 写入（按 `(snap_date,market,code)` 等唯一键幂等），返回各类统计 `imported`；旧版导出文件无 `custom_indices` 时回退内置 `DEFAULT_INDICES` 预置。
- 数据范围：覆盖全部用户输入数据，**不含**实时行情/K线等动态数据（导入后自动重新拉取）；前端 `localStorage` 偏好（输入框历史、排序选择、刷新频率）不纳入，与单用户本地架构一致。

### 4.6 财经资讯
- `GET /api/news?keyword=&limit=30` → 滚动要闻
  ```json
  { "code":0, "data":[ {
      "title":"美扩大国债回购难抑收益率 比特币黄金应声大涨",
      "summary":"美国财政部上周宣布将长期国债单次回购上限翻倍至40亿美元……",
      "url":"https://finance.sina.com.cn/...",
      "time":"2026-08-24 14:30:27" } ] }
  ```

---

## 5. 前端页面原型（布局草图）

### 5.1 总览 Dashboard `/`
```
┌──────────────────────────────────────────────────────────┐
│  总资产 ¥1,234,567   今日 +¥3,210 (+0.26%)   总收益 +12.5% │
├───────────────────────┬──────────────────────────────────┤
│  组合净值曲线 (ECharts)│  资产分布饼图 (按市场)            │
│  ─────────────────    │  A股 55% / 美股 20% / 黄金 15%…  │
├───────────────────────┴──────────────────────────────────┤
│  持仓 TOP（名称 / 市值 / 盈亏%）  + [去持仓管理]           │
└──────────────────────────────────────────────────────────┘
```

> **v23 持仓盈亏分布（v24 修正排序方向）**：`Dashboard.jsx` 的「持仓盈亏分布」柱状图取数后做派生排序——`pnlAll = positions.filter(p => p.data_available !== false).slice().sort((a,b) => (b.pnl ?? -Infinity) - (a.pnl ?? -Infinity))`（**降序：收益最高在左 → 收益最低/亏损在右**；v24 由原升序 `a.pnl - b.pnl` 改为 `b.pnl - a.pnl`，用户反馈原方向反了），`pnlMaxScroll = max(0, len - 10)`，`pnlView = pnlAll.slice(safeOffset, safeOffset + 10)`。ECharts 仅渲染 `pnlView`（最多 10 根），下方 `<input type="range" max={pnlMaxScroll} value={safeOffset}>` 滑块拖动改 `setPnlOffset`，滑块右侧展示 `第 N–M 个 / 共 X 个持仓` 窗口位置；持仓数 ≤10 时 `pnlMaxScroll === 0`，滑块与文案整体不渲染。组件 `pnlOffset` 状态在 `useEffect(load)` 刷新时由 `safeOffset = min(pnlOffset, pnlMaxScroll)` 夹紧，避免持仓减少后越界。

> **v26 总览卡片点击查看收益走势**：「当前持仓收益」「累计收益」两张 stat 卡片 `onClick` 调 `openPnlDetail(kind)` → `api.pnlSeries(30, kind)` → 置 `pnlDetail` 状态弹窗；`buildPnlSeriesOption(dates, daily, cumulative)` 用 ECharts line + `dataZoom`（滑块 `type:'slider'` + 滚轮 `type:'inside'`），最右为 `latest_date`（当前日期），向左滑动可至第一条记录；「暂无收益记录」时空状态提示。弹窗遮罩 `.alloc-modal-mask`（z-index 90，复用全局 K 线弹窗之下的层级）+ `.modal-card`（`.modal-head`/`.modal-close`/`.modal-body`/`.chart-lg`/`.pnl-series-foot`）。

### 5.2 行情看板 `/quotes`
```
┌──────────────────────────────────────────────────────────┐
│  搜索添加: [代码/名称____] [+ 加自选]   刷新: 每10s ●      │
│  指数快捷: [+上证综指][+科创50][+纳斯达克][+费城半导体]       │
├──────┬──────┬────────┬────────┬────────┬────────┬─────────┤
│ 名称 │ 市场 │ 最新价 │ 涨跌额 │ 涨跌幅 │ 更新  │ 操作     │
│ 茅台 │ A股  │1306.93 │ +34.10 │ +2.68% │14:20  │[K线][删] │
│ AAPL │ 美股 │ 309.35 │ -1.95  │ -0.63% │09:46  │[K线][删] │
└──────┴──────┴────────┴────────┴────────┴────────┴─────────┘
点击 [K线] → 右侧抽屉/弹窗显示蜡烛图 + MA + MACD
```

> **v23 排序与展示规则**：行情看板「自选基金（FUND）」与「自选标的」两列表，前端在渲染时按 `pct`（日涨跌幅）排序——`sortByPct(arr, dir)` 默认 `dir='desc'`（涨幅高→低），`pct` 缺失（行情不可用）者沉底；组件 `fundDir` / `watchDir` 状态控制方向，「日涨跌幅」「涨跌幅」表头可点击切换升/降序（`↓`/`↑` 指示），两表方向相互独立。`SHOW_PENETRATION=false` 时行情看板**不渲染**「场内基金穿透」独立模块（代码保留，置 `true` 即恢复），单只基金穿透仍可在「基金详情弹窗」查看。

> **v26 预估日收益列 + 异动跟踪**：① **预估日收益列**：「自选基金」「自选标的」两表新增「预估日收益」列 = 当日涨跌幅(%) × 当前持仓市值；`fetchWatchlist` 在拉取 `api.positions()` 时构建 `(market:code) → 持仓市值` 映射 `mvMap`（基金 code 经 `normCode` 去 `fu_` 前缀以匹配自选列表），渲染时 `valOf(it,'pnl') = it.pct!=null && mvMap[key]!=null ? it.pct/100*mv : null`，无持仓（null）显示「—」。② **排序重构**：原 `fundDir/watchDir` 升级为 `fundSort/watchSort`（`{key:'pct'|'pnl', dir}`），`toggleSort(set,cur,key)` 同列翻转方向、异列默认降序；`sortRows`/`valOf` 统一按 `pct`（日涨跌幅）或 `pnl`（预估日收益）排序，缺失值沉底；「日涨跌幅」「预估日收益」两表头均可点击切换（独立箭头指示）。③ **异动跟踪**：两模块头部均提供阈值下拉 `select.anomaly-sel`（1%~10%，默认 5%，`localStorage('fw_anomaly_pct')` 持久化）；`isAnomaly(it) = it.pct!=null && (it.pct>阈值 || it.pct<−阈值)` 时该行加 `.anomaly` 高亮 + 「异动」徽标 `anomaly-tag`，并在看板顶部渲染 `.anomaly-banner` 横幅列出所有触发产品（名称 + 涨跌幅），回落阈值内自动取消。所有计算基于真实持仓市值与真实行情涨跌幅，无虚拟数据。
>
> **v31 模块「当日收益总和」**（仅前端 `Quotes.jsx`）：自选基金/自选标的两模块标题行（异动阈值**左侧**）各显示 `pnlBadge(label, sum)`：`sum = pnlSumOf(rows) = Σ valOf(row,'pnl')`（口径与「预估日收益」列完全一致；基金模块对 `funds` 行集、标的模块对 `watchlist` 行集**各算各的**，自选基金模块含自动同步的持仓基金）。展示 `¥+xx.xx`：正 `up` 红（#f85149）、负 `down` 绿（#3fb950）、无持仓收益灰 `¥0.00`；title 注明公式；随行情轮询（30s 报价刷新驱动 `funds/watchlist` state）自动更新。
```

### 5.3 K 线图组件（抽屉/弹窗）
```
┌──────────────────────────────────────────────────────────┐
│  贵州茅台 (sh600519) 日K               [日K][周K] [MA][MACD]│
├──────────────────────────────────────────────────────────┤
│  蜡烛图 + MA5/MA10/MA20 叠加 (ECharts candlestick)         │
│  ── 成交量柱状                                            │
│  ── MACD: DIF/DEA 线 + 红绿柱                            │
└──────────────────────────────────────────────────────────┘
```
- MA / MACD 由前端根据 OHLC 计算（公式见 §7）。

### 5.4 持仓管理 `/positions`
```
┌──────────────────────────────────────────────────────────┐
│  [+ 录入交易]                                             │
├──────┬──────┬──────┬────────┬────────┬────────┬──────────┤
│ 名称 │ 数量 │ 成本 │ 现价   │ 市值   │ 浮动盈亏│ 收益率   │
│ 茅台 │ 100  │1250  │1306.93 │130693  │ +5693  │ +4.55%   │
└──────┴──────┴──────┴────────┴────────┴────────┴──────────┘
录入弹窗: 市场[下拉] 代码[输入] 方向[买/卖] 数量 价格 手续费 日期 备注
```

### 5.5 组合分析 `/analysis`
```
┌───────────────────────┬──────────────────────────────────┐
│  资产分配 (饼图)       │  绩效指标                        │
│  (按市场/按标的切换)   │  总收益 12.5% / 最大回撤 -8.32%  │
│                       │  夏普(可选) / 今日盈亏           │
├───────────────────────┴──────────────────────────────────┤
│  净值 vs 成本 曲线（叠加）                                │
└──────────────────────────────────────────────────────────┘
```

### 5.6 报表 `/reports`
```
┌──────────────────────────────────────────────────────────┐
│  持仓报表 / 交易流水   [导出 CSV] [打印]                  │
│  表格 + 汇总行                                          │
└──────────────────────────────────────────────────────────┘
```

### 5.7 财经资讯 `/news`
```
┌──────────────────────────────────────────────────────────┐
│  筛选: [关键词______] [刷新]                               │
├──────────────────────────────────────────────────────────┤
│  ● 美扩大国债回购难抑收益率 比特币黄金应声大涨            │
│    美国财政部上周宣布将长期国债单次回购上限翻倍至40亿美元… │
│    [原文链接]  2026-08-24 14:30                           │
└──────────────────────────────────────────────────────────┘
```

---

## 6. 前端组件与状态

- 框架：React 18 + Vite + TypeScript（可选 JS）
- 图表：ECharts（candlestick / line / pie）
- 状态：轻量（Zustand 或 React Context），无需重型框架
- 组件树：
  ```
  App
  ├─ Layout (侧边栏导航)
  ├─ Dashboard
  ├─ Quotes (QuoteTable + AddWatchlist + KlineDrawer)
  ├─ Positions (PositionTable + TradeForm)
  ├─ Analysis (AllocationPie + Metrics + EquityChart)
  ├─ Reports (Table + ExportBtn)
  └─ News (NewsList + Filter)
  └─ shared: EChart wrapper, KlineChart(MA+MACD), Card
  ```
- **K 线图例配色机制（v31 起，重要）**：ECharts legend 图标颜色默认取自 series 的**调色板色（palette）**，与 `lineStyle.color` / `itemStyle.color` **无关**。因此凡在 series 上写自定义色的地方，必须**同步在 `legend.data[].itemStyle.color` 再写一遍**，否则图例与实际线色对不上。
  - 单色系列（MA5/MA10/MA20/DIF/DEA）：由 `KlineChart.jsx` 的 `buildLegendData()` 统一补 `itemStyle.color`。
  - 双色系列（`itemStyle.color` 为红涨绿跌函数的成交量/MACD，以及红涨绿跌的 K线）：legend 取不到函数返回值，改用 `dualColorIcon()` 生成的 **SVG data-URI 图例图标**（`icon: 'image://data:image/svg+xml;...'`）。
  - **image 图标不会随取消勾选自动置灰** → 监听 `legendselectchanged`，用 **merge 模式**（`setOption(opt, false)`，notMerge 会重置全部选中态）重建 `legend.data`，把未选中项换成灰版图标。
  - 唯一颜色源：`KlineChart.jsx` 导出的 `KL_COLORS`（K线红 `#f85149` / 绿 `#3fb950`；MA5 `#d29922`、MA10 `#58a6ff`、MA20 `#a371f7`；DIF `#58a6ff`、DEA `#d29922`；买入 `#ff8c00`、卖出 `#1e90ff`；置灰 `#ccc`）。**新增任何 K线相关配色都必须在此常量中登记**，弹窗头部买卖点图例与图内 markLine 标注共用它。
  - `EChart.jsx` 提供 `onChartReady` 回传 ECharts 实例，供需要动态改图例的场景使用。
- **K 线周期与分时架构（v31 起）**：`KlineChart.jsx` 顶部周期 tab：`分时|1分|5分|15分|30分|60分|日K|周K|月K|年K`。
  - **默认周期**：分时（A/HK/BOND 有真实源默认分时；**指数卡片经 `preferMinute` 无条件先试分时**，其余市场直接日K）。分时请求失败/不可用时**自动落回日K**并提示原因（用户手动点分时才显示错误）。
  - **后端取数链路**（`app/main.py` `_kline_period` + `datasource.py`）：
    - day/week/month：腾讯 `fqkline` 原生（`proxy.finance.qq.com`）→ 不足回退新浪 `sina_kline`（scale 240/1200/7200）→ 再不足用日K聚合兜底（北交所）。
    - year：腾讯 `month` → 新浪 `month` → **`aggregate_kline(月K, 'year')`**（腾讯 year 仅返回当年 1 根，故统一月K聚合；上证可到 1990-12 基点、个股自上市起）。
    - 分钟K m1/m5/m15/m30/m60：腾讯 `kline/mkline`，**仅 A股（含指数）**；港股/美股/转债返回空 → 明确提示不可用。
    - 当日分时：**v31 起为三源链（腾讯当日→东财最近交易日→本地跟踪文件，见下文专块）**；腾讯 `minute/query` 逐分钟粒度，A股个股/指数、港股可用，**volume/amount 为当日累计值**（A股 volume 单位=手、港股=股），均价线=累计额/累计股（仅个股/港股，A股指数无均价概念置 null），量柱=当分钟量（累计差分）。
    - 基金（仅日净值）/黄金（仅新浪期货日K）：period≠day 时用真实日序列经 `aggregate_kline` 聚合（周=ISO周/月/年）。
  - **历史深度档位**（count 默认）：日K 1000（A股个股前复权源上限约 641 根≈2.6年、指数/港股 1000+ 根≈4年）、周K 640（≈12年）、月K 500（源全量）、年K 60（=月K聚合实际年数）、分钟 240。
  - **腾讯 K 线列序（重要，v31 修正）**：腾讯 `fqkline/mkline` 行内序为 `[日期,开,收,高,低,量]`；`datasource` 层统一输出 `ohlc=[开,收,低,高]`（ECharts candlestick 所需、与新浪一致）。**新增任何直接解析腾讯 K 线的代码必须沿用此列序**。
  - **聚合函数** `aggregate_kline(k, period)`：周=ISO 周（周一始）、月=自然月、年=自然年；组内 open=首日开、close=末日收、high=组内最高、low=组内最低、volume=求和；date=组内末日（与腾讯原生周/月K取末日一致）。已用茅台 qfqday 聚合周K 与腾讯原生周K 比对 130 周 **0 差异**。
  - 分时/分钟K 不显示买卖点标注（买卖点按交易日对齐，分钟K无法对齐）；买卖点标注仅日K及以上周期。
  - 图名动态（如「5分 · MA」）；分钟K x 轴标签显示 HH:MM。
- **分时三源链与本地跟踪（v31 起）**：统一入口 `app/main.py` `_intraday_for(market, code)`（30s 内存缓存），顺序：
  1. **腾讯当日实时** `minute/query`（A/港股盘中、美股其交易时段）→ `source=tencent`；
  2. **东财最近交易日** `push2delay trends2`（A/港股=当日；美股休市时段返回上一完整美股交易日全天分时）→ `source=eastmoney`，secid 映射见 `datasource.INDEX_EM_SECID`（只收录同标的项；纳指综合 IXIC / 费半 SOX 东财无）；
  3. **本地跟踪文件** `data/intraday_track/{mkt}_{code}.json` → `source=local`；
  4. 全无 → `available=false` + 明确 note + `intraday_track.ensure_tracked()` 登记（腾讯/东财有源分支自动 `remove_tracked`）。
  - **本地跟踪**（`app/intraday_track.py`，仅"无源指数"启用）：后台 daemon 线程每 60s `tick()`，对名单内指数在**其市场交易时段**（A/港股=北京时间窗口+交易日历；美股=美东时区窗口）用新浪报价采样；**交易日重置**：采样点市场日期 ≠ 文件 `trade_date` → 清空重记；同分钟覆盖、平线抑制（60 分钟价格不变视为非活跃，防休市/节假日平线）；报价新鲜度用新浪源侧 `src_date/src_time` 校验（A股 f[30]/f[31]、港股 f[17]/f[18]）。
  - **接口**：`GET /api/kline/intraday?market=&code=`（详情，含 avg/volume）、`GET /api/kline/intraday/batch?items=A:sh000001,US:ixic`（卡片小图轻量批量）；响应均带 `source`/`trade_date`，前端据此标注"数据日期（最近交易日）"。
  - **前端**：`Quotes.jsx` 指数卡片 `.ic-pct` 下方 `IndexSpark`（SVG polyline 迷你分时，60s 轮询 batch）；指数卡片点开 K 线传 `preferMinute`（`kline.jsx`/`KlineChart.jsx`）——无条件先请求分时，无当日源自动落回日K并提示（含休市/本地跟踪说明）。
  - **口径**：本地分时跟踪文件为行情衍生记录，不纳入持仓/港股数据导入导出。
- 行情刷新（分级，默认偏省请求）：
  - 实时报价：**每 30 秒**轮询 `/api/quotes/batch`（批量取数，不做逐只请求）
  - K线/技术指标：打开图表时拉取一次 + 手动刷新
  - 基金净值：**每日一次**
  - 财经资讯：**每 60 秒**
  - 持仓/组合计算：跟随报价（30s）联动重算
  - 间隔**可在前端设置**（30s / 60s / 暂停）；**非交易时段自动降频**（收盘后报价降为 5 分钟一次或暂停）

---

## 7. 技术指标计算（本地，无外部依赖）

- **MA(N)** = 最近 N 根 K 线收盘价的简单移动平均。
- **MACD**
  - `EMA12 = 收盘价的12日指数平滑`
  - `EMA26 = 收盘价的26日指数平滑`
  - `DIF = EMA12 − EMA26`
  - `DEA = DIF 的9日 EMA`
  - `MACD柱 = (DIF − DEA) × 2`
- 前端或后端均可计算；V1 放在后端 `/api/kline` 一并返回，前端直接画图。

---

## 8. 开发里程碑（V1 交付切片）

1. **骨架**：FastAPI + SQLite 建表 + 静态托管 React 构建产物；一条命令启动。
2. **行情代理**：统一解析新浪/腾讯 → 标准 JSON；`/api/quotes`、`/api/kline`、`/api/news`。
3. **看板**：自选 CRUD + 实时报价表 + 定时刷新 + K线抽屉(MA/MACD)。
4. **持仓**：交易录入/编辑/删除 + 持仓聚合 + 盈亏实时计算。
5. **组合/报表**：净值曲线、资产分布、绩效/回撤、CSV 导出。
6. **资讯**：滚动新闻列表 + 关键词筛选。
7. **打磨**：空状态、错误处理、README 启动说明。

> 全部接口与数据源已实测可达，技术栈在 Linux 可运行。设计确认后进入编码。
